// readme plugin retiré temporairement — réintégration ultérieure

allprojects {
    group = "education.cccp"
    version = "0.0.3"

    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}
