= Backlog — Codebase Gradle
:sectnums:
:date: 2026-05-19

_Ce fichier est le suivi prioritaire des tâches. L'INDEX.adoc est la source de vérité pour les statuts globaux._

== EPIC 1 : Initialisation gouvernance agent codebase-gradle

**Statut** : EN COURS (6/8 items)
**Priorite** : P0

* [x] Migrer .agents/ + fichiers gouvernance depuis quizz-benchmark-gradle
* [x] Creer AGENT.adoc adapte a codebase-gradle (cercles de confiance, BYOK, fine-tuning)
* [x] Creer INDEX.adoc (dashboard EAGER)
* [x] Mettre a jour BACKLOG.adoc (ce fichier)
* [x] Creer PROMPT_REPRISE.adoc pour session 1
* [x] Mettre a jour AGENT_GOVERNANCE.adoc dans configuration/ (ajouter codebase au portefeuille)
* [ ] Snapshot vision-archive post-migration dans configuration/
* [ ] Commit initial codebase-gradle

== EPIC 2 : Expert Anonymisation LangChain4j — Prérequis Transverse
**Statut** : MVP0 TERMINE (session 009)
**Priorite** : P0

* [x] Interface AnonymizationExpert + factory + deterministic fallback
* [x] 3 scénarios Cucumber @wip

== EPIC V-7 — Resolver + --model CLI + Completion Tracking (Session 068)
**Statut** : TERMINE (2026-05-26, 34/34 PASS)
**Priorite** : P0

* [x] LlmProviderResolver — model→provider mapping (Gemma4 pool ROUND_ROBIN)
* [x] VibecodingTask --model option (empty=deterministic backward compat)
* [x] TokenTracker.trackCompletion() wired in callLLMNode
* [x] Fix VibecodingTaskTest audit JSONL assertion

== EPIC V-8 — Resume Session + Dashboard CLI (Session 069)
**Statut** : TERMINE (audité 2026-06-14 — 5/5 codés, backlog stale)
**Priorite** : P0
**Depends on** : EPIC-V-7 (session 068)

* [x] --resume <sessionId> on VibecodingTask (VibecodingTask.kt:78-79, @Option + flow complet lignes 126-167)
* [x] Load state via SessionRepository.getSession() (SessionRepository.kt:37 interface + :180-197 impl R2DBC)
* [x] Reconstruct VibecodingState from SessionRecord (VibecodingGraph.kt:237-251 companion resumeSession)
* [x] Dashboard CLI: ./gradlew vibecodingDashboard (DashboardTask.kt 78 lignes + Dashboard.kt 95 lignes)
* [x] Refactoring: consolidate LlmProviderResolver pool integration (LlmProviderResolver.kt:25-67 gemma4Pool lazy singleton)

== EPIC V-6 — Cablage Ollama Cloud Docker + ConnectionFactory R2DBC (Session 065)
**Statut** : EN COURS (6/10 — audité 2026-06-14)
**Priorite** : P0

=== P0 — ConnectionFactory + Persistance R2DBC

* [x] Injecter `ConnectionFactory` (R2DBC) dans constructeur VibecodingGraph (VibecodingGraph.kt:46 + lazy effectiveSessionRepository :53-55)
* [x] Test integration avec vrai SessionRepository R2DBC via Testcontainers (VibecodingTaskTest.kt:197-260 E2E pgvector/pgvector:pg17)
* [x] `updateSession()` avec `TokenTracker.cost`/`estimatedCost` dans la boucle de persistance (SessionRepository.kt:118-148 + VibecodingGraph.kt:378-392 persistStateNode)
* [x] Verifier createSession + updateSession end-to-end (Testcontainers) (VibecodingTaskTest.kt:233-260)

=== P0 — OllamaLlmProvider cloud dockerise

* [ ] Scanner ports 11434→11465 sur l'hote (`curl http://localhost:{port}/api/tags`) — PARTIEL : OLLAMA_POOL_PORTS env var declaratif, pas de scan actif curl
* [x] `OllamaLlmProvider` via `OllamaChatModel.builder().baseUrl()` (OllamaLlmProvider.kt:80-85)
* [x] Utiliser `gpt-oss:120b-cloud` comme modele principal (LlmProviderResolver.kt:30 DEFAULT_MODEL)
* [ ] OllamaPoolKeyAdapter : wrapper resilient quota/quota depasse + rotation instances — PENDING : pas de classe dediee, quota gere directement dans OllamaPool
* [ ] Pattern docker SSH key : article 0120 (dual-abonnement Ollama docker) — PENDING : pas de code SSH, HTTP localhost direct
* [ ] Test Cucumber : "LLM calls Ollama cloud and handles quota exceeded" — PARTIEL : EpicV6CucumberRunner existe mais scenarios = error recovery/retry, pas specifiquement Ollama cloud

=== P1 — Nettoyage technique

* [x] Supprimer warnings `!!` inutiles lignes 139/257 de VibecodingGraph.kt
* [x] Nettoyer BACKLOG.adoc : EPIC 3 statut A FAIRE → TERMINE (fait session 046)

=== Reference

- Article 0120 : `/home/cheroliv/workspace/office/sites/cheroliv.com/jbake/content/blog/2026/0120_cumuler_deux_abonnements_ollama_pro_docker_post.adoc`

== EPIC 3 : Architecture Multi-Canal Convergent — Formalisation
**Statut** : TERMINE (2026-05-19, session 046)
**Priorite** : P0

_Pourquoi_ : Le contexte fourni au LLM ne doit pas être un bloc monolithique. Il est
composé de canaux convergents (EAGER/LAZY, RAG pgvector, Graphify, Ressources).
Le `CompositeContextBuilder` (déjà fait via EPIC 9) agrège ces canaux.

=== Les Quatre Canaux

[cols="1,2,3,3"]
|===
| Canal | Technologie | Type d'information | Rôle
| **EAGER/LAZY** | Fichiers AsciiDoc + `.agents/` | Règles, procédures, backlog, historique | Gouvernance déterministe
| **RAG pgvector** | LangChain4j + ONNX + PostgreSQL | Documents, code source, SPG, articles | Similarité vectorielle
| **Graphify** | `graph.json` + plantuml-gradle | Nœuds, arêtes, communautés, dépendances | Relations exactes
| **Ressources** | Dossiers encyclopédies, archives, datasets | Données brutes nettoyées, corpus, historique long | Mémoire froide
|===

=== Sous-tâches

* [x] Formaliser les interfaces de chaque canal (`ContextChannel` sealed class) — fait via EPIC 9, session 046
* [x] Implementer le `ContextBudget` : token budget par canal (EAGER 40%, RAG 30%, Graphify 20%, Ressources 10%) — fait via EPIC 9, session 046
* [x] Tests : mesurer la pertinence des réponses avec 1, 2, 3, 4 canaux activés — fait via EPIC 4 Benchmark (session 026)

== EPIC 4 : Benchmark de perception spatiale LLM
**Statut** : MVP1 TERMINE (session 026 : flash + Graphify 0% erreur)
**Priorite** : P0

* [x] 4 canaux réels (EAGER/LAZY/RAG/Graphify)
* [x] Scope LOCAL/WORKSPACE
* [x] Comparaison AsciiDoc, gate <5%
* [x] 175 classifications 0% erreur 128K

== EPIC 5 : Pipeline fine-tuning experts CDA/FPA — MIGRÉ vers training-gradle
**Statut** : MIGRÉ (Session 110 → training TRN-8, ~21 pts)
**Priorite** : N/A — codebase fournit le socle technique (ONNX, RAG pgvector, qualité EPIC 6)
**Cible** : training-gradle (Newark N2) — BACKLOG.adoc §EPIC TRN-8

== EPIC 6 : Integration ONNX — boucle qualite
**Statut** : TERMINE (session 056, 2026-05-23)
**Priorite** : P1

* [x] Analyse de sentiment sur les outputs experts (ONNX)
* [x] Detection de hors-sujet (classifieur ONNX)
* [x] Detection PII résiduelle post-anonymisation (double vérification)
* [x] Seuil de rejet configurable
* [x] Circuit de retry (max 3 iterations, feedback au LLM)

== EPIC 7 : Wiring LangChain4j + pgvector experts
**Statut** : A FAIRE
**Priorite** : P1

* [ ] Persistance partagee entre agents (PostgreSQL + pgvector)
* [ ] API d'appel aux experts fine-tunes (via Ollama)
* [ ] Integration avec le dispatcher deepseek-v4-pro
* [ ] Anonymisation pre-query automatique via EPIC 2

== EPIC 8 : Exposer experts via Ollama (consommation par plugins)
**Statut** : A FAIRE
**Priorite** : P2

* [ ] Packaging ONNX → Ollama
* [ ] Anonymisation des modèles exposés (zéro PII dans les weights)
* [ ] Modeles pre-charges dans Magic Stick
* [ ] Documentation consommation par slider-gradle, plantuml-gradle, bakery-gradle

== EPIC 9 : Opencode Augmenté — Réalité Augmentée Développeur
**Statut** : TERMINE (15/15 US DONE — sessions 001-021)
**Priorite** : P0

* [x] US-9.1 Container pgvector éphémère
* [x] US-9.2 Dataset de test minimal
* [x] US-9.3 Embeddings ONNX AllMiniLmL6V2
* [x] US-9.PROD Bridge to Production
* [x] US-9.4 Walk récursif typé
* [x] US-9.5 Indexation metadata Kotlin
* [x] US-9.6 Indexation YAML/JSON configs
* [x] US-9.7 Validation batch
* [x] US-9.8 VectorQueryService
* [x] US-9.9 Query filtrée par type
* [x] US-9.10 CompositeContextBuilder (EAGER/RAG/Graphify/Ressources + token budget 40/30/20/10)
* [x] US-9.11 OpencodeInjector
* [x] US-9.12 augmentOpencode
* [x] US-9.13 Benchmark pertinence
* [x] US-9.14 Segregation Vision/Opinion

== EPIC 10 : Instrumentation Gradle du Pattern STIMULUS
**Statut** : TERMINE (10/10 sous-taches — sessions 022 + 025)
**Priorite** : P0

* [x] DilutionExecutor (injection automatique + backup + dryRun)
* [x] StimulusDetector (scan workspace/*.adoc + stale > 2j)
* [x] detectStimuli + detectStaleStimuli Gradle tasks
* [x] StdoutFormatter.error()
* [x] 8/8 JUnit5 PASS

== EPIC 12 : Dilution Article de Blog — Narration Publique Automatique
**Statut** : ✅ TERMINÉ (Session 109 — 19/19 PASS)
**Priorite** : P1

* [x] Formaliser le template d'article de blog (structure AsciiDoc JBake)
* [x] Implementer `BlogArticleExtractor` — extraction exclusive depuis Cercle 4 (`foundry/`)
* [x] Implementer `BlogArticleRenderer` — sortie `.adoc` dans `office/sites/cheroliv/`
* [x] Wiring avec EPIC 10 : `diluteStimuli` ajoute une cible blog
* [x] Tache Gradle `endSessionBlog`
* [x] Tests : 3 sessions synthetiques → 3 articles generes

16 JUnit5 + 3 Cucumber = 19/19 PASS.
Package `codebase.blog/` : 3 fichiers src (BlogArticleExtractor, BlogArticleRenderer, EndSessionBlogTask).

== EPIC K : Ontologie Workspace — Taxonomie unifiée
**Statut** : ✅ TERMINE (2026-05-19, K-0→K-5 toutes les 6 phases)
**Priorite** : P0

== EPIC L : Migration langgraph4j → koog+langchain4j
**Statut** : ✅ TERMINE (8/8 pts, sessions 034-049)
**Priorite** : P0
_A remplacé les anciens EPIC 11 + EPIC 13._

== EPIC V : Vibecoding — Agent Gradle Autonome
**Statut** : ✅ TERMINE (15/15 pts — sessions 041-045)
**Priorite** : P0

_US backlog_ : Migrer VibecodingTask + VibecodingGraph → planner-gradle (N2).
Codebase conserve ToolRegistry + tools + KoogAugmentedContextGraph (N1).
Retardée après consolidation EPIC V.

=== EPIC-V-SESSION-REPO — Session 059 (TERMINE, audité 2026-06-14)

3 connecteurs critiques pour boucle continue :
* [x] SessionRepository — persistance cross-session dans pgvector via R2DBC (SessionRepository.kt 295 lignes, interface + R2dbcSessionRepository)
* [x] TokenTracker — mesure réelle consommation tokens LLM (TokenTracker.kt 64 lignes, trackPrompt/trackCompletion/estimatedCost)
* [x] Dashboard — visibilité historique sessions + coût (Dashboard.kt 95 lignes + DashboardTask.kt 78 lignes + Cucumber EpicV8)

=== EPIC-V-FLYWAY — Migration Flyway (déclencheur futur)

**Statut** : BACKLOG
**Priorite** : P2
**Seuil declencheur** : 5+ fichiers de migration de schema

_Note Session 059_ : Le schema SQL est deja versionne avec la convention Flyway
(`src/main/resources/db/migration/V1__init_vibecoding.sql`) mais sans la dependance
Flyway. Le `initSchema()` de SessionRepository gere les `CREATE TABLE IF NOT EXISTS`
directement en R2DBC. Quand le nombre de migrations depassera 5, importer
`org.flywaydb:flyway-database-postgresql` et basculer sur `Flyway.migrate()`.

_Contenu de V1_ : sessions, steps, indexes, colonne confidentiality_level
(aligne sur les 4 cercles du Jardin Secret).

== EPIC W — Auto-discovery Gradle Tasks → ToolRegistry + OpenAPI Spec

**Statut** : ✅ TERMINE (7/7 sous-EPICs, session 100)
**Priorite** : P1
**Origine** : Code Review V16 (2026-05-27) — suggestion architecture cross-borough

_Pourquoi_ : Aujourd'hui, le LLM peut appeler `exec_gradle` via ToolRegistry mais ne connaît pas
le catalogue des tâches disponibles. Chaque plugin Gradle expose des tâches avec `@Input`, `@Option`,
`@TaskAction` — c'est un schéma exploitable pour du tool calling dynamique.

=== Concept

Le pipeline d'auto-découverte :
```
Plugin Gradle (ex: GenerateSPGTask)
  ↓ @Input, @Option, @TaskAction ← déjà présents dans le code
  ↓
Scanner de plugin descriptors → liste des tâches avec leurs paramètres
  ↓
Génération OpenAPI Spec (ou schema JSON) depuis le Gradle TaskGraph
  ↓
Enregistrement automatique dans ToolRegistry
  ↓
LLM découvre et appelle n'importe quelle tâche Gradle sans code supplémentaire
```

=== Sous-tâches envisagées

* [x] W-0 : Analyse — Inventaire des schémas de tâches Gradle existants (VibecodingTask, GenerateSPGTask, etc.) et mapping avec le format ToolRegistry (session 098)
* [x] W-1 : Implémenter `TaskSchemaScanner` — inspecte le task graph Gradle, extrait les métadonnées des tâches (inputs, options, description, groupe) (session 098)
* [x] W-2 : Générer un schema structuré (JSON/OpenAPI) depuis les plugin descriptors Gradle (session 099)
* [x] W-3 : Auto-registrer les tâches découvertes dans ToolRegistry au démarrage de VibecodingGraph (session 099)
* [x] W-4 : Exposer le catalogue au LLM via un tool `list_tasks` — l'agent peut requêter les tâches disponibles par groupe, description, mots-clés (session 100 — TaskListFormatter + handler + 7 Cucumber PASS)
* [x] W-5 : Mapping `--option` → arguments structurés — le ToolRegistry construit les arguments Gradle depuis la décision LLM (session 100 — GradleTaskArgsMapper + 6 JUnit5 PASS)
* [x] W-6 : Cache des schémas + invalidation à chaud quand un nouveau plugin est appliqué (session 100 — SchemaCache + 6 JUnit5 PASS)

=== Bénéfice attendu

Le LLM passe de "je peux exécuter `exec_gradle` sur une tâche que l'humain m'a dite"
à **"je découvre le catalogue, je choisis la tâche adaptée, je construis les arguments, j'exécute"**.

À terme, chaque nouveau plugin Gradle ajouté à un projet est automatiquement
découvert et exposé au LLM — zero configuration, zero code d'intégration.

C'est le chaînon manquant entre "agent qui lit du code" et "agent qui utilise le métier".

=== Référence

Discussion originale dans Code Review V16 (configuration/GLOBAL_CODE_REVIEW.adoc §23) :
"Tu pourrais générer un OpenAPI spec depuis le plugin descriptor. Le mapping est quasi direct :
`--intention="Générer SPG" → ToolRegistry.exec_gradle("generateSPG", args)`"

== EPIC X — Décision Multi-Step : Plan → Execute → Verify → Adapt

**Statut** : ✅ TERMINÉ (7/7 — sessions 101-105)
**Priorite** : P1

_Pourquoi_ : La boucle actuelle (VibecodingGraph) est linéaire : buildContext → callLLM → executeTools → persistState → checkProgress. Le LLM décide *une action à la fois*. Pas de plan multi-step avec vérification intermédiaire et adaptation.

Le game changing viendra quand l'agent pourra :
```
1. PLANIFIER  : "Je dois (a) compiler, (b) tester, (c) corriger les échecs, (d) recompiler"
2. EXÉCUTER   : Lancer `./gradlew build` via tool calling
3. VÉRIFIER   : Parser le résultat — "3 tests échouent dans FooTest"
4. ADAPTER    : "Je lis FooTest, je corrige le bug, je relance"
5. BOUCLER    : Jusqu'à vert ou abandon après N tentatives
```

C'est la différence entre un `for` (séquentiel, dépendant de la qualité du LLM à chaque step)
et un `while` (adaptatif, auto-correctif, avec plan de secours).

=== Sous-tâches envisagées

* [x] X-0 : Analyse — Audit de la boucle actuelle `VibecodingGraph.execute()` : où insérer le pattern verify→adapt ? (session 101)

* [x] X-1 : `VibecodingPlan` — Promouvoir le plan d'un `List<String>` vers une structure multi-step
      avec dépendances explicites entre les étapes (session 101 — 14 JUnit5 + 8 Cucumber PASS)

* [x] X-2 : `TaskResultVerifier` — Parse la sortie d'une tâche Gradle et détermine le verdict :
      SUCCESS / FAILED / BLOCKED / UNKNOWN (session 102 — 14 JUnit5 + 6 Cucumber PASS)

* [x] X-3 : Boucle `verify → adapt` — StepVerifier intégré dans VibecodingGraph (session 102 — 7 JUnit5 PASS)

* [x] X-4 : `RollbackStrategy` — Sur échec définitif :
      STOP_ON_ERROR / REVERT_AND_CONTINUE / MARK_SKIPPED / FALLBACK_HUMAN
      (session 105 — RollbackStrategyExecutor 8 JUnit5 + 5 Cucumber PASS)

* [x] X-5 : Intégration avec le TaskSchemaScanner (EPIC W) — Le LLM utilise le catalogue
      des tâches pour construire son plan avant d'exécuter (session 105 — 3 Cucumber PASS)

* [x] X-6 : Tests — Scénario Cucumber "agent planifie, échoue, adapte, réussit"
      avec un projet test qui a volontairement un bug à la première compilation
      (session 105 — 3 Cucumber PASS)

=== Bénéfice attendu

L'agent passe de :
```
Exécutant séquentiel : "Je fais une chose, puis je demande quoi faire"
```
à :
```
Planificateur adaptatif : "Je décide des 5 prochaines étapes, je les exécute,
je vérifie chaque résultat, je m'adapte si nécessaire, je continue jusqu'au vert"
```

C'est cette boucle qui transforme un "assistant qui propose du code"
en **"ingénieur junior qui livre une feature, teste, et corrige les bugs tout seul"**.

La différence entre un outil et un employé.

=== Référence

Discussion originale dans Code Review V16 (configuration/GLOBAL_CODE_REVIEW.adoc §23) :
"Ce qui manque pour le game changing : décision multi-step (plan → execute → verify → adapt)"

== EPIC Y — Agentic Literature Compiler : Texte Agentique → Chunks → Ontologie → SQL → Gradle Tasks

**Statut** : ✅ TERMINE (8/8 — Y-0→Y-7, session 097). 120+ PASS.
**Priorite** : P1
**Dépend de** : EPIC K (Taxonomie Workspace), EPIC W (TaskSchemaScanner)

_Pourquoi_ : Aujourd'hui, la gouvernance agentique vit dans des fichiers texte (AGENT.adoc,
INDEX.adoc, SESSIONS_HISTORY, les 10 documents du Jardin Secret, PROMPT_REPRISE, CADRAGE_METHODE...).
Le LLM les lit via le canal EAGER et *espère* les interpréter correctement.

C'est un système *textuel* : lisible par l'humain, interprété par le LLM,
mais **pas exécutable** et **pas interopérable**.

Le constat plus large : tout l'écosystème agentique produit de la littérature textuelle
(prompts systèmes, règles d'agents, instructions Copilot/Cursor, protocoles de cadrage).
Chaque système réinvente son propre format textuel. Aucun standard.

Cette brique est le **compilateur** qui transforme cette littérature textuelle
en artefacts exécutables sur le système de contexte augmenté (koog+langchain4j+RAG).

=== Pipeline

```
Source : Littérature textuelle agentique
  │
  ├─ AGENT.adoc (règles, interdictions, protocoles)
  ├─ INDEX.adoc (roadmap, statuts, backlog)
  ├─ SESSIONS_HISTORY.adoc (trajectoire)
  ├─ PROMPT_REPRISE.adoc (contexte de reprise)
  ├─ Jardin Secret (10 documents racine)
  ├─ CADRAGE_METHODE_*.adoc (ateliers)
  └─ Littérature agentique externe (prompts systèmes, règles Copilot, etc.)
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ 1. CHUNKING                                        │
│    Segmentation sémantique en unités atomiques      │
│    - Règle (interdiction, permission, obligation)   │
│    - Concept (entité, relation, attribut)           │
│    - Procédure (séquence d'actions)                │
│    - Métadonnée (statut, priorité, dépendance)      │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ 2. ONTOLOGISATION                                  │
│    Mapping sur TAXONOMIE_WORKSPACE                  │
│    - Verbe : GENERER / COLLECTER / TRANSFORMER /    │
│              DÉPLOYER / INTERDIRE / VALIDER         │
│    - Domaine : codebase / training / planner / etc. │
│    - DAG niveau : N0→N4                            │
│    - Cercle de confiance : 0→4                     │
│    - Type de chunk : rule / concept / procedure /   │
│                       metadata / constraint          │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ 3. SQL SCHÉMATISATION                              │
│    Stockage relationnel avec contraintes            │
│    Tables : agent_chunks, chunk_relations,          │
│             chunk_ontologies, chunk_enforcements,   │
│             chunk_evolution (ligne du temps)        │
│    Propriétés : parent, poids, version, validité,   │
│                 source_file, checksum               │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ 4. TRANSFORMATION → GRADLE TASKS                   │
│    Chaque chunk peut produire :                    │
│    - Règle → ToolRegistry pre/post hook            │
│    - Procédure → Plan multi-step (EPIC X)          │
│    - Concept → Metadata.kt (format pivot)          │
│    - Contrainte → @DisableCachingByDefault,        │
│                    validation automatique           │
│    - Interdiction → Git hook / CI gate             │
│    - Contexte → Prompt template enrichi            │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ 5. BOUCLE D'ENRICHISSEMENT                         │
│    L'exécution des tâches enrichit l'ontologie     │
│    - Nouveaux chunks détectés dans les sessions    │
│    - Évolution des règles (obsolète, renforcée)    │
│    - Trajectoire (tel chunk → telle tâche)         │
│    → Mise à jour semi-automatique du corpus        │
└─────────────────────────────────────────────────────┘
```

=== Sous-tâches envisagées

* [x] Y-0 : Analyse — Inventaire des sources textuelles agentiques du workspace (cadré — pipeline documenté ci-dessus)

* [x] Y-1 : `AgenticChunker` — Analyse un fichier .adoc et le segmente en chunks
      avec signature sémantique :
      ```kotlin
      data class AgenticChunk(
          val id: String,                // hash SHA-256 du contenu
          val sourceFile: String,        // AGENT.adoc:42-48
          val chunkType: ChunkType,      // RULE | CONCEPT | PROCEDURE | METADATA | CONSTRAINT
          val content: String,           // texte original
          val verb: TaxonomyVerb?,       // GENERER | COLLECTER | TRANSFORMER | DÉPLOYER | INTERDIRE | VALIDER
          val domain: String?,           // codebase | training | planner | ...
          val dagLevel: DagLevel?,       // N0 | N1 | N2 | N3 | N4
          val circle: Int?,              // 0 | 1 | 2 | 3 | 4
          val weight: Double,            // importance relative (1.0 = critique, 0.1 = indicatif)
          val checksum: String           // pour détecter les modifications
      )
      ```

* [x] Y-2 : `AgenticOntologizer` — Mappe chaque chunk sur la TAXONOMIE_WORKSPACE :
      - Extrait le verbe dominant (patterns regex : "NE DOIT JAMAIS" → INTERDIRE)
      - Identifie le domaine (patterns : "codebase-plugin", "training-plugin")
      - Détermine le DAG niveau (patterns : "N1", "N2", "DAG N3")
      - Calcule le poids (mots-clés : "CRITIQUE", "BLOQUANT", "ABSOLUE" → 1.0)
      - Résultat : chunk ontologisé, prêt à stocker

* [x] Y-3 : `AgenticSchema` — Modèle relationnel pgvector pour stocker les chunks (AgenticChunkRepository.kt 353 lignes + V3__agentic_chunks.sql). 31/31 PASS. (sessions 088-096)
      ```sql
      CREATE TABLE agentic_chunks (
          id VARCHAR(64) PRIMARY KEY,           -- SHA-256
          source_file VARCHAR(512) NOT NULL,
          source_lines VARCHAR(32),             -- "42-48"
          chunk_type VARCHAR(32) NOT NULL,       -- RULE | CONCEPT | PROCEDURE | METADATA | CONSTRAINT
          verb VARCHAR(32),                     -- GENERER | COLLECTER | ...
          domain VARCHAR(64),
          dag_level VARCHAR(4),                 -- N0 | N1 | N2 | N3 | N4
          circle INT,
          weight DOUBLE PRECISION DEFAULT 0.5,
          content TEXT NOT NULL,
          embedding vector(384),                -- pour RAG sur les règles
          checksum VARCHAR(64) NOT NULL,
          valid_from TIMESTAMP DEFAULT NOW(),
          valid_until TIMESTAMP,                -- NULL = toujours actif
          created_at TIMESTAMP DEFAULT NOW()
      );
      CREATE TABLE chunk_relations (
          source_id VARCHAR(64) REFERENCES agentic_chunks(id),
          target_id VARCHAR(64) REFERENCES agentic_chunks(id),
          relation_type VARCHAR(32),            -- ENFORCES | CONFLICTS_WITH | DEPENDS_ON | REFINES
          weight DOUBLE PRECISION DEFAULT 1.0
      );
      ```

* [x] Y-4 : `AgenticCompiler` — Transforme un chunk ontologisé en artefact exécutable (AgenticCompiler.kt 73 lignes, 4 types de compilation RULE/PROCEDURE/CONSTRAINT/CONCEPT). 27/27 PASS. (session 094)

* [x] Y-5 : `AgenticIngestor` — Pipeline complet d'ingestion (chunk→ontologize→diff detection→store→compile). AgenticIngestor.kt + IngestionReport. 12/12 PASS. (session 095)

* [x] Y-6 : `AgenticExternalImporter` — Import littérature agentique externe (Copilot, Cursor, Claude, Gemini). NormalizeContent() markdown→AsciiDoc. 12/12 PASS. (session 096)

* [x] Y-7 : AgenticChunkEnforcement — 10 unit + 7 Cucumber = 17/17 PASS
      (session 097). Pipeline complet : règle AGENT.adoc → chunk → ontologize →
      compile → enforce → ToolRegistry bloque/allow. 3 fichiers production
      (EnforcementRule.kt, AgenticChunkEnforcement.kt) + 4 fichiers test
      (AgenticChunkEnforcementTest.kt, feature, World, Steps, Runner).

=== Pourquoi c'est la brique la plus stratégique

**1. Elle résout le problème fondamental de la gouvernance agentique.**
Aujourd'hui, les règles sont des textes que le LLM *devrait* suivre.
Après EPIC Y, les règles sont des contraintes *exécutées* par le système.
La différence entre "tu devrais" et "tu ne peux pas".

**2. Elle rend le système auto-descriptif et auto-évolutif.**
Les chunks ne sont pas statiques. L'exécution des tâches enrichit l'ontologie.
Les règles obsolètes sont détectées. Les nouvelles règles sont suggérées.
Le système apprend de sa propre exécution.

**3. C'est le pont entre tous les systèmes agentiques.**
Le problème actuel : Copilot a ses règles, Cursor a les siennes,
Claude a ses system prompts, toi t'as tes AGENT.adoc.
Aucun standard, aucune interopérabilité.

Le pipeline chunk → ontologie → SQL → Gradle task est un **format pivot**.
N'importe quelle littérature agentique peut être ingérée et transformée
en tâches Gradle sur le système de contexte augmenté.
C'est le "compilateur" qui manque à l'écosystème.

**4. C'est la couche manquante entre le texte et l'exécution.**
Le pipeline actuel (EPIC K → EPIC W → EPIC X) va du texte à la tâche.
Mais le texte source (AGENT.adoc) reste du texte. Y est le compilateur
qui rend la boucle entière :
```
Texte → Chunk → Ontologie → SQL → Tâche → Exécution → Enrichissement du texte
```

=== Référence

Discussion originale session code review V16 (2026-05-27) :
"Migration des systèmes agentiques textuels vers le système de contexte augmenté.
La littérature textuelle agentique peut être chunkée/ontologisée/SQL schématisée
et transforme la préconisation agentique en tâche Gradle."

== EPIC OCR : OCR Assisté IA (Gemini Vision) via contrats Codex

**Statut** : CADRÉ — Prêt à démarrer
**Priorité** : P0
**Dépend de** : Codex N2 (contrats partagés `OcrResult`/`OcrConfig`/`OcrRequest`)
**Origine** : Session 2026-05-30 — Discussion architecture OCR cross-borough

_Pourquoi_ : Le pipeline d'acquisition documentaire (codex-gradle N2) sait extraire du texte
depuis des PDFs natifs (PDFBox) et des EPUBs (Tika). Mais les documents scannés
(factures, manuels papier, captures d'écran) nécessitent de l'OCR.

Codex définira l'interface métier (contrats). Codebase fournira l'implémentation
IA via Gemini Vision (GeminiLlmProvider déjà câblé depuis EPIC-V-5, session 064).
Chaque compte Ollama est un compte Google → pool de clés API Gemini.

Pattern DAG identique à `ContextChannel`/`ChannelBudget` (codebase-contracts) :
codex (N2) définit les types, codebase (N1) les consomme.

**Stratégie Gemini** : Les modèles Gemini (2.5-pro, 2.5-flash) sont nativement
multimodaux (image + texte → texte structuré). Un seul appel peut faire
OCR + structuration sémantique + classification en une passe.
Langchain4j supporte déjà `UserMessage.from(ImageContent, TextContent)`.

=== Pipeline cible

```
Document scanné (image/PDF non-textuel)
    ↓
OcrTask Gradle (codebase) — ./gradlew ocrDocument
    ↓
GeminiVisionProvider → GoogleAiGeminiChatModel multimodal
    ↓ image/png + prompt structuré
Gemini extrait texte + structure (titres, tableaux, blocs)
    ↓
OcrResult (contrat codex) → AsciiDoc structuré
    ↓
Chaîne existante : anonymisation → chunking → embedding → pgvector
```

=== Sous-tâches

==== OCR-0 : Contrats partagés dans codex-gradle (prérequis externe) — TERMINE (audité 2026-06-14)

* [x] Définir `OcrRequest` — input (bytes image/PDF + format + langue + prompt) (codex/ocr/OcrRequest.kt 74 lignes)
* [x] Définir `OcrResult` — output (texte structuré AsciiDoc + bounding boxes + confiance + métadonnées) (codex/ocr/OcrResult.kt 86 lignes)
* [x] Définir `OcrConfig` — paramètres (provider, model, maxTokens, temperature) (codex/ocr/OcrConfig.kt 77 lignes)
* [x] Publier les contrats dans le package `codex.ocr` du codex-plugin
* [x] publishToMavenLocal pour consommation par codebase

==== OCR-1 : GeminiKeyPool — rotation de clés API — EN COURS (4/6, audité 2026-06-14)

* [x] Pattern `GeminiKeyPool` réutilisant l'architecture `OllamaPool` (GeminiKeyPool.kt 82 lignes, ROUND_ROBIN + LEAST_USED)
* [ ] Lecture `GEMINI_API_KEY_1..N` via variables d'environnement — PARTIEL : CodebaseOcrExtension a geminiApiKeys ListProperty, mais le mapping env var→LlmInstance n'est pas dans le pool
* [x] RotationStrategy ROUND_ROBIN sur les clés (GeminiKeyPool.kt:19)
* [ ] Quota detection : HTTP 429 → rotation automatique — PARTIEL : usageCounts map, pas de detection HTTP 429 reelle
* [ ] 1 pool par compte Google (Ollama Pro = 1 compte Google = N clés) — PENDING
* [x] Tests : 4+ clés en rotation, quota exceeded → fallback (GeminiKeyPoolTest.kt 162 lignes, 29 clés, 5 modèles)

==== OCR-2 : GeminiVisionProvider — multimodal — EN COURS (4/5, audité 2026-06-14)

* [x] Créer `GeminiVisionProvider` dédié (GeminiVisionProvider.kt 95 lignes, implements VisionProvider)
* [x] Support `UserMessage.from(ImageContent(bytes, mimeType), TextContent(prompt))` (GeminiVisionProvider.kt:40-43)
* [x] Prompt engineering OCR : extraction texte + structuration AsciiDoc + détection langue (buildOcrPrompt() :76-94)
* [ ] Fallback : si Gemini indisponible → Tesseract (OCR sans IA) — PENDING
* [x] Tests : image synthétique avec texte connu → vérification output (FakeVisionProviderTest.kt 77 lignes, 5 tests)

==== OCR-2b : OllamaOcrProvider — OCR cloud via Ollama

**Statut** : ✅ TERMINE (Session 103 — 6/6 sous-tâches)
**Priorité** : P0
**Origine** : Session 099 — recherche modèles OCR cloud sur Ollama

_Pourquoi_ : `qwen3-vl:235b-cloud` est le seul modèle vision+OCR cloud sur Ollama.
235B params, 32 langues, OCR robuste documents dégradés. `ollama pull` = manifest seul,
zéro Go local, inférence cloud. Fallback cloud quand Gemini est down/saturé.

_Chaîne de fallback_ :
```
Gemini (cloud, 1er choix) → qwen3-vl:235b-cloud (cloud Ollama, fallback)
```

_Architecture_ : `OllamaOcrProvider` implémente `VisionProvider`. Appel HTTP direct à Ollama
`/api/chat` avec `images: [base64]` + prompt OCR. Cible `qwen3-vl:235b-cloud` (cloud-only).
Client HTTP : `java.net.http.HttpClient` (JDK 24 built-in) — langchain4j-ollama ne supporte pas le multimodal.

=== Sous-tâches

* [x] OCR-2b.0 : Vérifier compatibilité langchain4j-ollama avec `/api/chat` multimodal (image base64) → NON supporté, HTTP direct JDK 24
* [x] OCR-2b.1 : `OllamaOcrProvider` — implémente `VisionProvider`, appelle Ollama `/api/chat` avec image base64 + prompt OCR structuré AsciiDoc. Cible `qwen3-vl:235b-cloud`
* [x] OCR-2b.2 : Prompt engineering qwen3-vl — OCR 32 langues, extraction texte + structuration AsciiDoc
* [x] OCR-2b.3 : Intégration dans `OcrTask` — provider selection: `gemini` | `ollama` | `gemini+ollama` (fallback). Nouvelles options CLI `ollamaBaseUrl`/`ollamaModel`
* [x] OCR-2b.4 : Tests — FakeOllamaOcrProvider + OllamaOcrProvider + ThrowingVisionProvider + OcrTask integration (9 JUnit5 PASS)
* [x] OCR-2b.5 : Tests Cucumber — 3 scénarios @epic_ocr_2b (ollama provider, gemini+ollama fallback, gemini fails→ollama) — 8/8 PASS

=== Bénéfice

Pipeline OCR double cloud : Gemini → qwen3-vl:235b-cloud. Zéro Go local, zéro GPU.
Résilience : si Gemini down/quota dépassé, Ollama cloud prend le relais automatiquement.

==== OCR-3 : OcrTask Gradle — EN COURS (4/5, audité 2026-06-14)

* [x] Tâche `ocrDocument` — `./gradlew ocrDocument -PinputFile=scan.pdf` (OcrTask.kt 240 lignes, @DisableCachingByDefault)
* [x] Configuration via `OcrExtension` (provider, model, clés, langue) (CodebaseOcrExtension.kt 86 lignes)
* [ ] Support batch : `-PinputDir=scans/` → tous les fichiers du répertoire — PARTIEL : inputDir property existe mais executeOcr() ne gere que single-file
* [x] Intégration avec `GeminiConfig` existant (OcrTask.kt:116-119 resolveGeminiConfig via llm-config.yml)
* [x] Tests Cucumber : "je scanne une image → j'obtiens du texte structuré AsciiDoc" (OcrCucumberRunner.kt + OcrSteps.kt 120 lignes)

==== OCR-3b : FakeVisionProvider + CI + Config YAML/CLI/gradle.properties — EN COURS (5/8, audité 2026-06-14)

*Gaps identifiés session 086*
- Pas de test réel Gemini (pas de `GEMINI_API_KEY`)
- Pas de CI/CD OCR (`ocr.yml`)
- Pas de config YAML testée pour OCR
- Pas de test CLI `-P` params réels
- Pas de test DSL `gradle.properties`

*Objectif* : Valider la plomberie OCR avec un `FakeVisionProvider` branché sur `GeminiKeyPool`
(secrets de test, rotation, quota, fallback) — zéro appel réseau.

* [x] `FakeVisionProvider` — même signature que `GeminiVisionProvider`, retourne faux AsciiDoc structuré (FakeVisionProvider.kt 62 lignes)
* [ ] Brancher `GeminiKeyPool` + secrets de test dans `FakeVisionProvider` — PENDING : FakeVisionProvider standalone, pas de dependance pool
* [x] Tests : rotation pool, quota exceeded, fallback best-effort (GeminiKeyPoolTest.kt 162 lignes + PoolSteps.kt Cucumber)
* [x] Config YAML `llm-config.yml` testée pour la branche `gemini.ocr` (OcrTaskTest.kt:176-236)
* [x] CLI `-P` params : `./gradlew ocrDocument -PinputFile=x.png` via `ProjectBuilder` (OcrTaskTest.kt:239-274)
* [x] DSL `gradle.properties` : `codebaseOcr { model, maxTokens, language }` testé en intégration (CodebaseOcrExtensionTest.kt 126 lignes, 12 tests)
* [ ] CI/CD : workflow `.github/workflows/ocr.yml` — PENDING : aucun fichier ocr.yml trouve
* [x] Tests Cucumber : "je scanne une image → j'obtiens du texte structuré AsciiDoc" (OcrCucumberRunner.kt + OcrSteps.kt)

==== OCR-4 : Intégration pipeline complet — PENDING (0/5, audité 2026-06-14)

* [ ] OcrResult → anonymisation automatique (EPIC 2, déjà existant)
* [ ] OcrResult AsciiDoc → chunking sémantique (réutilisation codex chunkDocument)
* [ ] Chunks → embedding ONNX → pgvector (réutilisation codex codexIngest)
* [ ] Test end-to-end : image scannée → pgvector → requêtable via RAG
* [ ] Métriques : temps OCR, qualité extraction, coût tokens Gemini

=== Bénéfice attendu

Le pipeline d'acquisition documentaire passe de « texte natif uniquement »
à « tout document, scanné ou natif, devient du texte structuré requêtable ».

Le pool de clés Gemini (1 compte Google = 1 clé API) permet de
monter en charge sans coût supplémentaire — chaque abonnement Ollama
Pro existant est déjà un compte Google avec une clé API Gemini gratuite
(jusqu'à 1500 requêtes/jour sur gemini-1.5-flash).

=== Références

- `GeminiLlmProvider.kt` — déjà câblé (session 064, EPIC-V-5)
- `GeminiConfig.kt` — résolution GEMINI_API_KEY (session 063)
- `OllamaPool.kt` — pattern de rotation à réutiliser (session 065)
- `LlmProviderResolver.kt` — mapping model→provider (session 068)
- LangChain4j Gemini multimodal : `dev.langchain4j:langchain4j-google-ai-gemini:1.14.1`
  (déjà dans `libs.versions.toml` × 5 projets du workspace)

== EPIC G : Publication Maven Central + Gradle Portal
**Statut** : AUDITE (session 046) — Pret, aucune action supplementaire necessaire
**Priorite** : P0

Credentials presents dans `~/.gradle/gradle.properties`.
Namespace `education.cccp` actif sur Maven Central + Gradle Portal.
Build config : `group=education.cccp`, Sonatype staging, GPG pret.
En attendant : `publishToMavenLocal` suffit pour conso inter-boroughs N1→N2.

'''

== EPIC PUB — Publication Maven Central + Plugin Portal

_Source : configuration/EPIC_PUB.adoc (Session 096)_

=== PUB-2.2 — Mettre a jour le commentaire relocation

Dans le `build.gradle.kts`, remplacer :
```kotlin
// RELOCATION : prepare la migration du groupId education.cccp →
// <futur-domaine>. Activer avec -Prem relocationGroup="io.github.cccp-education"
// Effet : injecte <distributionManagement><relocation><groupId>...</groupId></relocation>
// dans le POM publie. Les consommateurs existants seront rediriges automatiquement
// vers le nouveau groupId lors de la prochaine maj de dependance.
```
Par :
```kotlin
// RELOCATION : garde comme echappatoire future.
// Activable avec -Prem relocationGroup="<futur-namespace>"
```

=== PUB-3.08 — codebase-plugin + codebase-contracts

*Artefacts* : `education.cccp:codebase-plugin` + `education.cccp:codebase-contracts`

⚠️ Depend de codex-plugin et planner-plugin (N2) en library.
Option A : publier codex + planner avant codebase.
Option B : `includeBuild` temporaire.

```bash
cd foundry/public/codebase-gradle && ./gradlew publish closeAndRelease
```

=== PUB-5.8 — Verification

.Verification Maven Central

| Artefact | URL |
|---|---|
| codebase-plugin | https://central.sonatype.com/artifact/education.cccp/codebase-plugin |
| codebase-contracts | https://central.sonatype.com/artifact/education.cccp/codebase-contracts |

'''

== EPIC SESSION-PROTOCOL — Protocole opencode↔Gradle : Thin Client + Moteur Gradle

**Statut** : 🔄 EN COURS (2/7 — SP-0 ✅, SP-1 ✅)
**Priorité** : P1
**Dépend de** : workspace-bom (MEMPHIS N0) pour les contrats, EPIC V (VibecodingTask moteur)

_Pourquoi_ : Aujourd'hui, opencode est un agent complet (LLM + tools + UI terminal).
La philosophie DOGFOOD FIRST dit : tout le métier doit être dans des plugins Gradle,
pas dans un CLI externe. L'objectif est d'inverser le contrôle :

```
AVANT : opencode = UI + LLM + Tools + RAG (tout-en-un)
APRÈS  : opencode = UI terminal uniquement (thin client)
         └─ prompt → tâche Gradle → moteur codebase (LLM/RAG/tools) → réponse → affichage
```

=== Architecture cible

```
┌─────────────────────────────────────────────────────────┐
│ opencode (thin client)                                  │
│ - Capture prompt utilisateur                            │
│ - Affiche réponse                                       │
│ - ZÉRO logique LLM, ZÉRO RAG, ZÉRO tools               │
└───────────┬─────────────────────────────────────────────┘
            │ SessionPrompt (contrat N0)
            ▼
┌─────────────────────────────────────────────────────────┐
│ runner-gradle (N3) ou workspace-gradle (N4)             │
│ - Pont thin-client ↔ Gradle engine                      │
│ - Gère le cycle de vie de session                       │
│ - Mode daemon/serveur pour latence interactive          │
└───────────┬─────────────────────────────────────────────┘
            │ VibecodingTask (contrat N0)
            ▼
┌─────────────────────────────────────────────────────────┐
│ codebase-gradle (N1) — Moteur                           │
│ - VibecodingTask : LLM + ToolRegistry + RAG             │
│ - SessionRepository : persistance cross-session         │
│ - CompositeContextBuilder : contexte augmenté           │
│ - EPIC X : plan→execute→verify→adapt                    │
└─────────────────────────────────────────────────────────┘
```

=== Contrats N0 nécessaires (workspace-bom, MEMPHIS)

Un 7ème artefact `opencode-session-contracts` dans workspace-bom :

[source,kotlin]
----
data class SessionPrompt(
    val sessionId: UUID,
    val prompt: String,
    val context: AgentContext?,          // contexte injecté programmatiquement
    val maxActions: Int = 10,
    val model: String? = null,           // override modèle
)

data class SessionResponse(
    val sessionId: UUID,
    val output: String,                  // réponse formatée pour affichage
    val toolCalls: List<ToolCallRecord>, // traces d'exécution
    val tokenUsage: TokenUsage,
    val status: SessionStatus,           // IN_PROGRESS | COMPLETED | ERROR
)

data class AgentContext(
    val eagerRules: String,              // règles AGENT.adoc
    val ragChunks: List<String>,         // résultats RAG pgvector
    val graphRelations: String,          // relations Graphify
    val backlogItems: List<String>,      // items BACKLOG pertinents
)
----

=== Sous-tâches envisagées

* [x] SP-0 : Cadrage — Définir le contrat `opencode-session-contracts` dans workspace-bom (MEMPHIS N0). Types SessionPrompt/SessionResponse/AgentContext/SessionStatus. 7ème artefact N0. (contrats existent dans workspace-bom/opencode-session-contracts/)
* [x] SP-1 : Implémenter `SessionProtocolTask` dans codebase — tâche Gradle qui reçoit un SessionPrompt, exécute VibecodingTask, retourne SessionResponse. Point d'entrée unique pour le thin client. (SessionProtocolTask.kt + tests + Cucumber existants)
* [x] SP-2 : Mode serveur/daemon — Tâche Gradle en écoute continue (stdin/stdout JSON-lines) pour éviter la latence de compilation à chaque tour de dialogue. Réutilisation du Gradle daemon. (SessionProtocolServer.kt 134l + SessionProtocolDaemonTask.kt 37l, 5 JUnit5 + 4 Cucumber PASS)
* [x] SP-3 : Cycle de vie de session — SessionProtocolLifecycleManager (persistance JSON disque), SessionProtocolTask +--action create|resume|close|list. Création/reprise/clôture/liste sessions. Runner-gradle (N3) consomme sans code métier. (SessionProtocolLifecycleManager.kt 126l, 15 JUnit5 + 6 Cucumber PASS)
* [x] SP-4 : Tests E2E — opencode → runner → codebase → session. Scénario bout-en-bout complet : création → vibecoding → réponse → clôture. (Session 112 — 5 JUnit5 + 4 Cucumber @epic_sp_4 = 9/9 PASS. Bugfix takeUnless→takeIf dans SessionProtocolTask.kt:116.)
* [ ] SP-5 : ToolEventStream — Événements structurés temps réel (thinking/tool_call/tool_result/progress/error) en JSON-lines sur stdout.

=== Bénéfice attendu

opencode passe de "agent monolithique externe" à "interface utilisateur terminal".
Toute la logique métier (LLM, RAG, tools, planification, vérification) est dans
des plugins Gradle versionnés, testés, publiables sur Maven Central.

C'est la dernière brique pour le DOGFOOD FIRST complet :
l'humain dialogue avec un terminal, le terminal dialogue avec Gradle,
Gradle dialogue avec les LLMs. Zéro SaaS, zéro CLI externe propriétaire.

=== Référence

Brainstorming Session 103 (2026-06-14) — Discussion architecture thin-client opencode↔Gradle.
Contrats N0 dans workspace-bom (MEMPHIS), moteur dans codebase (N1), pont dans runner (N3).

== EPIC Z — Autofocus : Accommodation Contextuelle Programmatique

**Statut** : ✅ TERMINÉ (7/7 — Z-0 ✅ Z-1 ✅ Z-2 ✅ Z-3 ✅ Z-4 ✅ Z-5 ✅ Z-6 ✅ Z-7 ✅, session 108)
**Priorité** : P1
**Dépend de** : V-6 Feedback Loop (prérequis), graphify-gradle N0 (graph.json)
**Source** : AUTOFOCUS_CAPABILITY.adoc (document 11 du Jardin Secret, session 073)

_Pourquoi_ : Le gap identifié session 073 — l'IA associe (RAG+Graphify) mais ne zoome pas.
Sans autofocus, le LLM traite tout au même niveau → noyade dans le bruit → décisions
stratégiquement aveugles. L'autofocus est le second pilier de la cognition (accommodation),
après l'association (déjà couverte par le vecteur composite BOOM_BAP).

=== Pipeline

```
Intention utilisateur
    ↓
AutofocusClassifier.classify(intention, buildOutput) → AutofocusLevel
    ↓
ContextZoomer.zoom(target, augmentedContext) → contexte filtré
    ↓
VibecodingGraph exécute avec contexte ciblé
    ↓
verifyResult FAILED → AutofocusClassifier.classify(error) → zoom(IMPLEMENTATION) → retry
    ↓
checkProgress → AutofocusStack.pop() → remonter niveau
```

=== Sous-tâches

* [x] Z-0 : Cadrage — Document CADRAGE_AUTOFOCUS.adoc (7 ateliers agile). Contrat d'interface, intégration roadmap, 23 pts EPIC Z. (session 106)
* [x] Z-1 : `AutofocusLevel` — Enum 4 niveaux (BIG_PICTURE/ARCHITECTURE/MODULE/IMPLEMENTATION) + LevelMetadata + token budgets + file patterns. 16/16 JUnit5 PASS. (session 106)
* [x] Z-2 : `AutofocusClassifier` — Classifieur heuristique regex (0 token LLM, ~80% cas) + fallback LLM (~5 tokens). 32/32 JUnit5 PASS. (session 106)
* [x] Z-3 : `ContextZoomer` — Filtrage contexte par niveau, budget token par section. 14/14 JUnit5 PASS. (session 106)
* [x] Z-4 : `AutofocusStack` — Pile LIFO push/pop/currentLevel/isEmpty/clear. 14/14 JUnit5 PASS. (session 106)
* [x] Z-5 : Intégration VibecodingGraph — Node 0 (classify) + Node 1 (zoom) dans le graphe koog. Tests E2E. (session 107 — 16 JUnit5 PASS)
* [x] Z-6 : Intégration V-6 Feedback Loop — verifyResult FAILED → zoom(IMPLEMENTATION) → retry. Tests Cucumber. (session 107 — 4 Cucumber PASS)
* [x] Z-7 : Validation cross-borough — Scénario bout-en-bout : intention → autofocus → exécution → succès. 4 Cucumber @epic_z_7 PASS. popFocusNode wiré dans checkProgressNode (success path) + retry success path. (session 108)

=== Bénéfice attendu

Le LLM passe de "je lis tout le contexte et j'espère ne pas me noyer"
à "je zoome sur ce qui compte, j'exécute, je dézoome pour décider, je rezoome".

C'est la différence entre un script qui associe et un ingénieur qui comprend.
5ème verbe ZOOMER dans la taxonomie workspace.

=== Référence

- AUTOFOCUS_CAPABILITY.adoc (racine workspace) — spécification complète
- CADRAGE_AUTOFOCUS.adoc (.agents/) — cadrage agile 7 ateliers
- Session 073 (2026-05-27) — découverte du gap autofocus
