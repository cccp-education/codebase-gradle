= PROMPT_REPRISE — Codebase (Queens N1) — Session 136
:session: 136
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 135 — EPIC PUB ✅ TERMINÉ, reprise EPIC V-6

== Contexte Hérité

- Session 135 a vérifié `codebase-plugin:0.0.3` HTTP 200 sur Maven Central,
  clôturé EPIC PUB, validé la config nmcp, et analysé EPIC V-6 pour le
  prochain baby-step.
- Article 0120 = pattern canonique dual-abonnement Ollama Pro via Docker
  (isolation port + volume SSH).

== Mission Session 136

1. Implémenter `OllamaInstanceScanner.kt` : scanner actif des ports
   `11437→11465` via `GET /api/tags`, retourner `List<LlmInstance>`
   pour les instances vivantes.
2. Tests JUnit5 baby-step :
   - scanner avec fake HTTP client (ports up/down mixtes)
   - fallback env var `OLLAMA_POOL_PORTS` prioritaire sur le scan
   - test d'intégration conditionnel (`isOllamaReady`) optionnel
3. Câbler `LlmProviderResolver` : utiliser le scanner quand
   `OLLAMA_POOL_PORTS` n'est pas défini.
4. `./gradlew build -x test -x cucumberTest -x koverVerify -x validateDependencies`
   propre.

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation entre
chaque étape. Pas de subagent, pas de parallélisation.
