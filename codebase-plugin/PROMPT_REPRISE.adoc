= Prompt de Reprise — Codebase (Queens N1) — Session 190
:session: 190
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 189 — VIBE-HARDENING-2 US-1 (S2 Allowlist TaskDiscoveryRegistrar, 18 unit + 6 réécrits) + US-2 (S3 MAX_WRITE_CHARS write_file/edit_file, +5 unit). 2/8 US. Borough DORMANT.

== Mission Session 190

EPIC VIBE-HARDENING-2 (P2/P3 hardening) — 6 US restants sur 8.
Borough DORMANT — réveil sur déclencheur pilote.

=== US-3 C4 GradleTaskArgsMapper flags cassés (P2, 3 pts)

`GradleTaskArgsMapper.buildCommand` produit `--key value` (2 tokens)
mais `ExecGradleTool.validateGradleTask` (US-2 S-186) valide chaque
token vs `GRADLE_ARG_ALLOWLIST` (`--\\w+`) — `value` ne matche pas →
**rejeté deny-by-default**. Flags avec valeur cassés.

* Fail préexistant confirmé : `GradleTaskArgsMapperTest > execute
  structured gradle task via handler()` FAILED (S-189 gate).
* Stratégie : étendre `GRADLE_ARG_ALLOWLIST` à `--\\w+(?:=\\S+)?` (flag
  seul ou flag=value) ET accepter `--\\w+ \\S+` (pair flag+value 2
  tokens). ~6 unit.
* Gate ciblé : `./gradlew test --tests "codebase.koog.discovery.*"`.
* Document : `.agents/CADRAGE_EPIC_VIBE_HARDENING_2.adoc` US-3.

=== Backlog restant (sur déclencheur pilote)

US-4 C1 TaskResultVerifier refonte (4 pts) → US-5 C3 cleanup doc (1 pt)
→ US-6 C7 coerceAtLeast (1 pt) → US-7 C6 SessionProtocolEngine (4 pts)
→ US-8 Cucumber BDD (2 pts).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en attente — alternative
  possible sur déclencheur pilote.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.