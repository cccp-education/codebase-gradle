= PROMPT_REPRISE — Codebase (Queens N1) — Session 136
:session: 136
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 135 — EPIC PUB N0 migration + codebase-plugin 0.0.3 publié Central ✅

== Contexte Hérité

- Session 135 a migré les duplications N0 locales vers les JARs Central
  (`i18n-contracts:0.0.1`, `opencode-session-contracts:0.0.1`),
  bumpé la version `0.0.3`, et exécuté `publishAggregationToCentralPortal`
  (deployment `adc37d1c-...` en validation/publishing sur Central).
- La propagation Central est en cours ; la méthode de publication reste à
  aligner sur la méthode préférée de l'utilisateur (déjà validée ailleurs).

== Mission Session 136

1. Vérifier `codebase-plugin:0.0.3` HTTP 200 sur Maven Central
   (`https://repo1.maven.org/maven2/education/cccp/codebase-plugin/0.0.3/`).
2. Si 200 confirmé : passer EPIC PUB à ✅ TERMINÉ.
3. Adapter la configuration de publication selon la méthode éprouvée
   de l'utilisateur (remplacer nmcp si nécessaire).
4. Corriger la session checklist racine vs `codebase-plugin/.agents/`
   (split-brain gouvernance détecté).
5. `./gradlew build -x test` propre.

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation entre
chaque étape. Pas de subagent, pas de parallélisation.
