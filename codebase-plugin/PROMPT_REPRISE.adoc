= PROMPT_REPRISE — Codebase (Queens N1) — Session 137
:session: 137
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 136 — EPIC V-6 scanner actif ✅, reprise V-6 cablage OllamaPoolKeyAdapter

== Contexte Hérité

- Session 136 a livré `OllamaInstanceScanner` + ports DDD + câblage dans
  `LlmProviderResolver`. 22 tests ciblés PASS, build propre.
- EPIC V-6 reste partiel : `OllamaPoolKeyAdapter` (wrapper resilient
  quota/quota dépassé + rotation instances) et pattern SSH key article 0120
  ne sont pas encore implémentés.
- Article 0120 = pattern canonique dual-abonnement Ollama Pro via Docker
  (isolation port + volume SSH).

== Mission Session 137

1. Implémenter `OllamaPoolKeyAdapter` : wrapper autour du pool qui gère
   la rotation proactive quand une instance renvoie une erreur de quota
   ou de connexion, sans propager l'exception au premier échec.
2. Tests JUnit5 baby-step :
   - quota exceeded déclenche rotation vers instance suivante
   - connexion refusée déclenche rotation et marque l'instance inactive
   - épuisement du pool lève `IllegalStateException` explicite
3. Câbler `OllamaPoolKeyAdapter` dans `OllamaLlmProvider.call()` pour
   remplacer le retry naïf actuel.
4. `./gradlew :codebase-plugin:compileKotlin` puis tests ciblés
   `OllamaPoolKeyAdapterTest` + `OllamaLlmProviderTest` verts.

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation entre
chaque étape. Pas de subagent, pas de parallélisation.
