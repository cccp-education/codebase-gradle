= PROMPT_REPRISE — Codebase (Queens N1) — Session 140
:session: 140
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 139 — EPIC V-6 Resolver→Factory déterministe — 9 JUnit5 + 3 Cucumber PASS ✅

== Contexte Hérité

- Session 139 a clôturé EPIC V-6 : `LlmProviderResolver` utilise `OllamaInstanceFactory` par défaut (mode déterministe, 29 instances, zéro réseau) ; `OllamaInstanceScanner` reste disponible comme fallback explicite via `OLLAMA_SCAN_PORTS=true` ou `OLLAMA_POOL_PORTS`.
- Les 9 tests JUnit5 `LlmProviderResolverTest` et les 3 scénarios Cucumber `@epic_v6_resolver` passent. Build propre.
- EPIC 8 (Expert Exposure via Ollama) reste partiellement ouvert : session 127 a livré contrats, tâche Gradle `exposeExperts` et 5 scénarios Cucumber, mais l'INDEX indique "EN COURS (3/4)" — le dernier item (consommation/exposition réelle par un borough N2 ou wiring final) reste à boucler.

== Mission Session 140

1. Identifier l'item ouvert 4/4 de l'EPIC 8 Expert Exposure (consommation du manifeste JSON par un borough N2, wiring final, ou test cross-borough).
2. Implémenter le dernier baby step en TDD : un fichier modifié à la fois, compilation entre chaque étape.
3. Ajouter/ajuster les tests JUnit5 fonctionnels et les scénarios Cucumber `@epic8` pour couvrir le point manquant.
4. S'assurer que `./gradlew :codebase-plugin:compileKotlin` et les tests ciblés EPIC 8 restent verts.
5. Si EPIC 8 est véritablement terminé, mettre à jour `BACKLOG.adoc` et `.agents/INDEX.adoc` (statut ✅ TERMINE).

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.
