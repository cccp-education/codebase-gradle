= Prompt de Reprise — Codebase (Queens N1) — Session 187
:session: 187
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 186 — EPIC VIBE-HARDENING US-2 ExecGradleTool allowlist. 4 regex `GRADLE_BLACKLIST` remplacées par `GRADLE_TASK_ALLOWLIST` (2 patterns bare tasks + project:task) + `GRADLE_ARG_ALLOWLIST` (2 patterns -Pprop + --flag) + `GRADLE_DENYLIST` (8 patterns second ride). `validateGradleTask` split tokens + allowlist par position. 15/15 ExecGradleToolTest PASSED + gate `codebase.koog.tools.*` VERT + ToolRegistryTest 8/8 non-régression. Zéro bump BOM.

== Mission Session 187

EPIC VIBE-HARDENING continue. US-3 ou US-4 (indépendants, au choix) :

* US-3 (C5 Timeout LLM, ~3 pts) — wrapper `withTimeout(llmTimeoutMs)`
  sur 3 sites `runBlocking { llmProvider.call(...) }` (callLLMNode L413,
  replan L288, StepVerifier L44). Constructor param `llmTimeoutMs: Long
  = 30_000L`. Catch `TimeoutCancellationException` →
  `withError("LLMTimeout: ${ms}ms")`. ~8 unit.

* US-4 (C2 Retry un seul compteur, ~2 pts) — supprimer
  `retryCount = state.retryCount + 1` dans `executePlanTasks` (L587, L604)
  + `incrementRetry()` dans bloc error (L291, L298, L302). Utiliser
  `withRecoverableError` (déjà correct). MàJ `VibecodingGraphTest` +
  `StepVerifierTest`. ~6 unit.

US-5 (Cucumber BDD, ~2 pts) dépend de US-1→US-4 — à planifier en dernier.

== État actuel

- Version codebase-plugin : `0.0.8` (Maven Central, S-183)
- EPIC VIBE-HARDENING : 2/6 US terminés (US-0 ✅, US-1 ✅, US-2 ✅ S-186)
- Reste : US-3 (timeout), US-4 (retry), US-5 (cucumber) — ~7 pts
- Borough DORMANT (réveillé sur déclencheur pilote)

== Rappels

- GroupId : `education.cccp`
- Baby-step TDD strict : RED → GREEN → REFACTOR, 1 fichier à la fois
- Gate ciblé en fin d'US : `./gradlew test --tests "codebase.koog.*"`
  (pas de `./gradlew test` global — Cucumber interminable)
- `kotlinx-coroutines-core` déjà dans le classpath (`withTimeout` available)
- `VibecodingState.withRecoverableError` incrémente déjà `retryCount`
  (L46-47) — source de vérité unique pour US-4
- English only : code, Gherkin, comments
- Pas de bump BOM (EPIC local à codebase-plugin)
- `cd codebase-plugin && ./gradlew ...` (build autonome)

== Références

* `CADRAGE_EPIC_VIBE_HARDENING.adoc` — 10 décisions architecturales
* `ExecShellTool.kt` / `ExecGradleTool.kt` — patterns allowlist (US-1/US-2)
* `VibecodingGraph.kt` L277-313, L413, L580-607 (sites US-3/US-4)
* `VibecodingState.kt` L46-53 (`withRecoverableError`)
* `StepVerifier.kt` L44 (site US-3)