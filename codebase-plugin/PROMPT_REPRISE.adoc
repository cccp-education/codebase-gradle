= Prompt de Reprise — Codebase (Queens N1) — Session 215
:session: 215
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 214 — CB-FPA-RAG US-1/2/3 TERMINÉ (ingestion corpus FPA 387 chunks + 387 embeddings pgvector, 18 doubtful, proof of work query métier OK, BDD 4/4) + bug updateEmbedding corrigé (double bracket, P0) + propagation 0.0.14 Central (bundle `3e82490d`) + BOM 0.0.37 (bundle `43c8a1ca`), compile 7/7 VERTS
:session_pre_precedente: 213 — CB-FPA-RAG US-0 TERMINÉ (audit : format chunks codex COMPATIBLE) + OCR-QUALITY US-4 TERMINÉ (DoubtMetadata/DoubtExposure/ingestWithDoubt/searchWithDoubt, 31 unit + 9 Cucumber, zéro re-vectorisation)

== Mission Session 215 — Critical path (suite ordre acté)

[IMPORTANT]
Arbitrage pilote 2026-09-07 : priorité critical path MVP0.
CB-FPA-RAG est TERMINÉ 4/4 US (S-213 + S-214) — GBL-003.4 débloqué,
le corpus FPA (387 chunks) est requêtable via `codebase.store`.

*1. Fix Z-6 Cucumber (2 échecs préexistants, ~2 pts)* — « Autofocus
stack size should be at least 2 but was 1 » (régression S-166,
`popFocusNode` après replan). Test unitaire ne couvre que non-empty.
Baby-step TDD strict. 2 scénarios `epic_z_6_autofocus_feedback`
rouges — orthogonal à tout le reste, gate `check` bloquée.

*2. RENAME Vague suivante (ordre acté S-215)* — document → bakery →
capsule → plantuml → graphify/codex (codebase ✅ S-212). Gouvernance
pure cross-borough — voir BACKLOG racine RENAME.

*3. Support Newark A2 (sur demande pilote)* — le RAG FPA est prêt
pour la génération des plans de formation (canal Docs doubt-aware). Dogfooding training
TRN-1→2 si le pilote arbitre.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version locale : 0.0.14 (Central : 0.0.14, bundle `3e82490d`,
  repo1 200). Conventions 0.0.4. BOM 0.0.37 (Central, contrainte
  codebase 0.0.14). 7 consommateurs propagés, compile 7/7 VERTS.
* koverVerify : minBound 63 (baseline 68.03% à HEAD S-212). Gate
  NON validée S-214 (dépend cucumberTest global, bloqué Z-6).
* 44 cucumber runners dédiés dans `build.gradle.kts` (dernier :
  `cucumberTestCorpusIngest` S-214).
* Run ingestion FPA : consommateur transitoire
  `/tmp/opencode/fpa-rag-build` + init-script
  `/tmp/opencode/fpa-rag-init.gradle.kts` (tâches `corpusIngest` +
  `corpusQuery`, pattern Règle 2 — workspace wipe). Base pgvector :
  docker `angry_curran` port 32772, db `codex` (387 chunks, 387
  embeddings, 18 doubtful). Corpus : `office/metiers/FPA/.../codex-out/chunks.json`
  (PROPRIETARY, zéro donnée métier dans le repo).
* `StoreStatements.updateEmbedding` strip les brackets — contract
  documenté KDoc + test `updateEmbedding strips brackets` (S-214).
* Dossier gouvernance unique : `codebase-plugin/.agents/` (S-207).

== Échecs préexistants à documenter (orthogonaux, ne pas confondre)

* 2 × Z-6 Cucumber `epic_z_6_autofocus_feedback` ("Autofocus stack
  size should be at least 2 but was 1") — régression S-166
  (`popFocusNode` après replan). Confirmés présents S-214 (bloquent
  `cucumberTest` global → `koverVerify`/`check` rouge).
* 2 × `OcrTaskTest` (`detectMimeType octet-stream for pdf`,
  `isImageFile false for pdf`) — drift S-211 (`4601cb0` a ajouté pdf
  aux IMAGE_EXTENSIONS sans maj tests).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.
* Ne JAMAIS lancer `./gradlew test` global en fin de session sans permission.

== References

* `.agents/sessions/214-cb-fpa-rag-us123-corpus-ingest.adoc` (rapport S-214)
* `.agents/CADRAGE_EPIC_CB_FPA_RAG.adoc` (cadrage S-215 — EPIC terminé)
* `.agents/CADRAGE_EPIC_OCR_QUALITY.adoc` (US-4 ✅ S-213)