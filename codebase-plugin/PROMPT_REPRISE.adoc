= PROMPT_REPRISE — Codebase (Queens N1) — Session 143
:session: 143
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 142 — EPIC V-9.3 ScanAgent livré (8 JUnit5 + 6 Cucumber @epic_v_9_3 PASS, IngestGovernanceTask refactoré)

== Contexte Hérité

- Session 142 a livré V-9.3 ScanAgent : `ScanAgent.kt` (scanner récursif `.agents/`, ignore `build`/`.git`/`.gradle`/`node_modules`) + `ScannedFile.kt` (value object DDD) + 8 JUnit5 + 6 scénarios Cucumber `@epic_v_9_3` PASS. `IngestGovernanceTask.collectGovernanceFiles` refactoré (liste hardcoded 7 paths supprimée, délègue à `ScanAgent`). 5 tests `IngestGovernanceTaskTest` préservés (zéro régression).
- Under-EPIC 1 (V-9.1 AgenticChunker + V-9.2 ChunkType + V-9.3 ScanAgent + V-9.4 ChunkExtractor + V-9.5 ChunkValidator) est **COMPLET** (5/5).
- EPIC V-FLYWAY reste en BACKLOG (seuil déclencheur : 5+ migrations de schéma).

== Mission Session 143

1. Attaquer Under-EPIC 2 V-9.6 AgenticOntologizer — vérifier anti-duplication avec EPIC Y-2 (AgenticOntologizer.kt déjà livré, 20/20 PASS session 089).
2. Si V-9.6 déjà couvert par EPIC Y-2, marquer "déjà livré" et passer à V-9.7.
3. Identifier le premier item réellement nouveau de Under-EPIC 2/3 et le coder en baby-step DDD/TDD/BDD.
4. Un fichier modifié à la fois, compilation entre chaque étape.
5. `./gradlew :codebase-plugin:compileKotlin` doit rester vert.

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.