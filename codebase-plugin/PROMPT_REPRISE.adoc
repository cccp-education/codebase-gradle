= Prompt de Reprise — Codebase (Queens N1) — Session 159
:session: 159
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 158 — EPIC V-6-POOL terminé (4/4 items). AUTHORIZED_MODELS aligné sur AGENTS.adoc (2 modèles gpt-oss:120b-cloud + gemma4:31b-cloud) dans OllamaInstanceFactory + OllamaInstanceScanner. LlmProviderResolver cycle les 2 modèles sur OLLAMA_POOL_PORTS au lieu de DEFAULT_MODEL statique. Feature Cucumber epic_v6_pool.feature (3 scénarios) + runner + step. Tous tests unitaires + Cucumber PASS, zéro régression.

== Mission Session 159

EPIC V-6-POOL étant clôturé, attaquer l'EPIC OCR-1/OCR-2 restant
(P2, items PENDING) en baby-step DDD/TDD/BDD.

Source : `codebase-plugin/.agents/BACKLOG.adoc` lignes 591-607.

=== Items OCR-1 restants

* OCR-1 : Lecture `GEMINI_API_KEY_1..N` via variables d'environnement —
  mapping env var→LlmInstance pas dans le pool (PARTIEL)
* OCR-1 : Quota detection HTTP 429 → rotation automatique — PARTIEL
  (usageCounts map, pas de detection HTTP 429 réelle)
* OCR-1 : 1 pool par compte Google (Ollama Pro = 1 compte Google = N clés) — PENDING

=== Items OCR-2 restants

* OCR-2 : Fallback Tesseract (OCR sans IA) si Gemini indisponible — PENDING

=== Contexte technique

* `GeminiKeyPool.kt` (82 lignes, ROUND_ROBIN + LEAST_USED) déjà livré.
* `GeminiVisionProvider.kt` (95 lignes) déjà livré.
* `FakeVisionProvider.kt` (62 lignes) branché sur GeminiKeyPool.
* `OllamaOcrProvider.kt` fallback cloud déjà livré (OCR-2b terminé).
* Tests : GeminiKeyPoolTest 162 lignes (29 clés, 5 modèles).

=== Ordre d'attaque (baby-step)

. OCR-1 quota detection : Ajouter détection HTTP 429 dans GeminiKeyPool
   ou GeminiVisionProvider. Tests unitaires (zéro réseau, FakeProvider).
. OCR-1 env var mapping : Câbler `GEMINI_API_KEY_1..N` → LlmInstance
   dans le pool. Tests unitaires.
. OCR-1 multi-compte : 1 pool par compte Google. Tests unitaires.
. OCR-2 Tesseract fallback : Si Gemini + Ollama indisponibles → Tesseract.
   Tests unitaires + Cucumber.

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié
à la fois, compilation entre chaque étape. Pas de subagent, pas de
parallélisation.

== Contrainte Ollama (rappel AGENTS.adoc)

Ports 11434-11436 INTERDITS. Ports 11437-11465 autorisés (rotation).
Modèles autorisés : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
Aucun autre modèle ni port.