= Prompt de Reprise — Codebase (Queens N1) — Session 196
:session: 196
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 195 — EPIC FT-PIPELINE US-3 FineTuningGraph (koog itératif). 5 fichiers prod créés (FineTuningStage, FineTuningState, FineTuningPromptBuilder, FineTuningLlm, FineTuningGraph) + 2 test (FineTuningStateTest 18 unit, FineTuningGraphTest 17 unit). 35 nouveaux tests, 0 FAIL. Gate `codebase.finetuning.*` 78/78 VERT. Domaine `codebase.finetuning` (7 fichiers prod + 5 test). Zéro bump BOM, zéro secret, zéro régression.

== Mission Session 196

EPIC FT-PIPELINE 3/7 US terminé. Borough DORMANT — réveil sur
déclencheur pilote uniquement.

=== Option A — EPIC FT-PIPELINE US-4 (P0 vision, tâches Gradle + extension)

US-4 (4 pts, ~10 unit + 2 functional) : tâches Gradle
`prepareFineTuningDataset` (groupe `finetuning`) + `fineTuneExpert`
(groupe `finetuning`, dependsOn prepare) + `publishExpertToOllama`
(groupe `finetuning`, dependsOn fineTune, register dans `ExpertRegistry`
+ manifest JSON via `ExpertExposureTask`). Extension
`fineTuning { baseModel; dataset; outputModelName; corpusRatio;
maxIterations }` 4 sources DSL (pattern `CapsuleConfig`). Document :
`.agents/CADRAGE_EPIC_FT_PIPELINE.adoc` US-4.

=== Option B — Maintenance / déclencheur pilote

Aucun EPIC actif. Borough DORMANT jusqu'à déclencheur pilote.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en cours — 3/7 US livrés.
* Gate ciblé : `./gradlew test --tests "codebase.finetuning.*"`
  (pas de `./gradlew test` global — Cucumber interminable).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.