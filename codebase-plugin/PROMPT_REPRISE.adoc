= Prompt de Reprise — Codebase (Queens N1) — Session 204
:session: 204
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 203 — Fix `BenchmarkRunner.kt` vers socle `codebase.store.RagVectorStore` (pattern codex `searchBlocking`), gate worktree isolé VERT. Session écourtée : session opencode codex (Brooklyn) éditait codebase en parallèle (migration legacy `VectorStore` → `RagVectorStore` en cours).

== Mission Session 204

[IMPORTANT]
Pré-requis ABSOLU : vérifier que la session codex a terminé et commité sa
migration sur codebase. Baseline doit être VERTE (`./gradlew compileKotlin`)
avant de coder. Si la migration Brooklyn est commitée, vérifier que mon fix
`BenchmarkRunner.kt` n'a pas été absorbé/conflitté.

=== Option A — PUB-0.9 : republier codebase-plugin (prioritaire)

Constat S-203 (repo1 vérifié en direct) :

* POM Central `codebase-plugin:0.0.8` contient encore `codex-plugin:0.0.2`
  runtime → l'inversion N1→N2 est morte côté code mais PAS côté Central.
* Baby-steps :
  . Bump version 0.0.8 → 0.0.9
  . TDD : test unitaire caractérisant le POM publié (contient
    `ocr-contracts:0.0.1`, NE contient plus `codex-plugin`)
  . `publishToMavenLocal` vert + validation POM ~/.m2
  . Publication Central (`publishAggregationToCentralPortal`) — accord pilote

=== Option B — Signal cross-borough à relayer

* `codex-plugin:0.0.6` ABSENT de Maven Central (dernier = 0.0.5) malgré
  la claim Brooklyn S-104 — à signaler au pilote pour Brooklyn.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* codebase-plugin 0.0.8 = Maven Central. Dep N0 `ocr-contracts:0.0.1`.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* 41 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).
* Session concurrente détectée S-203 : toujours vérifier `git status`,
  mtimes récents, et l'absence d'autre session active AVANT d'éditer.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.
