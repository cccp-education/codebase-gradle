= PROMPT_REPRISE — Codebase (Queens N1) — Session 100
:session: 100
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:sessions_precedente: 099 — W-2+W-3 OpenAPI Spec + Auto-register ToolRegistry (21/21 PASS, audit BACKLOG)

== Contexte Hérité

- EPIC W 4/7 terminé : W-0 ✅, W-1 ✅, W-2 ✅, W-3 ✅
- OpenApiSpecGenerator (85 lignes, 8 tests) + TaskDiscoveryRegistrar (30 lignes, 7 tests)
- Auto-register câblé dans VibecodingTask.executeVibecoding()
- BACKLOG audité : 17 `[ ]` → `[x]`, 53 unchecked restants
- OCR-2b cadré : OllamaOcrProvider cloud (qwen3-vl:235b-cloud, pull testé port 11439)
- ToolRegistry enrichi dynamiquement avec gradle_* tools

== Mission Session 100 — EPIC W-4 + W-5 + W-6

*W-4* : Exposer le catalogue au LLM via un tool `list_tasks`.
  L'agent peut requêter les tâches disponibles par groupe, description, mots-clés.

*W-5* : Mapping `--option` → arguments structurés.
  Le ToolRegistry construit les arguments Gradle depuis la décision LLM.

*W-6* : Cache des schémas + invalidation à chaud quand un nouveau plugin est appliqué.

→ EPIC W TERMINÉ (7/7).

== Ordonnancement Sessions Suivantes

| Session | EPIC | Livrable
| 100 | W-4 + W-5 + W-6 | list_tasks tool + mapping args + cache → EPIC W TERMINÉ
| 101 | X-0 + X-1 | Cadrage + VibecodingPlan structuré (dépend de W)
| 102 | X-2 + X-3 | TaskResultVerifier + boucle verify→adapt
| 103 | X-4 + X-5 + X-6 | RollbackStrategy + intégration W + Cucumber → EPIC X TERMINÉ
| 104 | Z-0 + Z-1 | Autofocus cadrage + ZoomLevel
| 105 | Z-2 + Z-3 | ContextZoomer + ZoomPolicy
| 106 | Z-4 + Z-5 + Z-6 | Intégration VibecodingGraph → EPIC Z TERMINÉ
| 107 | 12 | Dilution Article de Blog (quick win)
| 108+ | 5 | Pipeline fine-tuning experts CDA/FPA (lourd)
