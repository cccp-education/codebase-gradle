= Prompt de Reprise — Codebase (Queens N1) — Session 188
:session: 188
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 187 — EPIC VIBE-HARDENING TERMINÉ 6/6 US (US-3 timeout LLM withTimeout 30s 3 sites, US-4 retry single counter, US-5 Cucumber BDD 4 scénarios @vibe-hardening). Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier VERT 64 tests 0 FAIL + 4/4 cucumber PASSED. Zéro bump BOM, zéro secret. Borough DORMANT.

== Mission Session 188

Session de maintenance + cross-borough + hardening supplémentaire
(pilote ordonné : 1 maintenance → 4 cross-borough → 3 code review).
Borough DORMANT réveillé sur déclencheur pilote.

=== Phase 1 — Maintenance (gouvernance pure)

Synchroniser la gouvernance interne codebase-plugin avec l'état réel
post-S-187 :

* INDEX.adoc : `session_en_cours` 186 → 188, EPIC VIBE-HARDENING
  `🟡 EN COURS (2/6)` → `✅ TERMINÉ 6/6`, sessions 186/187 ajoutées.
* BACKLOG.adoc : section EPIC VIBE-HARDENING ajoutée, `last_update` S-188.
* SESSIONS_HISTORY.adoc : entrées S-186/S-187 ajoutées (S-119→S-126 déjà
  rattrapées).
* PROMPT_REPRISE.adoc : mission N+1 (ce document).
* TEST_COVERAGE_ANALYSIS.adoc : sessions S-173→S-187 si tests modifiés.

Aucun code modifié — gouvernance pure pattern S-057/S-063.

=== Phase 2 — Cross-borough (support consommateurs)

Identifier les boroughs consommateurs de codebase-plugin et vérifier leur
alignement de version. Consommateurs connus : planner (Manhattan),
slider (MIAMI), capsule (Détroit), training (Newark), plantuml (HTOWN).
Vérifier :

* Version codebase-plugin consommée dans chaque `libs.versions.toml`.
* Delta éventuel entre version consommée et 0.0.8 (dernière Central).
* Backlog EPIC PUB dans chaque borough consommateur (bump requis ?).

Ne pas publier — audit uniquement. Décider si un EPIC PUB bump
codebase-plugin 0.0.8 → 0.0.9 est requis (si modifications post-0.0.8
non publiées — hors scope si working tree propre et 0.0.8 = Central).

=== Phase 3 — Code review / hardening supplémentaire

Audit des P2/P3 restants de CR-GLOBALE-004 reportés vers EPIC
VIBE-HARDENING-2 :

* S2 — pas d'allowlist Gradle globale (au-delà ExecGradleTool).
* S3 — edit/write sans garde taille (limite taille fichier écrit).
* C1 — heuristique `contains("failed")` dans TaskResultVerifier.
* C3 — VibecodingPlan dead code.
* C4 — flags Gradle cassés.
* C6 — duplication.
* C7 — compteur négatif.

Décider : cadrer EPIC VIBE-HARDENING-2 (gouvernance pure) ou laisser
dormant. Pas d'exécution code sans permission pilote.

== État actuel

- Version codebase-plugin : `0.0.8` (Maven Central, S-183, alignée code)
- workspace-bom : `0.0.19` (Maven Central, aligné)
- agent-contracts : `0.0.3` (Maven Central, aligné — alias `0.0.2` dans
  BOM `libs.versions.toml:72` est un mort non référencé, stale cosmétique)
- EPIC VIBE-HARDENING : ✅ TERMINÉ 6/6 US (S-184→S-187)
- EPIC SUBGRAPH : ✅ TERMINÉ 4/4 US (S-178→S-182)
- EPIC PUB 0.0.8 : ✅ TERMINÉ 3/3 US (S-183)
- Borough DORMANT — tous EPICs terminés

== Rappels

- GroupId : `education.cccp`
- Baby-step TDD strict : RED → GREEN → REFACTOR, 1 fichier à la fois
- Gate ciblé en fin d'US : `./gradlew test --tests "codebase.koog.*"`
  (pas de `./gradlew test` global — Cucumber interminable)
- English only : code, Gherkin, comments
- Build autonome : `cd codebase-plugin && ./gradlew ...`
- Pas de bump BOM sans permission (artefact local)
- AGENTS.adoc racine : commit/push autorisés en fin de session (pilote)

== Références

* `CADRAGE_EPIC_VIBE_HARDENING.adoc` — 10 décisions architecturales
* `.agents/sessions/187-vibe-hardening-us3-us4-us5-clos.adoc` — clôture EPIC
* `.agents/sessions/186-vibe-hardening-us2-gradle-allowlist.adoc` — US-2
* `.agents/archives/COMPLETED_TASKS_ARCHIVE_2026-08.adoc` — archive août