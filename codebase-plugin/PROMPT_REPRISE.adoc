= PROMPT_REPRISE — Codebase (Queens N1) — Session 130
:session: 130
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 129 — EPIC PUB Preflight + Pivot Gouvernance Locale ✅

== Contexte Hérité

- Build débloqué (annotations fix, codex 0.0.1, validatePlugins OK).
- `publishToMavenLocal` signé et OK.
- Gouvernance `.agents/` versionnée et réconciliée.
- `GovernanceContextLoader` créé avec tests JUnit5 pour charger AGENT/INDEX/PROMPT_REPRISE/SESSIONS_HISTORY/TEST_COVERAGE/BACKLOG dans un `AgentContext`.
- Décision : ne pas publier sur Maven Central maintenant ; prioriser la boucle vibecoding locale auto-alimentée puis le pont vers la gouvernance augmentée (EPIC Y).

== Mission Session 130

Finaliser la boucle vibecoding locale avec gouvernance textuelle, puis ouvrir le pont EPIC Y.

. Câbler `GovernanceContextLoader` dans `SessionProtocolServer` et `SessionProtocolTask` :
** fallback auto quand aucun `--contextFile` n'est fourni ;
** utiliser le répertoire racine du projet Gradle comme source de gouvernance.
. Tests baby-step :
** Test unitaire : `GovernanceContextLoader` ✅ (déjà fait session 129)
** Test fonctionnel : `SessionProtocolTask` sans `--contextFile` charge la gouvernance.
** Test Cucumber : scénario "opencode prompt → daemon répond avec contexte EAGER injecté".
. Mettre à jour `LOCAL_VIBECODING_LOOP.adoc` :
** Documenter le chargement automatique des fichiers agents.
** Expliquer le fallback textuel vs contexte augmenté (EPIC Y).
. Préparer le pont EPIC Y :
** Stuber ou implémenter une tâche Gradle `IngestGovernanceTask` qui envoie les fichiers agents dans `AgenticIngestor` (chunks → ontologie → SQL → pgvector).
** Documenter la transition "texte → chunks → SQL → augmented context".

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation + tests entre chaque étape, pas de publication Central.
