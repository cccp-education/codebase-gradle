= PROMPT_REPRISE — Codebase (Queens N1) — Session 130
:session: 130
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 129 — EPIC PUB Preflight ✅ (build + validateDependencies + validatePlugins + publishToMavenLocal OK)

== Contexte Hérité

- Prérequis PUB débloqués en session 129 :
** Conflit annotations fixé (resolutionStrategy force 13.0)
** codex-plugin aligné sur 0.0.1 (Central)
** 8 erreurs validatePlugins corrigées
** `publishToMavenLocal` signé et OK
- `codebase-plugin` version 0.0.2 (libs.versions.toml)
- Gouvernance `.agents/` réconciliée (archive 129, SESSIONS_HISTORY, INDEX)

== Mission Session 130

EPIC PUB — Finaliser la publication Maven Central de `codebase-plugin`.

. Vérifier credentials `~/.gradle/gradle.properties` (signing + nmcp centralPortal).
. Lancer `./gradlew publishAggregationToCentralPortal --no-daemon` si credentials OK.
. Vérifier artefacts sur https://central.sonatype.com/artifact/education.cccp/codebase-plugin.
. Si publication réussie : marquer EPIC PUB ✅ TERMINÉ dans BACKLOG.adoc et INDEX.adoc.
. Si échec credentials : documenter le blocage et passer à l'audit PUB-5.8.

== Méthodologie

Baby-step DDD/TDD/BDD : chaque étape validée par build/test avant la suivante.
