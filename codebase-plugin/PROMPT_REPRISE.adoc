= Prompt de Reprise — Codebase (Queens N1) — Session 177
:session: 177
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 176 — SLD-8 US-8.1 LlmBuildService Gradle bridge + corrections architecture. Pattern « Plugin Indépendant + Racine Consommateur » restauré (article 0124). codebase-plugin/ autonome avec son catalog. BOM 0.0.16 + codebase 0.0.5 mavenLocal. plugin.serialization fix (codebase + planner). US-8.1 livrée TDD/BDD 3 niveaux (5 unit + 3 functional + 4 cucumber = 12 tests, 0 FAIL).

== Mission Session 177

SLD-8 continue : US-8.2 côté slider. `AssistantManager` (648 lignes, 4
factories LLM) réduit à du câblage — injection `Provider<LlmBuildService>`
depuis codebase. Adaptateur `LlmProviderChatModelAdapter` (côté slider)
adapte `LlmProvider` (suspend fun `call(prompt): String`) → `ChatModel`
langchain4j (concat system+user en prompt string). Préserve le code de
consommation existant (`RagTasks`, `TranslateDeckTask`).

== État actuel

- Version codebase-plugin : `0.0.5` (hardcodé build.gradle.kts, mavenLocal)
- BOM workspace-bom : `0.0.16` (mavenLocal, codebase constraint 0.0.5)
- US-8.1 ✅ TERMINÉ : `LlmBuildService` + `LlmServiceResolver` (codebase)
- US-8.2 🟡 À CÂBLER : `AssistantManager` slider → 4 factories supprimées
- US-8.3 🟡 À PLANIFIER : `DeckPipelineGraph` sur services codebase
- BOM 0.0.16 + codebase 0.0.5 non publiés sur Central (mavenLocal only)

== Rappels

- GroupId : `education.cccp`
- Pattern architecture : « Plugin Indépendant + Racine Consommateur » (article 0124)
- `codebase-plugin/` est un build autonome (son propre settings, catalog, wrapper)
- `codebase-gradle/` est racine consommateur (catalog minimal, dogfooding)
- DI native Gradle (Decision 001 slider) : `BuildService` + `Provider<T>`
- `LlmProvider` (fun interface) : `suspend fun call(prompt: String): String`
- `LlmProviderResolver.resolve(model): LlmProvider` — factory `gemini`/`ollama`/custom
- `LlmBuildService` : `BuildService<Params>` avec `provider(): LlmProvider`
- Baby-step TDD/BDD 3 niveaux : unit → functional → cucumber
- English only : code, Gherkin, comments
- `./gradlew compileKotlin` côté codebase ; `./gradlew :slider:check` côté slider

== Décisions architecture (DECISIONS_ARCHITECTURE.adoc slider)

- Decision 001 : rejet PicoContainer, DI native Gradle
- Decision 002 : slider consomme codebase (N1) comme socle LLM unifié