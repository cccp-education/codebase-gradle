= PROMPT_REPRISE — Codebase (Queens N1) — Session 099
:session: 099
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:sessions_precedente: 098 — W-0+W-1 TaskSchemaScanner (7/7 JUnit5 PASS)

== Contexte Hérité

- EPIC W-0 + W-1 TERMINÉS : TaskSchema (name, description, group, options, toToolInfo) + TaskSchemaScanner (walkHierarchy, @Option reflexion)
- 3 fichiers créés : TaskSchema.kt, TaskSchemaScanner.kt, TaskSchemaScannerTest.kt
- 7/7 tests JUnit5 PASS, 0 régression
- BACKLOG.adoc à jour (EPIC W 2/7)
- ToolRegistry existant (7 tools N0) + gradle_<taskName> mapping prêt

== Mission Session 099 — EPIC W-2 + W-3

*W-2* : Générer un schema structuré JSON/OpenAPI depuis le TaskSchemaScanner.
  Format OpenAPI `paths` → chaque tâche = un endpoint.

*W-3* : Auto-registrer les tâches découvertes dans ToolRegistry
  au démarrage de VibecodingGraph. `list_tasks` tool pour que le LLM
  découvre le catalogue.

== Ordonnancement Sessions Suivantes

| Session | EPIC | Livrable
| 099 | W-2 + W-3 | OpenAPI Spec + Auto-register ToolRegistry
| 100 | W-4 + W-5 + W-6 | list_tasks tool + mapping args + cache → EPIC W TERMINÉ
| 101 | X-0 + X-1 | Cadrage + VibecodingPlan structuré (dépend de W)
| 102 | X-2 + X-3 | TaskResultVerifier + boucle verify→adapt
| 103 | X-4 + X-5 + X-6 | RollbackStrategy + intégration W + Cucumber → EPIC X TERMINÉ
| 104 | Z-0 + Z-1 | Autofocus cadrage + ZoomLevel
| 105 | Z-2 + Z-3 | ContextZoomer + ZoomPolicy
| 106 | Z-4 + Z-5 + Z-6 | Intégration VibecodingGraph → EPIC Z TERMINÉ
| 107 | 12 | Dilution Article de Blog (quick win)
| 108+ | 5 | Pipeline fine-tuning experts CDA/FPA (lourd)
