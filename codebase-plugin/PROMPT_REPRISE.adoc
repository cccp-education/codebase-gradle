= PROMPT_REPRISE — Codebase (Queens N1) — Session 142
:session: 142
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 141 — EPIC V-9.5 ChunkValidator livré (10 JUnit5 + 5 Cucumber @epic_v_9_5 PASS)

== Contexte Hérité

- Session 141 a livré V-9.5 ChunkValidator : `ChunkValidator.kt` (validate id/sourceFile/content/sourceLines/checksum + checksum sha256 cohérent + weight 0.0..1.0) + `ValidationResult` + 10 JUnit5 + 5 scénarios Cucumber `@epic_v_9_5` PASS. Zéro réseau, zéro Testcontainers.
- V-9.1 AgenticChunker + V-9.2 ChunkType enum déjà livrés par EPIC Y (anti-duplication vérifiée session 141).
- V-9.4 ChunkExtractor déjà couvert par AgenticChunker (sha256 id + checksum).
- V-9.3 ScanAgent reste à coder : `IngestGovernanceTask.collectGovernanceFiles` hardcode 7 paths EAGER — remplacer par scanner récursif du dossier `.agents/` sans casser les 5 tests `IngestGovernanceTaskTest` PASS.
- EPIC V-FLYWAY reste en BACKLOG (seuil déclencheur : 5+ migrations de schéma).

== Mission Session 142

1. Démarrer EPIC V-9 Under-EPIC 1 item restant : V-9.3 ScanAgent — scanner récursif du dossier `.agents/` découvrant tous les `.adoc` (pas une liste hardcoded).
2. Baby-step DDD/TDD/BDD : contrat `ScanAgent` + `ScannedFile` d'abord (unit JUnit5), puis extraction fonctionnelle, puis scénario Cucumber `@epic_v_9_3`.
3. Un fichier modifié à la fois, compilation entre chaque étape.
4. `./gradlew :codebase-plugin:compileKotlin` doit rester vert.
5. Ne PAS casser `IngestGovernanceTaskTest` (5 tests PASS existants) — si refactor de `collectGovernanceFiles`, préserver son comportement public.
6. Si V-9.3 terminé, Under-EPIC 1 est complet (5/5) → attaquer Under-EPIC 2 V-9.6 AgenticOntologizer (déjà partiellement livré par EPIC Y-2, vérifier anti-duplication).

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.