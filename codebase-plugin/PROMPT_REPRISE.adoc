= Prompt de Reprise — Codebase (Queens N1) — Session 154
:session: 154
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 153 — EPIC V-9.17 Ingestion Summary DSL livré (extension `codebaseGovernance { strictValidation.set(true) }` + propriété `codebase.governance.strictValidation`, 3 JUnit5 + 4 fonctionnels + 3 Cucumber @epic_v_9_17 PASS, régressions V-9.x vertes)

== Mission Session 154

1. Attaquer V-9.18 Compilation vers Gradle tasks — pont AgenticCompiler → tâches concrètes : quand un chunk PROCEDURE/GENERER ou CONSTRAINT est ingéré, produire une tâche Gradle enregistrée dynamiquement dans le projet (ex: `enforceRule<ruleId>` ou `runProcedure<chunkId>`).
2. Coder en baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber.
3. Préserver V-9.3, V-9.5, V-9.7, V-9.8, V-9.9, V-9.10, V-9.11, V-9.13, V-9.14, V-9.15, V-9.16, V-9.17 et tous les tests `ToolRegistry` existants (zéro régression).
4. `./gradlew :codebase-plugin:compileKotlin` doit rester vert entre chaque fichier modifié.

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.
