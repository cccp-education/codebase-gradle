= Prompt de Reprise — Codebase (Queens N1) — Session 191
:session: 191
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 190 — VIBE-HARDENING-2 US-3 C4 GradleTaskArgsMapper flags cassés (P2, 3 pts). `GRADLE_ARG_ALLOWLIST` étendu `--\w+(?:=\S+)?` + `isBareLongFlag` helper + `validateGradleTask` accepte pair flag+value. Fail préexistant résolu. 5 unit + 110 gate VERT. 3/8 US. Borough DORMANT.

== Mission Session 191

EPIC VIBE-HARDENING-2 (P2/P3 hardening) — 5 US restants sur 8.
Borough DORMANT — réveil sur déclencheur pilote.

=== US-4 C1 TaskResultVerifier refonte (P1, 4 pts)

`TaskResultVerifier.verify` (TaskResultVerifier.kt:17-43) heuristique
fragile : `combined.contains("failed")` matche `0 failed tests` →
`TaskVerdict.FAILED` faux positif. `expectedOutput` (GradleTask N0
S-064, StepVerifier) n'est jamais comparé au stdout réel.

* Stratégie : (1) heuristique raffinée — `failed` doit être précédé
  d'un contexte négatif (`BUILD FAILED`, `tests failed`, `compilation
  error`), pas `0 failed` ; (2) comparaison `expectedOutput` quand
  non-default `"BUILD SUCCESSFUL"` : si `stdout.contains(expectedOutput)`
  → SUCCESS, sinon FAILED. ~10 unit.
* Backward compat : heuristique actuelle conservée comme fallback quand
  `expectedOutput` est default.
* Gate ciblé : `./gradlew test --tests "codebase.koog.planning.*"`.
* Document : `.agents/CADRAGE_EPIC_VIBE_HARDENING_2.adoc` US-4 (Décision 3).

=== Backlog restant (sur déclencheur pilote)

US-5 C3 cleanup doc INSERT-X1..X6 (1 pt) → US-6 C7 coerceAtLeast (1 pt)
→ US-7 C6 SessionProtocolEngine (4 pts) → US-8 Cucumber BDD (2 pts).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en attente — alternative
  possible sur déclencheur pilote.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.