= Prompt de Reprise — Codebase (Queens N1) — Session 200
:session: 200
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 199 — EPIC CDX-RAG-SOCLE US-1 (cross-borough Brooklyn pilote). Domaine `codebase.store` créé (5 prod + 3 test) : migration verbatim `RagVectorStore`/`StoreStatements`/`IngestIndexing` + types autonomes `RetrieveResult`/`DocumentChunk` depuis `codex.store`. 25/25 characterization tests PASSED. Pré-requis bump versions : codex 0.0.5 mavenLocal + codebase BOM 0.0.24 + codex 0.0.5 + codebase 0.0.8 republié mavenLocal. Zéro régression (call sites `CodexVectorStore` inchangés — bascule RAG-2). EPIC CDX-RAG-SOCLE 2/6 US.

== Mission Session 200

[STRATEGIE]
EPIC CDX-RAG-SOCLE US-2 : bascule interne codebase.

=== US CDX-RAG-2 — Bascule 6 call sites

Remplacer les 6 call sites `codex.store.CodexVectorStore` →
`codebase.store.RagVectorStore` (API identique `searchBlocking`).

Fichiers concernés (audit S-097) :
. `codebase/rag/CompositeContextBuilder.kt:7,17,63,75` (param + log messages)
. `codebase/rag/CodebaseCompositeContextTask.kt:3,41,54,56` (import + instanciation + logs)
. `codebase/koog/KoogAugmentedContextGraph.kt:14,125` (import + instanciation)
. `codebase/koog/MultiChannelContextGraph.kt:17,142,150` (import + instanciation + log)
. `codebase/koog/KoogPlanningOrchestrator.kt:14,153` (import + instanciation)
. `codebase/CodebasePlugin.kt:176` (description task)

Gate non-régression : tests `codebase.rag.*` + `codebase.koog.*` VERTS.
Pure mécanique — API identique. Zéro bump BOM, zéro secret.

=== Suite EPIC (après RAG-2)

* RAG-3 : codex — bascule + purge `codex.store.*` (dep `libs.codebase.plugin`)
* RAG-4 : codebase — retrait `libs.codex.plugin` (`build.gradle.kts:49`)
* RAG-5 : Cucumber BDD — composite context canal Docs + retrieval koog

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central. BOM 0.0.24
  (bump S-199). codex-plugin 0.0.5 (bump S-199).
* Domaine `codebase.store` créé S-199 (5 prod + 3 test, 25 tests).
* Gate ciblé : `./gradlew test --tests "codebase.store.*"` +
  `--tests "codebase.rag.*"` (pas de `./gradlew test` global — Cucumber interminable).
* 39 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.