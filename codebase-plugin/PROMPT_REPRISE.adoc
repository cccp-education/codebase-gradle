= Prompt de Reprise — Codebase (Queens N1) — Session 192
:session: 192
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 191 — VIBE-HARDENING-2 US-4 C1 TaskResultVerifier refonte (P1, 4 pts). Surcharge `verify(stdout, stderr, expectedOutput)` + `hasActualFailure` raffiné (`\\bfailed\\b` non précédé de `0 `/`no `) + `StepVerifier` câble `step.expectedOutput` (contrat N0 PLN-VERIFY). Bug subtil lowercase `DEFAULT_EXPECTED_OUTPUT_LOWER` fix. 26 TaskResultVerifier + 13 StepVerifier + 22 VibecodingGraph + 97 gate = 158 tests VERT. 4/8 US. Borough DORMANT.

== Mission Session 192

EPIC VIBE-HARDENING-2 (P2/P3 hardening) — 4 US restants sur 8.
Borough DORMANT — réveil sur déclencheur pilote.

=== US-5 C3 cleanup doc INSERT-X1..X6 (P3, 1 pt)

`VibecodingGraph.kt:60-84` contient 6 blocs commentaires `// [INSERT-X1]`
jusqu'à `[INSERT-X6]` décrivant des insertions à faire — ces insertions
ont été faites (EPIC X sessions 101-105). Le javadoc est stale et trompeur.

* Stratégie : supprimer les commentaires INSERT-X1..X6 (dead doc).
  `VibecodingPlan` (L18 `planning/VibecodingPlan.kt`) est utilisé
  (import L13) — pas dead code. ~0 test (pure doc cleanup).
* Document : `.agents/CADRAGE_EPIC_VIBE_HARDENING_2.adoc` US-5 (Décision 4).

=== Backlog restant (sur déclencheur pilote)

US-6 C7 coerceAtLeast (1 pt) → US-7 C6 SessionProtocolEngine (4 pts) →
US-8 Cucumber BDD (2 pts).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en attente — alternative
  possible sur déclencheur pilote.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.