= Prompt de Reprise — Codebase (Queens N1) — Session 160
:session: 160
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 159 — EPIC OCR-1/OCR-2 restants TERMINÉS (9/9 items). HTTP 429 detection + rotation auto (GeminiKeyPool.markRateLimited/isRateLimited, GeminiVisionProvider catch 429). Env var mapping GEMINI_API_KEY_1..N (GeminiPoolFactory.fromKeys/fromEnvVars). Multi-compte Google (GeminiMultiAccountPool + fromEnvVars multi). Tesseract fallback harmonisé : OcrEngine + TesseractOcrEngine dans codex-gradle (Brooklyn), CodexOcrEngineAdapter dans codebase-gradle (Queens). 35 tests nouveaux + 11 Cucumber @epic_ocr_1/@epic_ocr_2 PASS. codex publishToMavenLocal 0.0.1. EPIC OCR complet 7/7.

== Mission Session 160

EPIC OCR étant clôturé (7/7), reprendre l'EPIC V-9 Gouvernance Agentique
(14/35, Under-EPIC 3 reste) ou EPIC V-6-POOL extensions selon backlog.

Source : `codebase-plugin/.agents/BACKLOG.adoc` + `codebase-plugin/.agents/INDEX.adoc`.

=== Items V-9 restants (Under-EPIC 3+)

* V-9.21+ : Items non commencés du Under-EPIC 3 (voir BACKLOG.adoc)
* Vérifier INDEX.adoc ligne 49 pour statut exact

=== Contexte technique

* `GeminiKeyPool.kt` enrichi (markRateLimited/isRateLimited/rateLimitedIds)
* `GeminiPoolFactory.kt` + `GeminiMultiAccountPool.kt` livrés (OCR-1 complet)
* `CodexOcrEngineAdapter.kt` + `codex.ocr.TesseractOcrEngine` livrés (boundary codex↔codebase)
* `GeminiVisionProvider.kt` ouvert, accepte `GeminiKeyPool` optionnel
* `OcrTask.kt` : `geminiApiKeys`, `tesseractOcrProvider`, `resolveGeminiProvider`/`resolveGeminiPool`

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié
à la fois, compilation entre chaque étape. Pas de subagent, pas de
parallélisation.

== Contrainte Ollama (rappel AGENTS.adoc)

Ports 11434-11436 INTERDITS. Ports 11437-11465 autorisés (rotation).
Modèles autorisés : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
Aucun autre modèle ni port.