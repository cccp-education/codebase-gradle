= Prompt de Reprise — Codebase (Queens N1) — Session 211
:session: 211
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 210 — Maintenance état post-EPIC MEMPHIS-BOM-FIX (2026-08-27). Tous EPICs vérifiés terminés/CADUQUES, build compile, 679 tests 0 FAIL baseline préservée, zéro modification production, borough DORMANT confirmé.
:session_pre_precedente: 209 — EPIC MEMPHIS-BOM-FIX US-4 TERMINÉ (cross-borough). codebase-plugin 0.0.9 → 0.0.10 publié Central (fix kotlin-stdlib-jdk8 version explicite, POM importe BOM 0.0.27). EPIC clôturé 5/5 US.
:pub: codebase-plugin 0.0.10 publié Maven Central (S-209). Version locale courante : 0.0.11 (bump post-S-210, voir ci-dessous).

== Mission Session 211

[IMPORTANT]
Borough DORMANT — tous EPICs TERMINÉS ou CADUQUES. Maintenance uniquement.

=== ⚠️ État git post-S-210 à consigner (à faire en ouverture)

Des commits post-S-210 ne sont consignés dans aucun rapport de session :

* `58670ad` — bump codebase-plugin 0.0.10 → **0.0.11** (sync après publication Central)
* `ba2fa1c`/`0911bf7` — align conventions-plugin 0.0.4
* `fb19b7c` — fix(ocr) : propriété morte `ocrEngine Provider<OcrEngine>` retirée de `CodebaseOcrExtension` (décoration Gradle impossible)

Action d'ouverture : vérifier `git log --oneline -15`, confirmer que ces
commits sont intentionnels, puis consigner un rapport de session si absent.

=== Option A — Maintenance

* Dette technique ciblée, code review, bumps de version.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).

=== Option B — Cross-borough

* EPIC MEM-CAT-ROLLOUT (MEMPHIS) en cours — codebase n'est pas dans les
  boroughs restants (ROLLOUT-6 = dashboard, readme, graphify, bureau).
* Assister un borough (codex, planner, capsule) si demandé.

=== Option C — Nouvel EPIC

* Cadrer si besoin identifié (ex : extension RagVectorStore pour
  ingestion documentaire côté socle si codex délègue davantage).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version locale : 0.0.11 (Central : 0.0.10). Conventions 0.0.4.
* 41 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).
* Inversion N1→N2 morte : codex consomme codebase via ocr-contracts N0.
* Session concurrente possible : vérifier `git status`, mtimes récents,
  absence d'autre session active AVANT d'éditer.
* Dossier gouvernance unique : `codebase-plugin/.agents/` (migration S-207).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.
* Ne JAMAIS lancer `./gradlew test` global en fin de session sans permission.

== References

* `.agents/sessions/210-maintenance-etat-post-mempfix.adoc`
* `.agents/sessions/209-memphis-bom-fix-us4.adoc`
* `.agents/BACKLOG.adoc` (tous EPICs ✅/CADUQUE)
* `.agents/INDEX.adoc` (dernière mise à jour session 211 — sync gouvernance)