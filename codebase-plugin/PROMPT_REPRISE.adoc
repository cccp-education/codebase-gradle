= Prompt de Reprise — Codebase (Queens N1) — Session 198
:session: 198
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 197 — EPIC FT-PIPELINE US-5 TERMINÉ (Cucumber BDD). 4 scénarios `@finetuning` (pipeline complet dataset→GGUF→manifest, cycle itératif convergence, fallback Ollama unavailable, validation seuil) + `FineTuningCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestFineTuning")` 39e + `FineTuningWorld` (`StubRegistryClient` in-memory) + `FineTuningSteps` (16 step defs préfixés + `FakeFineTuningLlm` FIFO + `StubPromptBuilder`). 4/4 PASSED 9s (17 steps). Zéro prod modifié — pure BDD. Gate finetuning + CodebasePluginTest non-régression VERT.

== Mission Session 198

EPIC FT-PIPELINE 5/7 US terminé. Borough DORMANT — réveil sur
déclencheur pilote uniquement.

=== Option A — EPIC FT-PIPELINE US-6 (cross-borough training-gradle)

US-6 (1 pt, gouvernance pure) : training-gradle TRN-8 reformulé en
recettes CDA/FPA consommant `fineTuneExpert` de codebase. Dépendance
N2→N1 propre (training appelle codebase pipeline, spécifie
datasets/modèles). Documentation mise à jour (INDEX/BACKLOG training +
codebase). Document : `.agents/CADRAGE_EPIC_FT_PIPELINE.adoc` US-6.
Clôture EPIC FT-PIPELINE TERMINÉ 7/7 US.

=== Option B — Maintenance / déclencheur pilote

Aucun EPIC actif. Borough DORMANT jusqu'à déclencheur pilote.
Réveil sur déclencheur (nouveau borough consommateur, bug, évolution
contrat N0).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en cours — 5/7 US livrés
  (reste US-6 cross-borough training-gradle, gouvernance pure 1 pt).
* Gate ciblé : `./gradlew test --tests "codebase.finetuning.*"`
  (pas de `./gradlew test` global — Cucumber interminable).
* 39 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.