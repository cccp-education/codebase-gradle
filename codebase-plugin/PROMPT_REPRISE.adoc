= Prompt de Reprise — Codebase (Queens N1) — Session 199
:session: 199
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 198 — EPIC FT-PIPELINE US-6 TERMINÉ (cross-borough training-gradle). TRN-8 reformulé en recettes CDA/FPA consommant FT-PIPELINE (~10 pts). Séparation N1/N2 actée — machinerie codebase (TERMINÉ 7/7 US S-188→S-198), recettes métier training. Littérature agentique préparatoire posée dans BACKLOG training (constat N1/N2 + plan baby-steps TRN-8.0→8.6), démarrage coding reporté post-travaux codex prioritaires. EPIC FT-PIPELINE TERMINÉ 7/7 US. Zéro code modifié — gouvernance pure cross-borough pattern S-057.

== Mission Session 199

EPIC FT-PIPELINE TERMINÉ 7/7 US. Borough DORMANT — réveil sur
déclencheur pilote uniquement. Aucun EPIC actif.

=== Option A — Maintenance / déclencheur pilote

Aucun EPIC actif. Borough DORMANT jusqu'à déclencheur pilote.
Réveil sur déclencheur (nouveau borough consommateur, bug, évolution
contrat N0, training TRN-8 démarrage coding post-travaux codex).

=== Option B — Travaux codex (Brooklyn) prioritaires

Le pilote a signalé des travaux codex prioritaires avant tout démarrage
coding training TRN-8. Codebase reste DORMANT pendant ce temps.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) TERMINÉ 7/7 US
  (S-188→S-198). Domaine `codebase.finetuning` clos BDD S-197
  (17 fichiers prod + 13 test + 1 feature + 3 cucumber infra).
* Gate ciblé : `./gradlew test --tests "codebase.finetuning.*"`
  (pas de `./gradlew test` global — Cucumber interminable).
* 39 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.