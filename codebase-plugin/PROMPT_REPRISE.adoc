= Prompt de Reprise — Codebase (Queens N1) — Session 203
:session: 203
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 202 — CDX-OCR-CONTRACTS US-4 Cucumber BDD boundary port N0 (`codebase_ocr_contracts.feature` 5 scénarios `@ocr-contracts`, runner `OcrContractsCucumberRunner` 41e CucumberTaskSpec, `StubVisionProvider` fake). **EPIC CDX-OCR-CONTRACTS TERMINÉ 5/5 US.**

== Mission Session 203

[STRATEGIE]
Borough retombe DORMANT côté EPICs. Deux pistes possibles (accord pilote) :

=== Option A — Publications Central en attente (accord pilote requis)

* MEMPHIS : `ocr-contracts:0.0.1` Maven Central (nmcp) + BOM 0.0.26
* codex : `codex-plugin:0.0.6` Maven Central (PUB-3.12)
* codebase : republier si bump nécessaire (dep N0 déjà explicite)

=== Option B — Maintenance / dette

* SLD-8 US-8.2/8.3 : côté slider (consommation LlmBuildService)
* EPIC V-FLYWAY : P2 conditionnel (seuil 5+ migrations, actuellement 4)
* PublishingConventionsPlugin pom.name+description (conventions-gradle)
* Warnings CI actions@v4→v5

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* codebase-plugin 0.0.8 = Maven Central. Dep N0 `ocr-contracts:0.0.1`
  mavenLocal (publication Central à coordonner avec MEMPHIS).
* EPICs récents tous terminés : CDX-RAG-SOCLE 6/6 (S-201),
  CDX-OCR-CONTRACTS 5/5 (S-202), FT-PIPELINE 7/7 (S-198).
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* 41 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.
