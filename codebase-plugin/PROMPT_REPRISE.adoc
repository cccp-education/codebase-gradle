= Prompt de Reprise — Codebase (Queens N1) — Session 189
:session: 189
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 188 — Maintenance + cross-borough audit + cadrage EPIC VIBE-HARDENING-2 + propagation 5 boroughs (planner/slider/capsule/codex/training bumpés codebase 0.0.8, 4 compileKotlin VERT codex fail préexistant hors scope). 3 bugs découverts signalés (MEMPHIS BOM 0.0.19 slider 0.0.8 non publié, ABIDJAN codex kotlin-stdlib, capsule codebase-contracts 0.1.0 local-only corrigé). EPIC VIBE-HARDENING-2 CADRÉ 8 US ~21 pts. Borough DORMANT.

== Mission Session 189

EPIC VIBE-HARDENING-2 est cadré (8 US ~21 pts) et prêt à démarrer
sur déclencheur pilote. Borough DORMANT.

=== Phase 1 — EPIC VIBE-HARDENING-2 US-1 (S2 Allowlist TaskDiscoveryRegistrar)

`TaskDiscoveryRegistrar` auto-registration libre `gradle_*` sans allowlist
(publication accidentelle possible). Ajouter `TASK_DISCOVERY_ALLOWLIST`
(2 patterns : bare tasks + project:task) + deny-list second ride, pattern
US-1/US-2 VIBE-HARDENING (ExecShellTool/ExecGradleTool).

* Baby-step TDD strict (RED → GREEN → REFACTOR).
* Gate ciblé : `./gradlew test --tests "codebase.koog.discovery.*"`
  (pas de `./gradlew test` global — Cucumber interminable).
* ~8 unit, 4 pts.

=== Phase 2 — EPIC VIBE-HARDENING-2 US-2 (S3 Garde taille write/edit)

`ToolRegistry.kt:125/138` `write_file`/`edit_file` sans garde de taille
(OOM potentiel — `read_file` a déjà `MAX_OUTPUT_CHARS` L117). Ajouter
`MAX_WRITE_CHARS` 1MB + validation avant exécution.

* Baby-step TDD strict.
* Gate ciblé : `./gradlew test --tests "contracts.vibecoding.tools.*"`.
* ~4 unit, 2 pts.

=== Phase 3 — EPIC VIBE-HARDENING-2 US-3 (C4 Flags GradleTaskArgsMapper)

`GradleTaskArgsMapper.kt:18` flags `--key value` cassés vs
`GRADLE_ARG_ALLOWLIST` US-2 (S-186) `--\w+` (value rejeté deny-by-default).
Fix pattern flags avec valeur.

* Baby-step TDD strict.
* Gate ciblé : `./gradlew test --tests "codebase.koog.discovery.*"`.
* ~6 unit, 3 pts.

=== Backlog restant (sur déclencheur pilote)

* US-4 C1 TaskResultVerifier refonte expectedOutput comparison + failed raffiné
  (~10 unit, 4 pts).
* US-5 C3 Cleanup doc INSERT-X1..X6 (0 test, 1 pt).
* US-6 C7 coerceAtLeast(0) remaining tasks (~1 unit, 1 pt).
* US-7 C6 Extraction SessionProtocolEngine (~8 unit non-régression, 4 pts).
* US-8 Cucumber BDD codebase_vibe_hardening_2.feature (~4 scénarios, 2 pts).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...` (la racine
  `codebase-gradle` n'a aucun sous-projet — `:codebase-plugin:compileKotlin`
  échoue depuis la racine).
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Consommateurs publics à jour : planner, slider, capsule (0.0.8).
  plantuml/bakery résolvent 0.0.8 via BOM 0.0.19. codex fail préexistant
  (bug ABIDJAN conventions-gradle). training 0.0.8.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* ARRET OBLIGATOIRE avant toute action destructive (lire, vérifier
  `git ls-files`, confirmation pilote).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable,
  pratique canonique S175/S179).
* Zéro secret, zéro token dans le code ou les logs.