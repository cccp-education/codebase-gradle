= PROMPT_REPRISE — Codebase (Queens N1) — Session 139
:session: 139
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 138 — OllamaInstanceFactory ✅ + Cucumber Ollama cloud quota ✅

== Contexte Hérité

- Session 138 a livré `OllamaInstanceFactory` : génération déterministe d'instances Ollama Cloud sur ports 11437→11465, modèles cyclés, et `volumeTag` métier dans le contrat N0 `LlmInstance`.
- La feature Cucumber `@epic_v6_ollama_cloud` couvre la rotation sur quota exceeded avec fake provider (zéro réseau).
- EPIC V-6 reste ouvert : `LlmProviderResolver` utilise encore `OllamaInstanceScanner` (probe HTTP) par défaut ; il faut lui permettre de basculer vers la factory déterministe.
- Build propre, tests ciblés verts.

== Mission Session 139

1. Modifier `LlmProviderResolver` pour utiliser `OllamaInstanceFactory` quand `OLLAMA_POOL_PORTS` est absent (mode déterministe, sans probe HTTP).
2. Conserver `OllamaInstanceScanner` comme fallback explicite si `OLLAMA_SCAN_PORTS=true` ou `OLLAMA_POOL_PORTS` est défini.
3. Tests JUnit5 baby-step : resolver retourne provider avec 29 instances en mode factory, resolver honore `OLLAMA_POOL_PORTS`, scanner fallback activable.
4. Test Cucumber `@epic_v6_resolver` : "Resolver builds Ollama cloud provider from deterministic factory".
5. `./gradlew :codebase-plugin:compileKotlin` puis tests ciblés verts.

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.
