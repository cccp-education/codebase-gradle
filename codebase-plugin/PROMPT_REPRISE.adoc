= Prompt de Reprise — Codebase (Queens N1) — Session 193
:session: 193
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 192 — EPIC VIBE-HARDENING-2 TERMINÉ 8/8 US (US-5 C3 cleanup doc INSERT-X1..X6 + US-6 C7 coerceAtLeast(0) + US-7 C6 SessionProtocolEngine extraction + US-8 Cucumber BDD 4 scénarios). Baby-step TDD/BDD strict. 11 fichiers (5 modifiés + 6 créés). `SessionProtocolEngine.kt` créé (105 lignes) — délégation Server/Task (-37/-49 lignes). 2 unit coerceAtLeast + 9 unit engine + 4/4 cucumber PASSED. `compileKotlin` VERT, zéro régression, zéro secret, zéro bump BOM. Borough DORMANT.

== Mission Session 193

EPIC VIBE-HARDENING-2 TERMINÉ 8/8 US. Borough DORMANT — réveil sur
déclencheur pilote uniquement.

=== Option A — EPIC FT-PIPELINE US-1 (P0 vision, domaine `codebase.finetuning`)

US-1 (3 pts, ~8 unit) : `FineTuningConfig` data class + merger 4 sources
(pattern `ValidationConfig`) — `epochs`, `learningRate`, `batchSize`,
`corpusGlobs`, `continualPreTrainingRatio` (0.10). Domaine
`codebase.finetuning` (7ème). Baby-step TDD strict. Document :
`.agents/CADRAGE_EPIC_FT_PIPELINE.adoc` US-1.

=== Option B — Maintenance / déclencheur pilote

Aucun EPIC actif. Borough DORMANT jusqu'à déclencheur pilote.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* Borough DORMANT — réveil sur déclencheur pilote uniquement.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) en attente — alternative
  possible sur déclencheur pilote.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.