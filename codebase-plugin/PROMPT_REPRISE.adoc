= Prompt de Reprise — Codebase (Queens N1) — Session 173
:session: 173
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 172 — EPIC C-BOM-FIX TERMINÉ. Versions explicites pour 6 artefacts education.cccp + 27 deps externes dans libs.versions.toml. Bump 0.0.5→0.0.6. publishToMavenLocal OK. 22/22 UT bakery LangSwitch GREEN. Zéro régression codebase.

== Mission Session 173

Codebase-gradle est DORMANT. Version 0.0.6 publiée sur Maven Local.
La session 172 a résolu le bug bloquant EPIC C-BOM-FIX (débloque bakery BKY-LANG-SWITCH).

Réveil sur déclencheur externe uniquement :
. *Publication* — publier `codebase-plugin:0.0.6` sur Maven Central via nmcp
  (`publishAggregationToCentralPortal`). Le POM 0.0.6 contient les versions
  explicites, la propagation transitives est résolue.
. *Consommateur* — training-gradle (Newark) ou Edster SaaS MVP1 exprime un besoin
. *V-FLYWAY* — si 5+ fichiers de migration de schéma (actuellement 4 : V1→V4)
. *Bug critique* — régression signalée par un borough consommateur
. *Dette technique CI* — warnings GitHub Actions Node.js 20 deprecated (actions@v4→v5)

== Backlog Restant

. EPIC V-FLYWAY — P2 conditionnel, seuil 5+ migrations (actuellement 4 : V1→V4)
. Publication Maven Central 0.0.6 — si bakery confirme que 0.0.6 Maven Local suffit
. Warnings CI — actions/checkout@v4, setup-java@v4, cache@v4, upload-artifact@v4 → upgrade v5 (Node.js 24)

== Contexte Technique

* Version : `0.0.6` (bump session 172)
* Maven Local : `education.cccp:codebase-plugin:0.0.6` publié (POM + .module avec versions)
* Maven Central : `0.0.5` publié (POM sans versions — bug C-BOM-FIX résolu en 0.0.6)
* BOM : `education.cccp:workspace-bom:0.0.4` publié
* Tests : tous UT PASS (zéro régression), 3 nouveaux UT publishing
* Bakery : 22/22 UT LangSwitch GREEN avec codebase-plugin:0.0.6

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié
à la fois, compilation entre chaque étape.

== Contrainte Ollama (rappel AGENTS.adoc)

Ports 11434-11436 INTERDITS. Ports 11437-11465 autorisés (rotation).
Modèles autorisés : `gpt-oss:120b-cloud`, `gemma4:31b-cloud`.
Exception CI/CD : ports 11466-11467 + `qwen3:0.6b` (tests d'intégration GitHub Actions uniquement).