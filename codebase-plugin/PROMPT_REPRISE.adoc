= PROMPT_REPRISE — Codebase (Queens N1) — Session 104
:session: 104
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:sessions_precedente: 103 — Brainstorming Protocole opencode↔Gradle (cadrage EPIC SP + MEM-4)

== Contexte Hérité

- EPIC W TERMINÉ (7/7)
- EPIC X 4/7 — X-0✅ X-1✅ X-2✅ X-3✅
- EPIC Y 7/8 — Y-1✅ Y-2✅ Y-3✅ Y-4✅ Y-5✅ Y-6✅ Y-7✅ (Y-7 restant)
- EPIC SP cadré (BACKLOG) — Protocole opencode↔Gradle, 7 sous-EPICs SP-0→SP-6, ~27 pts
  - SP-5 ToolEventStream : events thinking/tool_call/tool_result en JSON-lines
  - SP-6 LiveContextInjector : injection contexte dans conversation active
- Full suite 330+/333 PASS
- publishToMavenLocal OK

== Mission Session 104 — EPIC X-4 + X-5 + X-6

*X-4* : `RollbackStrategy` — Intégrer les 4 stratégies dans la boucle d'exécution :
  - `STOP_ON_ERROR` — arrête tout (comportement actuel)
  - `REVERT_AND_CONTINUE` — git checkout des fichiers de l'étape, continue
  - `MARK_SKIPPED` — marque l'étape comme contournée, continue
  - `FALLBACK_HUMAN` — pause, demande input utilisateur
  Tests : chaque stratégie vérifiée sur état erreur.

*X-5* : Intégration EPIC W (TaskSchemaScanner) dans la boucle verify→adapt.
  Le LLM utilise le catalogue `list_tasks` (W-4) pour construire son plan multi-step
  avant d'exécuter. `ToolRegistry.listTasks()` intégré dans `buildReplanPrompt()`.

*X-6* : Tests Cucumber — Scénario "agent planifie, échoue, adapte, réussit"
  avec un projet test qui a un bug à la première compilation.
  Le LLM doit lire l'erreur, corriger, recompiler.
  Mock ToolRegistry + FakeLlmProvider pour isolation.

→ EPIC X TERMINÉ (7/7).

== Ordonnancement Sessions Suivantes

| Session | EPIC | Livrable
| 104 | X-4 + X-5 + X-6 | RollbackStrategy + intégration W + Cucumber → EPIC X TERMINÉ
| 105 | SP-1 | SessionProtocolTask — pont contrat N0 ↔ VibecodingTask
| 106 | SP-5 | ToolEventStream — events JSON-lines temps réel
| 107 | SP-6 | LiveContextInjector — injection contexte conversation active
| 108 | Z-0 + Z-1 | Autofocus cadrage + ZoomLevel
| 109 | Z-2 + Z-3 | ContextZoomer + ZoomPolicy
| 110 | Z-4 + Z-5 + Z-6 | Intégration VibecodingGraph → EPIC Z TERMINÉ
| 111 | 12 | Dilution Article de Blog (quick win)
