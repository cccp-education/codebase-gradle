= PROMPT_REPRISE — Codebase (Queens N1) — Session 131
:session: 131
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 130 — Koog 1.0.0 API : replacement forwardTo ✅

== Contexte Hérité

- koog-agents 1.0.0 déployé, imports `forwardTo` corrigés dans 5 fichiers.
- Build compile et tests compilent (0 erreur).
- `GovernanceContextLoader` créé session 129.
- Boucle vibecoding locale + pont EPIC Y en attente.

== Mission Session 131

Câbler `GovernanceContextLoader` dans `SessionProtocolServer` et `SessionProtocolTask` :
fallback auto quand aucun `--contextFile` n'est fourni.

== Méthodologie

Baby-step DDD/TDD/BDD : un fichier modifié à la fois, compilation + tests entre chaque étape.
