= Prompt de Reprise — Codebase (Queens N1) — Session 205
:session: 205
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 204 — PUB-0.9 TERMINÉ : codebase-plugin 0.0.9 publié Maven Central (POM sans codex-plugin, avec ocr-contracts:0.0.1). TDD PublishedPomCharacterizationTest + fix VersionCatalogTransitiveVersionsTest (échec préexistant). Travail en worktree isolé /tmp/opencode/cb204.

== Mission Session 205

[IMPORTANT]
Pré-requis ABSOLU : vérifier l'état de la zone sale parallèle. Le working
tree contenait 11 fichiers WIP non commités de la session Brooklyn
(revert imports `codebase.store.RagVectorStore` → legacy
`codebase.rag.VectorStore` + ~243 lignes schéma codex dans RagVectorStore.kt).
Si Brooklyn a commité sa migration CB-STORE-UNIFY, vérifier la baseline ;
sinon, travailler en worktree isolé (pattern S-203/S-204).

=== Option A — Suivi publication 0.0.9

* Vérifier propagation repo1 : `codebase-plugin/0.0.9/codebase-plugin-0.0.9.pom`
  (propagation ~20 min post-bundle S-204).
* Si propagé : signaler aux consommateurs (slider/capsule/planner consomment
  codebase via mavenLocal 0.0.8 ou contrainte BOM) — bump à la discrétion
  MEMPHIS (pratique canonique zéro bump BOM).

=== Option B — Accueil migration Brooklyn (si commitée)

* Si le WIP Brooklyn (CB-STORE-UNIFY) est committé sur codebase : audit de
  la migration (RagVectorStore vs legacy VectorStore), gate compileKotlin +
  tests ciblés `codebase.store.*`, synchronisation gouvernance Queens.
* Sinon : borough DORMANT — maintenance seule.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* codebase-plugin 0.0.9 = Maven Central (S-204). Dep N0 `ocr-contracts:0.0.1`.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* 41 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).
* Session concurrente possible : vérifier `git status`, mtimes récents,
  absence d'autre session active AVANT d'éditer.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.
