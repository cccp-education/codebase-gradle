= Prompt de Reprise — Codebase (Queens N1) — Session 147
:session: 147
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 146 — EPIC V-9.10 ChunkValidationReport livré (`ChunkValidationErrorType`, `lineStart`/`lineEnd`, `validationErrorsByType` JSON, 3 JUnit5 + 1 test fonctionnel + 3 Cucumber @epic_v_9_10 PASS, zéro régression V-9.x)

== Mission Session 147

1. Attaquer V-9.11 AgenticCompiler — transformer un chunk ontologisé en artefact exécutable (règle/procédure/contrainte).
2. Coder en baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber.
3. Préserver V-9.3, V-9.5, V-9.7, V-9.8, V-9.9, V-9.10 et `IngestGovernanceTaskTest` (zéro régression).
4. `./gradlew :codebase-plugin:compileKotlin` doit rester vert entre chaque fichier modifié.

== Méthodologie

Baby-step DDD/TDD/BDD : unit → fonctionnel → Cucumber. Un fichier modifié à la fois, compilation entre chaque étape. Pas de subagent, pas de parallélisation.
