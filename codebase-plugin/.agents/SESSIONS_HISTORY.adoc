= SESSIONS_HISTORY — Codebase Gradle
:date: 2026-08-25

|===
| Session | Date | EPIC | Résultat

| 204 | 2026-08-25 | PUB-0.9 | **PUB-0.9 TERMINÉ — codebase-plugin 0.0.9 publié Maven Central** (baby-step TDD strict RED → GREEN → REFACTOR, accord pilote obtenu). RED : `PublishedPomCharacterizationTest` créé (package `codebase.publishing`, 2 tests — parse `~/.m2/.../codebase-plugin-0.0.9.pom` : `ocr-contracts` présent / `<artifactId>codex-plugin</artifactId>` absent) → 2 FAILED (POM inexistant). GREEN : bump `version = "0.0.9"` (`build.gradle.kts:8`) + entrée catalogue `ocr-contracts = { module = "education.cccp:ocr-contracts", version = "0.0.1" }` + bascule `implementation(libs.ocr.contracts)` (littéral → catalogue, pattern contracts N0) + fix `VersionCatalogTransitiveVersionsTest` (échec préexistant à HEAD : `codex-plugin` retiré de `transitiveArtifacts` suite S-201 + résolution chemin corrigée vers le catalogue du plugin — le test lisait le catalogue racine consommateur, sans les artefacts education.cccp). `publishToMavenLocal` VERT, POM ~/.m2 validé, 4/4 tests publishing PASSED. Gate non-régression ciblé VERT (compileKotlin + CodebasePluginTest + codebase.store.*, 1m16s). Publication `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL 31s depuis worktree isolé `/tmp/opencode/cb204` (HEAD + 4 fichiers portés via git apply patch). ⚠️ Zone sale parallèle : 11 fichiers WIP Brooklyn non commités (revert imports RagVectorStore→legacy VectorStore + ~243 lignes méthodes schéma codex dans RagVectorStore.kt, mtimes 23h36-23h39) — laissés intacts, NE PAS TOUCHER sans audit. Option B S-203 RÉSOLUE : `codex-plugin:0.0.6` EST sur Central (repo1 vérifié direct, POM complet ocr-contracts 0.0.1 — constat S-203 périmé). 4 fichiers modifiés (1 créé + 3 modifiés), zéro secret. Rapport : `.agents/sessions/204-pub-009-central.adoc`.

| 203 | 2026-08-24 | Maintenance (DORMANT) | **Fix BenchmarkRunner vers socle RagVectorStore + interruption session concurrente.** Complétion WIP `BenchmarkRunner.kt` (reliquat S-199/200 non compilable : imports supprimés mais référencés, `RagVectorStore` sans import, `buildContext` cassé) suivant le pattern codex latest `CodexRetrieveTask` : construction `RagVectorStore()` + appel direct `searchBlocking(queryText, topK = 10)` + champs `chunkText`/`similarity`, suppression `EmbeddingPipeline`, signature `buildContext(vectorStore: RagVectorStore?, graphModel?)`. Gate `compileKotlin` VERT en worktree isolé (`git worktree add /tmp/opencode/cb203 HEAD` + copie du seul fichier — les erreurs live provenaient des WIP concurrents). ⚠️ Session opencode codex (Brooklyn) éditait codebase en parallèle (23:04–23:10, 8 fichiers : MultiChannelContextGraph, OcrIngestTask, VectorQueryService, 3 steps de test, RagVectorStore — migration legacy `codebase.rag.VectorStore` → `RagVectorStore` inachevée non compilable) → stop immédiat de toute édition, clôture conservatoire. Audit publications Central (repo1 vérifié direct) : `ocr-contracts:0.0.1` ✅ + `workspace-bom:0.0.26` ✅ ; **`codex-plugin:0.0.6` ❌ ABSENT** (dernier 0.0.5, claim Brooklyn S-104 non matérialisée sur Central) ; **POM `codebase-plugin:0.0.8` contient encore `codex-plugin:0.0.2` runtime** → inversion N1→N2 morte côté code mais pas côté Central → bump 0.0.9 prioritaire S-204 (TDD test POM publié + publishToMavenLocal + Central sur accord pilote). Zéro bump BOM, zéro secret. Rapport : `.agents/sessions/203-benchmark-ragvectorstore-fix-interruption.adoc`.

| 202 | 2026-08-24 | CDX-OCR-CONTRACTS | **US-4 TERMINÉ — EPIC CDX-OCR-CONTRACTS 5/5 US** (pure BDD, zéro prod modifié, baby-step BDD strict RED → GREEN → REFACTOR). `codebase_ocr_contracts.feature` 5 scénarios `@ocr-contracts` (fake AI engine AsciiDoc via port N0 `contracts.ocr.OcrResult`, miroir métadonnées requête→résultat language/sourceFormat/model, port stateless réutilisable 2 appels FIFO stub 2×, rejet provider logiciel tesseract → erreur "codex" sans output, Gemini+Ollama both failing → AI-only pointant vers codex collectOcr) + `OcrContractsCucumberRunner` (pattern S-082, 41e `CucumberTaskSpec("cucumberTestOcrContracts")`) + `OcrContractsWorld`/`StubVisionProvider` (fake AI engine FIFO + call count, pattern RecordingVisionProvider + FakeFineTuningLlm) + `OcrContractsSteps` (préfixés S-088). RED : 5 UndefinedStepException / 23 steps → GREEN : 5/5 PASSED. Gates : adapter unit 4/4 + OcrTaskAiOnlyBoundaryTest 2/2 + cucumberTestEpicOcr 29/29 VERT. Écart mission : « cache hit » non testable codebase-side (cache modèle exige clé+réseau ; cache hash côté codex) → scénario stateless équivalent. Publications Central en attente accord pilote (ocr-contracts 0.0.1, BOM 0.0.26, codex 0.0.6). Zéro bump BOM, zéro secret.

| 201 | 2026-08-24 | CDX-OCR-CONTRACTS / CDX-RAG-SOCLE | **US-3 TERMINÉ — RAG-4 FINAL, EPIC CDX-RAG-SOCLE 6/6 US** (cross-borough MEMPHIS+codex+codebase). `VisionOcrEngineAdapter` + test : imports `codex.ocr.*` → `contracts.ocr.*` (artefact N0 `ocr-contracts:0.0.1` créé S-104 MEMPHIS, typealiases codex S-104). Retrait `implementation(libs.codex.plugin)` + entrée catalog. **Inversion N1→N2 totalement morte** — flèche unique codex→codebase. Tables pgvector inchangées. Zéro bump BOM local, zéro secret. Reste EPIC CDX-OCR-CONTRACTS US-4 Cucumber BDD + publications Central.

| 200 | 2026-08-24 | CDX-RAG-SOCLE | **US-2 + US-4 partial + US-5 Cucumber BDD TERMINÉS** (3 US en 1 session, pattern S-113). US-2 bascule 6 call sites `CodexVectorStore` → `RagVectorStore` (API identique, pure mécanique, 6 fichiers). US-4 partielle : imports `codex.store.*` éliminés (RAG inversion dead), `libs.codex.plugin` conservé pour port OCR `VisionOcrEngineAdapter` (gate extraction `codex.ocr` N0 future EPIC CDX-OCR-CONTRACTS). US-5 Cucumber BDD `codebase_rag_socle.feature` 6 scénarios `@rag-socle` + `RagSocleCucumberRunner` (40e `CucumberTaskSpec`) + `RagSocleWorld`/`StubRagStore` fake + `RagVectorStore.open` pour testabilité. `check` VERT. EPIC CDX-RAG-SOCLE 4/6 US (reste US-3 cross-borough codex + US-4 complétion). Commit `3a95c96`. Zéro bump BOM, zéro secret.

| 199 | 2026-08-22 | CDX-OCR-BOUNDARY (cross-borough Brooklyn pilote) | **CDX-OCR-2 TERMINÉ côté Queens** (US-2/4, baby-step TDD strict RED → GREEN → REFACTOR — session codex S-099). `VisionOcrEngineAdapter` créé (`codebase.koog.llm.adapter`, implémente le port codex `codex.ocr.OcrEngine` fun interface, wrappe `VisionProvider` IA, bridge suspend→runBlocking, 4 unit RED→GREEN). Suppression `CodexOcrEngineAdapter.kt` (sens inverse de la boundary) + `OcrTask` IA-uniquement : imports codex retirés, propriété `tesseractOcrProvider` supprimée, provider "tesseract" et fallback software gemini+ollama → `GradleException(AI_ONLY_ERROR_MESSAGE)` pointant vers codex `collectOcr`. Migration tests : `OcrTaskAiOnlyBoundaryTest` 2 unit remplace `OcrTaskTesseractFallbackTest`, `OcrSteps` capture d'erreur, `ocr.feature` 2 scénarios boundary réécrits PASSED. Gate : cucumberTestEpicOcr 29 PASSED, `check` VERT 41m26s ; 3 échecs préexistants au baseline vérifiés par stash (`VersionCatalogTransitiveVersionsTest`, Z-6 Autofocus flaky). Tesseract reste intégralement dans codex. Zéro bump BOM, zéro secret.

| 197 | 2026-08-19 | FT-PIPELINE | **US-5 Cucumber BDD `codebase_finetuning.feature` TERMINÉ** (P0vision, 2 pts, baby-step BDD strict RED → GREEN → REFACTOR). 4 scénarios `@finetuning` (pipeline complet dataset→GGUF→manifest, cycle itératif convergence après ajustement corpus ratio, fallback degraded Ollama registry indisponible 503, validation seuil dès la 1re itération) dans `codebase_finetuning.feature` (36 lignes) + `FineTuningCucumberRunner.kt` (pattern S-082, filtre `@finetuning and not @integration`) + `FineTuningWorld.kt` (`StubRegistryClient` in-memory `OllamaRegistryClient` + state shared PicoContainer) + `FineTuningSteps.kt` (16 step defs préfixés "finetuning" pattern S-088 + `FakeFineTuningLlm` FIFO queues propose/validate + `StubPromptBuilder` deterministic — zéro appel Gradle task, zéro I/O réseau, pure BDD domaine). `build.gradle.kts` modifié : `CucumberTaskSpec("cucumberTestFineTuning", ...)` 39e câblé. 4/4 scénarios PASSED 9s (17 steps). Zéro fichier prod modifié — pure BDD pattern VibeHardening2 S-192. Gate `codebase.finetuning.*` + `CodebasePluginTest` non-régression VERT 0 FAIL. Domaine `codebase.finetuning` clos BDD. EPIC FT-PIPELINE 5/7 US (reste US-6 cross-borough training-gradle). Zéro bump BOM, zéro secret, zéro régression. Rapport : `.agents/sessions/197-ft-pipeline-us5-cucumber-bdd.adoc`.
| 196 | 2026-08-19 | FT-PIPELINE | **US-4 TERMINÉ — tâches Gradle + extension DSL** (P0 vision, 4 pts, baby-step TDD strict RED → GREEN → REFACTOR, pattern S-113 multi-US 1 session). 4 fichiers prod créés : `CodebaseFineTuningExtension.kt` (abstract class, 10 propriétés DSL `baseModel`/`dataset`/`outputModelName`/`corpusRatio`/`maxIterations`/`epochs`/`learningRate`/`batchSize`/`corpusGlobs`/`validationThreshold` + helpers `toConfig()`/`toRequest()`, pattern `CodebaseExpertExtension`, defaults backward compat mirrors `FineTuningConfig`) + `PrepareFineTuningDatasetTask.kt` (groupe `finetuning`, `@DisableCachingByDefault`, `@Input` baseModel/dataset/corpusGlobs/outputModelName/corpusRatio + `@OutputFile`, `executePrepare()` assemble Modelfile section `FROM baseModel` + corpus ratio + Dataset section globs, `corpusGlobs` fallback when dataset empty, 3 invariants GradleException) + `FineTuneExpertTask.kt` (groupe `finetuning`, `dependsOn("prepareFineTuningDataset")`, `@Internal pipeline: FineTuningPipeline?` injectable — défaut `OllamaFineTunerAdapter(OllamaHttpRegistryClient(baseUrl), ggufDir)`, `@OutputFile outputReport` + `@OutputDirectory ggufOutputDir`, `executeFineTune()` délègue à `pipeline.fineTune(request)` + rapport texte SUCCESS/FAILURE avec baseModel/dataset/score/reason) + `PublishExpertToOllamaTask.kt` (groupe `finetuning`, `dependsOn("fineTuneExpert")`, `@Internal expertRegistry: ExpertRegistry` injectable, `executePublish()` enregistre `ExpertRegistration` dans `ExpertRegistry` (domaine `codebase.koog.expert` existant — boucle fabrication→exposition) + délègue manifest JSON à `ExpertExposureTask` via `findByName` + fallback `register` pour isolation test). `CodebasePlugin.kt` câblé (+87 lignes) : extension `fineTuning` créée + 3 tâches enregistrées + 13 props CLI `-Pcodebase.finetuning.*` (baseModel/dataset/outputModelName/corpusRatio/maxIterations/epochs/learningRate/batchSize/corpusGlobs/validationThreshold/domainName/domainLabel/ollamaBaseUrl, pattern `ocrDocument`/`ingestGovernance`). 5 fichiers test créés : `CodebaseFineTuningExtensionTest` (10 unit) + `PrepareFineTuningDatasetTaskTest` (11 unit) + `FineTuneExpertTaskTest` (12 unit — default Ollama adapter degraded → FAILURE validé) + `PublishExpertToOllamaTaskTest` (11 unit) + `FineTuningFunctionalTest` (3 functional). `CodebasePluginTest.kt` mis à jour : 8→11 tâches attendues + 5 nouveaux tests (3 groupes `finetuning`, 2 dependsOn via `taskDependencies.getDependencies().map { it.name }` — `dependsOn` String lazily-resolved non visible via `TaskReference` cast dans `ProjectBuilder`, extension registered). Bug `DuplicateTaskException` `exposeExperts` (CodebasePlugin déjà enregistré) → fix `findByName` + fallback `register` (idempotent). Bug `contains("0.10")` fragile (`Double.toString()` supprime zéro final `0.1`) → `contains("corpus ratio: 0.1")`. Gate `codebase.finetuning.*` + `CodebasePluginTest` VERT **140 tests 0 FAIL** (8 config + 19 merger + 11 pipeline + 5 adapter + 18 state + 17 graph + 10 extension + 11 prepare + 12 fineTune + 11 publish + 3 functional + 15 plugin). `compileKotlin` + `validatePlugins` VERT. Domaine `codebase.finetuning` 13→17 fichiers prod. Zéro bump BOM (version 0.0.8 inchangée), zéro secret, zéro régression. EPIC FT-PIPELINE 4/7 US. Rapport : `.agents/sessions/196-ft-pipeline-us4.adoc`.
| 195 | 2026-08-19 | FT-PIPELINE | **US-3 `FineTuningGraph` TERMINÉ — koog itératif 3 nœuds propose/train/validate** (P0 vision, 4 pts, baby-step TDD strict RED → GREEN → REFACTOR). 5 fichiers prod créés : `FineTuningStage.kt` enum 6 stages (INITIALIZED/PROPOSED/TRAINED/VALIDATED/CONVERGED/FAILED, pattern `slider.pipeline.DeckStage`) + `FineTuningState.kt` data class immutable 11 champs (request, config, proposal, trainResult, validationScore, validationThreshold, iteration, maxIterations, error, stage) + 10 invariants croisés `init` (pattern `slider.pipeline.DeckState`) + `FineTuningPromptBuilder.kt` port 2 méthodes (buildProposePrompt, buildValidatePrompt, pattern `DeckPromptBuilder`) + `FineTuningLlm.kt` port 2 méthodes sync (propose, validate, pattern `DeckLlm`) + `FineTuningGraph.kt` graphe koog `AIAgentGraphStrategy<FineTuningState, FineTuningState>` 3 nœuds propose/train/validate + 7 edges conditionnels (train→validate on Success, train→finish on Failure, validate→finish on convergence, validate→propose on retry budget, validate→finish on max iterations exhausted) + `execute` séquentielle pattern `KoogAugmentedContextGraph.execute` + `asMermaidDiagram()` + `adjustedCorpusRatio()` parser (Regex premier Double `[0,1]` du proposal, fallback ratio request). 2 fichiers test créés : `FineTuningStateTest.kt` 18 unit (default state, PROPOSED/TRAINED/CONVERGED/FAILED invariants, ranges, copy preservation) + `FineTuningGraphTest.kt` 17 unit (happy path convergence, proposal populated, loop back to propose, MaxIterationsExhausted, train Failure, propose/train/validate LLM throws, prompt builder state forwarded, validate skipped on Failure, LLM prompts forwarded, asMermaidDiagram non-blank, request+config preserved, corpus ratio parsed/fallback). Bug loop invariant découvert RED : re-entry `proposeNode` cassait `trainResult non-null requires stage >= TRAINED` car `trainResult`/`proposal`/`validationScore` restaient du tour précédent — fix `state.copy(proposal="", trainResult=null, validationScore=0.0, stage=INITIALIZED)` avant re-proposer. Bug backtick Kotlin `0..1` illegal chars → renommé `0 to 1 range`. Gate `codebase.finetuning.*` 78/78 PASSED (+35 nets : 17 graph + 18 state), 0 FAIL. `compileKotlin` + `compileTestKotlin` VERT. Domaine `codebase.finetuning` enrichi (7 prod + 5 test). Zéro bump BOM, zéro secret, zéro régression. EPIC FT-PIPELINE 3/7 US. Rapport : `.agents/sessions/195-ft-pipeline-us3-finetuning-graph.adoc`.
| 194 | 2026-08-19 | FT-PIPELINE | **US-2 TERMINÉ — port `FineTuningPipeline` + `OllamaFineTunerAdapter`** (P0 vision, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR, pattern S-113 multi-US 1 session). Domaine `codebase.finetuning` (7ème) enrichi : `FineTuningRequest.kt` data class 4 champs + invariants × 4 + `FineTuningResult.kt` sealed Success/Failure + factory + `FineTuningPipeline.kt` fun interface sync (pattern `TranscriptLlmEnhancer`) + `OllamaRegistryClient.kt` port interne createModel/pushModel + `RegistryResponse` sealed Ok/Fail + `OllamaHttpRegistryClient.kt` impl HTTP JDK 24 (`POST /api/create` + `/api/push`, pattern `OllamaOcrProvider`) + `OllamaFineTunerAdapter.kt` adapter pipeline Modelfile→create→push→GGUF marker + fallback degraded economy of ink (Failure préserve dataset original) + `FakeFineTuner.kt` test (FIFO + defaultResult + capture, pattern `FakeLlmTranslator`). 43/43 unit PASSED (8 config + 19 merger + 11 pipeline + 5 adapter). 9 fichiers créés (6 prod + 1 fake + 2 test). `compileKotlin` VERT, zéro régression, zéro secret, zéro bump BOM. EPIC FT-PIPELINE 2/7 US. Rapport : `.agents/sessions/194-ft-pipeline-us2.adoc`.
| 193 | 2026-08-18 | FT-PIPELINE | **US-1 TERMINÉ — domaine `codebase.finetuning` (7ème) ouvert** (P0 vision, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR). `FineTuningConfig.kt` data class 5 champs (`epochs=3`, `learningRate=2e-4`, `batchSize=4`, `corpusGlobs=[]`, `continualPreTrainingRatio=0.10`) + invariants `init` × 4. `FineTuningConfigMerger.kt` object pur (merge 4 sources + loadFromGradleProperties + loadFromEnvironment + helpers). 27/27 unit PASSED. 4 fichiers créés. `compileKotlin` VERT, zéro régression, zéro secret, zéro bump BOM. 1/7 US FT-PIPELINE. Rapport : `.agents/sessions/193-ft-pipeline-us1.adoc`.
| 192 | 2026-08-18 | VIBE-HARDENING-2 | **EPIC VIBE-HARDENING-2 TERMINÉ 8/8 US** (pattern S-113, 4 US en 1 session). US-5 C3 cleanup doc INSERT-X1..X6 — 32 lignes KDoc stale supprimées `VibecodingGraph.kt:53-85` (section "EPIC X Audit" + 6 blocs `[INSERT-X1]` à `[INSERT-X6]`, insertions faites en EPIC X S-101→S-105), `VibecodingPlan` toujours utilisé (import L13), zéro test impacté. US-6 C7 coerceAtLeast(0) — bug `buildPromptForIteration` L430 produisait `"Plan remaining tasks: -2"` quand `executedTasks.size > total plan tasks` (retry, tâche hors plan) ; baby-step TDD strict RED (2 unit `VibecodingGraphTest.buildPromptForIteration*` — le test négatif FAIL car la ligne contient `-`, test positif PASSED par accident car 2 est positif ; `buildPromptForIteration` rendu `internal` pour testabilité, pattern `buildReplanPrompt`) → GREEN (`val totalTasks = plan.epics.sumOf { it.userStories.sumOf { s -> s.tasks.size } }; val remaining = (totalTasks - state.executedTasks.size).coerceAtLeast(0)`) → REFACTOR (gate 24 VibecodingGraph +2 + 13 StepVerifier + 26 TaskResultVerifier = 63 tests 0 FAIL). US-7 C6 SessionProtocolEngine extraction — `SessionProtocolEngine.kt` créé (105 lignes) shared engine qui encapsule `executeVibecoding(promptText, workspaceRootPath, maxActions, sessionId, agentContext, model)` + `resolveAgentContext(context, workspaceRootPath, onContextResolved)` + `buildOutput(state)` ; `SessionProtocolServer.executePrompt` délégué (69→32 lignes, -37) + `SessionProtocolTask.executeVibecoding` délégué (329→280 lignes, -49) + `buildOutput` privé dupliqué supprimé des deux + imports inutilisés nettoyés (TokenTracker, TokenUsage, ToolCallRecord, SessionStatus dans Server ; TokenTracker, TokenUsage, ToolCallRecord, VibecodingState dans Task) ; 9 unit `SessionProtocolEngineTest` dédiés (executeVibecoding sessionId/ERROR/toolCalls/output format, resolveAgentContext provided/fallback, buildOutput error/plan/blank) + 4 classes SessionProtocol non-régression (Task + Server + E2E + Lifecycle) VERT. US-8 Cucumber BDD `codebase_vibe_hardening_2.feature` `@vibe-hardening-2` 4 scénarios (write_file rejects content exceeding MAX_WRITE_CHARS, TaskResultVerifier matches custom expectedOutput against stdout, ExecGradleTool accepts flag with separate value argument, Plan remaining tasks coerced to zero when executed exceeds plan total) + `VibeHardening2World` (PicoContainer) + `VibeHardening2Steps` (14 step defs préfixés "vibe hardening 2", bug S-088 glue `codebase.scenarios` partagée évité — step texts uniques) + `VibeHardening2CucumberRunner` (pattern S-082, filter `@vibe-hardening-2 and not @integration`, glue `codebase.scenarios`, plugin pretty/html/json reports `cucumber-vibe-hardening-2`) + `CucumberTaskSpec("cucumberTestVibeHardening2")` 38e cucumber dédié câblé `build.gradle.kts` après `cucumberTestVibeHardening`. 4/4 scénarios PASSED. Bug `TaskResultVerifier.verify` est méthode d'instance (classe, pas object) — corrigé dans steps (`TaskResultVerifier().verify(...)`). Bug `AgentContext.eagerRules` est `String` pas `List<String>` (contrat N0) — corrigé dans test. 11 fichiers (5 modifiés + 6 créés). `compileKotlin` + `compileTestKotlin` VERT. Zéro régression (gate SessionProtocol + VibecodingGraph + planning VERT). Zéro bump BOM (EPIC local à codebase-plugin). Zéro secret, zéro token. Rapport : `.agents/sessions/192-vibe-hardening-2-cloture.adoc`.
| 191 | 2026-08-18 | VIBE-HARDENING-2 | **US-4 C1 TaskResultVerifier refonte TERMINÉ** (P1, 4 pts, baby-step TDD strict RED → GREEN → REFACTOR). `TaskResultVerifier.kt` : surcharge `verify(stdout, stderr, expectedOutput)` + branche `isCustomExpected` (check `build failed`/`compilation error`/`hasActualFailure` → FAILED ; sinon `stdout.contains(expected)` case insensitive → SUCCESS sinon FAILED) + branche default heuristique fallback conservée + `hasActualFailure` raffiné (`FAILED_PATTERN = \\bfailed\\b` findAll → any match non précédé de `0 `/`no ` — exclut `0 failed tests` faux positif). Bug subtil lowercase : `DEFAULT_EXPECTED_OUTPUT_LOWER` const séparée (`expected` est lowercased mais `DEFAULT_EXPECTED_OUTPUT` non lowercased → `isCustomExpected` toujours true → tous les tests default cassés). `StepVerifier.kt` : `verify(state.lastToolResult, "")` → `verify(state.lastToolResult, "", step.expectedOutput)` — câble contrat N0 `expectedOutput` du `VibecodingStep` (PLN-VERIFY S-064, GradleTask N0 S-064). `TaskResultVerifierTest.kt` : 17→26 tests (+9 nouveaux expectedOutput + failed raffiné). `StepVerifierTest.kt` : 11→13 tests (+2 nouveaux custom expectedOutput matching/not matching, 2 adaptés `BLOCKED`/`UNKNOWN` → `DEFAULT_EXPECTED_OUTPUT` car expectedOutput custom est maintenant comparé). 26 TaskResultVerifier + 13 StepVerifier + 22 VibecodingGraph + 97 gate (`codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest`) = 158 tests VERT 0 FAIL. `compileKotlin` VERT. Zéro bump BOM, zéro secret, zéro régression. 4/8 US VIBE-HARDENING-2. Rapport : `.agents/sessions/191-vibe-hardening-2-us4-task-result-verifier.adoc`.
| 190 | 2026-08-18 | VIBE-HARDENING-2 | **US-3 C4 GradleTaskArgsMapper flags cassés TERMINÉ** (P2, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR). `ExecGradleTool.kt` : `GRADLE_ARG_ALLOWLIST` étendu `--\w+` → `--\w+(?:=\S+)?` (flag seul ou `--flag=value`) + `isBareLongFlag(token)` helper (détecte flag sans `=` précédant une valeur séparée) + `validateGradleTask` refondu : un token d'arg qui ne matche pas l'allowlist est accepté si le token précédent est un `bareLongFlag`, à condition de ne pas commencer par `-` et de ne pas contenir `;&|` (defense in depth préservée). `ExecGradleToolTest.kt` : +5 tests (3 positifs pair flag+value/flag=value/mixed + 2 négatifs bare value/standalone token), 15→20 tests. **Fail préexistant C4 résolu** : `GradleTaskArgsMapperTest > execute structured gradle task via handler()` PASSED (subprocess Gradle `test --tests FakeTestClassName` ne lance plus `SecurityException`). Description `ExecGradleTool` mise à jour. Gate combinée `codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 110 tests 0 FAIL (20 ExecGradleTool + 17 ExecShellTool + 60 discovery + 13 ToolRegistry). `compileKotlin` VERT. Zéro bump BOM, zéro secret, zéro régression. 3/8 US VIBE-HARDENING-2. Rapport : `.agents/sessions/190-vibe-hardening-2-us3-gradle-task-args-mapper.adoc`.
| 189 | 2026-08-17 | VIBE-HARDENING-2 | **US-1 + US-2 TERMINÉS** (pattern S-113 multi-US 1 session). US-1 S2 Allowlist TaskDiscoveryRegistrar : `TASK_ALLOWLIST` (2 regex bare tasks + project:task) + `TASK_DENYLIST` (6 regex publishToMavenCentral/publishAggregation/clean/purge/wrapper/bootstrap) + `validateTaskName` companion (allowlist deny-by-default + deny-list second ride, pattern `ExecGradleTool.validateGradleTask`) + `registerAll` filtre via validateTaskName (skip denied). 18 unit allowlist + 6 tests existants réécrits sécuritaires (vibecode/qualityGate devenus denied — tasks métier plugin ne sont plus auto-enregistrées, sécuritaire). US-2 S3 Garde taille write_file/edit_file : `MAX_WRITE_CHARS = 1_000_000` const companion + 3 checks (write_file content, edit_file newString, edit_file resulting updated) → `IllegalArgumentException` avant writeText. Pattern `read_file MAX_OUTPUT_CHARS` mirror. +5 unit. Gate `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 105 tests (1 fail préexistant C4 US-3). 2/8 US VIBE-HARDENING-2. Zéro bump BOM, zéro secret, zéro régression. Rapport : `.agents/sessions/189-vibe-hardening-2-us1-us2.adoc`.

| 188 | 2026-08-17 | FT-PIPELINE + VIBE-HARDENING-2 + cross-borough | **Maintenance + cross-borough audit + cadrage 2 EPICs + propagation 5 boroughs** (gouvernance pure + bump catalogues consommateurs). Phase 1 maintenance : INDEX/BACKLOG/SESSIONS_HISTORY/PROMPT_REPRISE synchronisés post-S-187. Phase 2 cross-borough audit : 6 consommateurs audités. Phase 3 cadrage EPIC VIBE-HARDENING-2 : 7 constats P2/P3 (S2/S3/C1/C3/C4/C6/C7), 12 décisions archi, 8 US ~21 pts, `CADRAGE_EPIC_VIBE_HARDENING_2.adoc`. Phase 4 propagation : 5 boroughs bumpés codebase 0.0.8 (planner `d724e5e`, slider `f2612e0`, capsule `e8f1fec`, codex `e0ff21d`, training `3073b52`) — 4 compileKotlin VERT, codex fail préexistant hors scope. Phase 5 cadrage EPIC FT-PIPELINE : audit alignement vision globale (WORKSPACE_AS_PRODUCT.adoc:642-658 — codebase propriétaire pipeline fine-tuning), récupération conception notebook-gradle (vestige LA HAVANA), 16 décisions archi, 7 US ~18 pts, domaine `codebase.finetuning` (7ème), boucle fabrication→exposition avec domaine `codebase.koog.expert` existant (13 fichiers), `CADRAGE_EPIC_FT_PIPELINE.adoc`. 3 bugs découverts : MEMPHIS BOM 0.0.19 slider 0.0.8 non publié, ABIDJAN codex kotlin-stdlib, capsule codebase-contracts 0.1.0 local-only. Zéro fichier production codebase modifié. Rapport : `.agents/sessions/188-maintenance-cross-borough-vibe-hardening-2-cadrage.adoc`.
| 187 | 2026-08-17 | VIBE-HARDENING | **US-3+US-4+US-5 TERMINÉ — EPIC VIBE-HARDENING CLOS 6/6 US** (pattern S-113, 3 US en 1 session). US-3 C5 Timeout LLM : `StepVerifier.kt` constructor param `llmTimeoutMs: Long = DEFAULT_LLM_TIMEOUT_MS (30_000L)` + wrap `withTimeout(llmTimeoutMs)` site replan L44 (catch `TimeoutCancellationException`) ; `VibecodingGraph.kt` constructor param déplacé AVANT `stepVerifier` (default param propagation) + wrap `withTimeout` 2 sites (callLLMNode L413 terminal `withError("LLMTimeout")`, error recovery replan L288 récupérable `clearError().nextIteration().copy(lastToolResult="LLMTimeout...")`). US-4 C2 Retry single counter : supprimé `retryCount = state.retryCount + 1` 2 sites `executePlanTasks` (L587/L604) + 3 `incrementRetry()` branches replan → 1 `incrementRetry()` début bloc error recovery. US-5 Cucumber BDD : `codebase_vibe_hardening.feature` 4 scénarios `@vibe-hardening` + `VibeHardeningWorld` + `VibeHardeningSteps` (14 step defs préfixés "vibe hardening", bug S-088) + `VibeHardeningCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestVibeHardening")` 37e. 4/4 PASSED 47s. Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier VERT 64 tests 0 FAIL. Zéro bump BOM, zéro secret. Rapport : `.agents/sessions/187-vibe-hardening-us3-us4-us5-clos.adoc`.
| 186 | 2026-08-17 | VIBE-HARDENING | **US-2 ExecGradleTool allowlist deny-by-default TERMINÉ** (baby-step TDD strict, pattern US-1 shell). 4 regex `GRADLE_BLACKLIST` (bypassables) remplacées par `GRADLE_TASK_ALLOWLIST` (2 patterns bare tasks + project:task) + `GRADLE_ARG_ALLOWLIST` (2 patterns -Pprop + --flag) + `GRADLE_DENYLIST` (8 patterns second ride : clean build, --refresh-dependencies, rm, delete, sudo, curl, semicolon, pipe). `validateGradleTask` split tokens + allowlist par position + deny-list second ride. `ExecGradleToolTest` 6→17 tests (15/15 PASSED, 2 @integration exclus). Gate `codebase.koog.tools.*` VERT 32 tests 0 FAIL (17 shell + 15 gradle). `ToolRegistryTest` 8/8 non-régression. Zéro bump BOM. Rapport : `.agents/sessions/186-vibe-hardening-us2-gradle-allowlist.adoc`.
| 185 | 2026-08-17 | VIBE-HARDENING | **US-1 ExecShellTool allowlist deny-by-default TERMINÉ** (baby-step TDD strict RED → GREEN → REFACTOR). `BASH_BLACKLIST` (10 regex bypassables `ExecShellTool.kt:10-21`) remplacé par `SHELL_ALLOWLIST` (10 patterns : git read, rg, ls, find ., mkdir /tmp, echo, cat, head/tail, ./gradlew, pwd) + `SHELL_DENYLIST` second ride (10 patterns : rm -[rR], sudo, curl, wget, chmod 0?777, pipe sh/bash, /etc/, /dev/, redirect /). `validateCommand` : allowlist passe d'abord (deny-by-default), deny-list en second ride (defense in depth). Messages "denied" au lieu de "blacklisted". `ExecShellToolTest` 11→17 tests (6 nouveaux : `rm -Rf` uppercase, `chmod 0777` zero-prefixed, `curl localhost` rejeté allowlist, `unknown command` deny-by-default, `git push` subcommand non-allowlistée, `redirect to root` ; 1 supprimé : `sleep 5` timeout — non-allowlisté rejeté avant timeout process ; 1 adapté : `exit 42` → `git show deadbeef` allowed non-zero exit). Gate `codebase.koog.tools.*` VERT 21 tests 0 FAIL (17 shell + 4 gradle non-régression US-2). `ToolRegistryTest` 8/8 PASSED (consommateur non régressé). 2 fichiers modifiés. Zéro bump BOM. Zéro secret. Rapport : `.agents/sessions/185-vibe-hardening-us1-shell-allowlist.adoc`.
| 184 | 2026-08-17 | VIBE-HARDENING | **US-0 Cadrage EPIC VIBE-HARDENING (gouvernance pure, zéro code)**. 3 constats P1 audités (S1 blacklists bypassables `ExecShellTool.kt:10-21` 10 regex + `ExecGradleTool.kt:10-15` 4 regex, C2 retryCount double-compté `VibecodingGraph.kt:587/604` + bloc error `:291/298/302`, C5 3 sites `runBlocking { llmProvider.call(...) }` sans timeout `:413/:288/StepVerifier:44`). 7 rappels P2/P3 (S2/S3/C1/C3/C4/C6/C7). 10 décisions architecturales : allowlist deny-by-default 10 patterns shell + 8 gradle + deny-list second ride, domaine `contracts.vibecoding.tools` conservé, un seul compteur retry via `withRecoverableError` (déjà correct L46-47), `withTimeout(llmTimeoutMs=30_000L)` configurable catch `TimeoutCancellationException`, backward compat préservée, baby-step TDD strict, zéro bump BOM, Cucumber BDD `@vibe-hardening` US-5 runner dédié, pas de nouvelle tâche Gradle, P2/P3 reportés EPIC VIBE-HARDENING-2. 6 US ~12 pts (US-0 ✅, US-1→5 À FAIRE). Document `.agents/CADRAGE_EPIC_VIBE_HARDENING.adoc`. Pattern S-057/S-063. `compileKotlin` VERT pré-cadrage. Working tree propre.
| 183 | 2026-08-16 | PUB | **EPIC PUB TERMINÉ 3/3 US — codebase-plugin 0.0.8 publié Maven Central**. US-0.0.8-1 : POM `withType<MavenPublication>` (name/description) + signing `useGpgCmd` (conventions-gradle publishing 0.0.2 ligne 112) + release deps vérifiées (zéro SNAPSHOT, 12 artefacts education.cccp en release Central) + nmcpSettings AUTOMATIC (ossrhUsername/Password ~/.gradle/gradle.properties). US-0.0.8-2 : `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL 28s — bundle `4ae55e92-a116-4a25-8edf-b1f7cc9a56d4` AUTOMATIC, HTTP 200 repo1 `codebase-plugin-0.0.8.pom` après ~20 min propagation (404 à 90s/5min — ne pas conclure prématurément). US-0.0.8-3 : zéro tag local/origin (ls-remote vide) → cleanup néant ; BOM non bumpé (constraint codebase 0.0.5 dans BOM Central 0.0.18 — bump à la discrétion MEMPHIS, pratique canonique zéro bump BOM S073/S179/S182). Zéro fichier source modifié — publication pure. Rapport : `.agents/sessions/183-epic-pub-0.0.8-central.adoc`.
| 182 | 2026-08-16 | SUBGRAPH | **US-3 + US-4 TERMINÉS — EPIC SUBGRAPH CLÔTURÉ 4/4**. US-3 : `GovernanceContextLoader(graphFile: File? = null, lensConfig: LensConfig = LensConfig())` — `graphRelations = graphFile?.let { GraphifyContextProvider(it, lensConfig).provide() } ?: ""` (fallbacks conservés) ; `MultiChannelContextGraph(graphFile: File? = null)` — `collectGraphifyNode` + `assembleNode` consomment `graphFile ?: rootDir.resolve("office/graph.json")`, canal `ContextChannel.Resource` alimenté depuis le sous-graphe réel (tronqué au `resourceTokens`) ; câblage `SessionProtocolTask.loadGovernanceContext()` + `SessionProtocolServer.resolveAgentContext()` passent `root.resolve("office/graph.json")` → `LiveContextInjector` effectif. 6 unit (4 GovernanceContextLoaderTest + 2 MultiChannelContextGraphTest budget resource 0.10). TDD strict RED (compileTestKotlin `No parameter with name 'graphFile'` 3 sites) → GREEN → REFACTOR. US-4 : `src/test/features/codebase_subgraph.feature` 4 scénarios @subgraph (Graphify channel réel, Resource channel réel, graphRelations peuplé, fallback fichier manquant) + `SubgraphWorld` (picocontainer, graph.json synthétique @TempDir) + `SubgraphSteps` (14 step defs) + `SubgraphCucumberRunner` (pattern EpicSld8LlmServiceCucumberRunner) + `CucumberTaskSpec("cucumberTestSubgraph")` câblé build.gradle.kts. RED : UndefinedStepException 4 scénarios + `CucumberExpressionException createAlternativeMayNotBeEmpty` (slash `/` final dans l'expression → alternation vide — corrigé en retirant `in office/`/`office/graph.json` des expressions). GREEN : 4/4 PASSED 21s, zéro production modifié — pure BDD. Gates ciblés VERT : 4 classes 0 FAIL (~4min 25s) + `codebase.graph.*` 37/37 + SessionProtocol 0 FAIL. Glob `codebase.koog.*` timeout 420s×2 (pratique canonique gates ciblés S175/S179/S180). Zéro bump BOM. Working tree bump 0.0.8 + repo `GradlePluginPortal` non commités (diff pré-session, EPIC PUB S181). Rapport : `.agents/sessions/182-subgraph-us3-us4.adoc`.
| 181 | 2026-08-15 | PUB | **Audit version + EPIC PUB (gouvernance pure, zéro code)** — delta codebase-plugin 0.0.7 vs Central 0.0.7 (bundle e08acb47 S-073) + commits post-publication non propagés (`f840b93` SUBGRAPH US-1+US-2). Décision : bump 0.0.7→0.0.8 reporté fin d'EPIC SUBGRAPH (S179) → cadré EPIC PUB dédié (3 US PUB-0.0.8-1/2/3) ajouté au BACKLOG. Zéro code modifié — gouvernance pure pattern bakery S199.
| 180 | 2026-08-15 | SUBGRAPH | **US-2 TERMINÉ — canal Graphify alimenté par le sous-graphe réel** (faux positif stats regex éliminé). US-2a `GraphContextRenderer` (object pur, format texte structuré ligne par ligne compatible troncature : header `[Graphify] subgraph: N nodes, M edges, C communities` + ligne par nœud/arête/communauté, 6 unit). US-2b `GraphifyContextProvider` (File+LensConfig injectables, pipeline load→extract→render, fallbacks conservés contrat backward-compat `"[Graphify] graph.json non trouve dans office/"` + `"illisible:"`, 5 unit — fixture corrigée `.kt`→`.adoc` car défaut `fileExtensions=["adoc","md","html"]`). US-2c câblage `CompositeContextBuilder` (param `graphFile: File? = null` + `graphifyProvider` constructeur, `loadGraphifyStats` regex supprimé) + `MultiChannelContextGraph.collectGraphifyNode` (provider `rootDir/office/graph.json`, regex supprimé). 3 functional `CompositeContextBuilderGraphifyTest` (URL jdbc bogus localhost:1 → fallback RAG hors docker) + 1 nouveau test `MultiChannelContextGraphTest` graphify (graph.json réel @TempDir, 7 total). Baby-step TDD strict RED → GREEN → REFACTOR. Gate ciblé VERT 24s 0 FAIL, `codebase.graph.*` 37 tests 0 FAIL. Suite complète `codebase.koog.*` interminable (2 timeouts 420s/720s — pratique canonique S175/S179 gates ciblés). ⚠️ Incident environnemental (non lié US-2) : racine `codebase-gradle` ne configure plus (`:help` FAILED — strict `kotlin-stdlib:2.3.21` embedded Gradle 9.6.1 vs `workspace-bom:0.0.4` requires 2.3.20 sur buildscript classpath via plugin publié `0.0.3`→`codebase-plugin:0.0.5` mavenLocal, artefacts inchangés depuis 08-11/06-26) — validation via build autonome `codebase-plugin/` (settings.gradle.kts propre) ✅
| 179 | 2026-08-15 | SUBGRAPH | **US-1 TERMINÉ — domaine `codebase.graph` ouvert (portage bakery → N1)**. Dépendance `education.cccp:graphify-plugin:0.0.2` (catalog + build.gradle.kts implementation, pattern bakery). `LensConfig` (scope/communities/nodeTypes/edgeTypes/maxDepth/fileExtensions + enum LensScope SUBGRAPH/FULL/SEMANTIC_ONLY, defaults identiques bakery ; DSL Gradle Action + rules/rag LENS-2 NON portés — YAGNI) + `SubgraphExtractor` (loadGraph/extract/extractFromPath/expandBfs + `SiteSubgraph` : nodeCount/edgeCount/communityCount/nodeIds/communityIds/nodesByCommunity/edgesByType/nodesInCommunity/edgesOfType/neighbors). 26 unit portés de bakery `SubgraphExtractorTest` (GraphModel synthétique, package `com.cheroliv.graphify.model` — WORKAROUND relocalisé confirmé). Baby-step TDD strict RED (compileTestKotlin FAILED `Unresolved reference`) → GREEN (BUILD SUCCESSFUL 13s) → REFACTOR. Gate non-régression VERT : `--tests codebase.graph.* codebase.koog.* contracts.vibecoding.*` → 912 tests / 105 classes / 0 FAIL (suite complète non relancée — Cucumber, pratique canonique S175). Zéro bump BOM. Décision : bump 0.0.7→0.0.8 + publication Central reportés en fin d'EPIC (après US-2/3/4) ✅
| 178 | 2026-08-15 | SUBGRAPH | Cadrage EPIC SUBGRAPH (gouvernance pure, zéro code). 4 constats audités (stats regex `loadGraphifyStats` CompositeContextBuilder.kt:138 + MultiChannelContextGraph.kt:130, `graphRelations` hardcodé `""` GovernanceContextLoader.kt:68, `ContextChannel.Resource` vide, zéro dépendance graphify-plugin). Precedent bakery `SubgraphExtractor`+`LensConfig`+`BfsProximityCache` (graphify-plugin 0.0.2, sous-graphe ~200 nœuds) identifié. 10 décisions architecturales : dépendance graphify-plugin 0.0.2 (WORKAROUND package relocalisé `com.cheroliv.graphify.model`), domaine `codebase.graph` 6ème, portage extracteur bakery → N1 socle, câblage canal Graphify réel budget 0.20, `graphRelations` alimenté, source office/graph.json surchargeable DSL, GraphModel synthétique tests, bump 0.0.6, zéro bump BOM. 5 US découpées (~39 pts). Bénéfice : planner + tous consommateurs codebase construisent leur graphe/sous-graphes. BACKLOG/INDEX/PROMPT_REPRISE/SESSIONS_HISTORY mis à jour ✅
| 176 | 2026-08-05 | SLD-8 US-8.1 | LlmBuildService Gradle bridge + corrections architecture. Pattern « Plugin Indépendant + Racine Consommateur » restauré (article 0124). codebase-plugin/ autonome avec son catalog. BOM 0.0.16 + codebase 0.0.5 mavenLocal. plugin.serialization fix (codebase + planner). US-8.1 livrée TDD/BDD 3 niveaux (5 unit + 3 functional + 4 cucumber = 12 tests, 0 FAIL). 2 fichiers prod additifs (LlmServiceResolver + LlmBuildService), zéro modification existant ✅
| 175 | 2026-06-26 | CR | Clôture code review vibecoding CR-GLOBALE-004. 10 findings archivés. Borough DORMANT ✅
| 172 | 2026-07-16 | EPIC C-BOM-FIX | C-BOM-FIX TERMINÉ 3/3 US (8/8 pts) — Versions explicites pour 6 artefacts `education.cccp` + 27 deps externes dans `libs.versions.toml` (le BOM en `implementation(platform(...))` ne propage pas ses contraintes aux consommateurs transitifs). Bump 0.0.5→0.0.6. `publishToMavenLocal` OK. POM 0.0.6 + `.module` contiennent les versions. 3 nouveaux UT `VersionCatalogTransitiveVersionsTest`. 22/22 UT bakery LangSwitch GREEN. Zéro régression codebase. Débloque bakery BKY-LANG-SWITCH ✅
| 171 | 2026-06-30 | — | Réduction bruit pgvector logs. 8 log.warn→debug dans 4 fichiers Koog/RAG. compileKotlin + cucumberTestEpicVLocal PASS ✅
| 169 | 2026-06-26 | — | CI-green confirmé (run 28206228434 fast-tests ✅ + full-tests ✅ 33m50s). Backlog synchronisé ✅
| 168 | 2026-06-26 | CI | 19 échecs JUnit5 full-tests résolus — @integration tag + EpicV6CucumberRunner excludes ✅
| 167 | 2026-06-26 | CI | V-6 runner @integration fix — EpicV6CucumberRunner excludes @integration ✅
| 161 | 2026-06-23 | Code Review | Affinage `CODE_REVIEW_MATURITY_ANALYSIS.adoc` — 5/10 critiques déjà résolues, 3 actions sûres appliquées (CVE-2015-6420→7501, README.md créé, CI split fast/full/publish), 3 concerns transverses dispatchés vers BACKLOG global. Compile OK ✅
| 160 | 2026-06-23 | Backlog Cleanup | Backlog & Roadmap cleanup — 4 fichiers gouvernance mis à jour (`.agents/BACKLOG.adoc` réécrit, `INDEX.adoc` V-9→TERMINÉ+V-LOCAL/TRAD ajoutés, `BACKLOG.adoc` root EPIC 1→STALE, `STRATEGIC_ROADMAP.adoc` phases 1-3→DELIVERED). Version bump 0.0.3→0.0.4. Borough confirmé dormant — aucun EPIC actif ✅
| 159 | 2026-06-23 | EPIC OCR-1/OCR-2 | OCR-1/OCR-2 restants TERMINÉS (9/9 items) — HTTP 429 detection + rotation auto (`GeminiKeyPool.markRateLimited`/`isRateLimited`, `GeminiVisionProvider` catch 429), env var mapping `GEMINI_API_KEY_1..N` (`GeminiPoolFactory.fromKeys`/`fromEnvVars`), multi-compte Google (`GeminiMultiAccountPool`), Tesseract fallback harmonisé boundary codex↔codebase (`OcrEngine`+`TesseractOcrEngine` dans codex Brooklyn, `CodexOcrEngineAdapter` dans codebase Queens), 35 tests nouveaux + 11 Cucumber `@epic_ocr_1`/`@epic_ocr_2` PASS, codex publishToMavenLocal 0.0.1, EPIC OCR complet 7/7 ✅
| 158 | 2026-06-23 | EPIC V-6-POOL | V-6-POOL terminé 4/4 : AUTHORIZED_MODELS aligné sur AGENTS.adoc (2 modèles `gpt-oss:120b-cloud` + `gemma4:31b-cloud`) dans `OllamaInstanceFactory` + `OllamaInstanceScanner`, `LlmProviderResolver` cycle les 2 modèles sur `OLLAMA_POOL_PORTS` au lieu de `DEFAULT_MODEL` statique, feature Cucumber `epic_v6_pool.feature` (3 scénarios @epic_v6_pool) + runner + step `includes model`, tous tests unitaires + Cucumber PASS, zéro régression V-6 ollama_cloud + V-6 resolver ✅
| 155 | 2026-06-23 | EPIC V-9 | V-9.19 Auto-detection of new governance files — `GovernanceFileSnapshot`/`GovernanceFileDiff`/`GovernanceFileChangeDetector`/`JsonGovernanceStateStore`/`IncrementalReport` DDD, mode incrémental câblé `IngestGovernanceTask` (diff snapshot précédente → `ingestFiltered`), extension DSL `incremental.set(true)` + CLI `-Pcodebase.governance.incremental=true`, 15 unit + 7 fonctionnel + 5 Cucumber @epic_v_9_19 PASS, régressions V-9.3→V-9.18 vertes ✅. Contrainte Ollama globale sauvegardée `AGENTS.adoc` + `AGENT.adoc`, 52/52 inférences `gpt-oss:120b-cloud`+`gemma4:31b-cloud` PASS.
| 154 | 2026-06-23 | EPIC V-9 | V-9.18 Compilation vers Gradle tasks — `AgenticGradleTaskRegistrar` enregistre GRADLE_TASK/CONSTRAINT_CHECK/VALIDATION comme tâches Gradle dynamiques, câblage `IngestGovernanceTask`, enrichissement `AgenticChunker` verbes taxonomiques, 7 JUnit5 + 3 fonctionnels + 3 Cucumber @epic_v_9_18 PASS, régressions V-9.3→V-9.17 vertes ✅
| 153 | 2026-06-23 | EPIC V-9 | V-9.17 Ingestion Summary DSL — extension Gradle `codebaseGovernance { strictValidation.set(true) }` + propriété projet `codebase.governance.strictValidation`, `GovernanceSummaryConfig` DDD + `CodebaseGovernanceExtension`, câblage `IngestGovernanceTask.governanceConfig`, 3 JUnit5 + 4 fonctionnels + 3 Cucumber @epic_v_9_17 PASS, régressions V-9.3/9.5/9.7/9.8/9.9/9.10/9.11/9.13/9.14/9.15/9.16 vertes ✅
| 152 | 2026-06-23 | EPIC V-9 | V-9.16 StrictValidationMode — option Gradle `strictValidation` dans `IngestGovernanceTask`, levée de `GradleException` si chunks invalides en mode strict, 3 JUnit5 + 3 Cucumber @epic_v_9_16 PASS, régression V-9.4 batch TAXONOMIE_WORKSPACE corrigée, régressions V-9.x vertes ✅
| 151 | 2026-06-23 | EPIC V-9 | V-9.15 Invalid Chunk Quarantine — port `InvalidChunkRepository` + implémentations in-memory/R2DBC (migration V4) + `InvalidChunkQuarantine` câblé dans `AgenticIngestor`, `IngestionReport.invalidChunks` + JSON, 12 JUnit5 + 2 Cucumber @epic_v_9_15 PASS, régressions V-9.x vertes ✅
| 150 | 2026-06-23 | EPIC V-9 | V-9.14 Auto-activation du hook d'interdiction — `GovernanceEnforcementWirer` câblé dans `VibecodingTask` et `SessionProtocolTask`, 5 JUnit5 + 2 fonctionnels + 2 Cucumber @epic_v_9_14 PASS, zéro régression V-9.x ✅
| 149 | 2026-06-23 | EPIC V-9 | V-9.13 ToolRegistry enforcement hook — `EnforcementHook` contrat N0, `buildEnforcementHook()` N1, `ToolRegistry` bloque nativement exec_shell/exec_gradle, 8 JUnit5 + 3 fonctionnels + 5 Cucumber @epic_v_9_13 PASS, zéro régression V-9.x ✅
| 148 | 2026-06-23 | EPIC V-9 | V-9.12 AgenticExecutor — `AgenticExecutor` PRE_HOOK runner, `IngestionReport.executables`, câblage `IngestGovernanceTask.lastExecutor`, 8 JUnit5 + 5 Cucumber @epic_v_9_12 PASS, zéro régression V-9.x et tests Y-4/Y-7. ✅
| 147 | 2026-06-23 | EPIC V-9 | V-9.11 AgenticCompiler exécutable — `ExecutableArtifact` + `ArtifactPayload` + `ExecutionResult`, `compileExecutable()`, `AgenticChunkEnforcement` consomme PreHookPayload, 8 JUnit5 + 4 Cucumber @epic_v_9_11 PASS, zéro régression V-9.x et tests Y-4/Y-7. ✅
| 146 | 2026-06-23 | EPIC V-9 | V-9.10 ChunkValidationReport — `ChunkValidationErrorType`, `lineStart`/`lineEnd`, `validationErrorsByType` JSON, 3 JUnit5 + 1 test fonctionnel + 3 Cucumber @epic_v_9_10 PASS, zéro régression V-9.x ✅
| 145 | 2026-06-23 | EPIC V-9 | V-9.9 ChunkValidationGate — `ChunkValidator` câblé dans `AgenticIngestor`, `IngestionReport` enrichi `chunksInvalid`/`validationErrors`, JSON enrichi, 3 JUnit5 + 2 Cucumber @epic_v_9_9 PASS, régressions V-9.3/9.5/9.7/9.8 vertes. BACKLOG.adoc synchronisé.
| 144 | 2026-06-23 | EPIC V-9 | V-9.8 Governance ingestion report — `GovernanceOntologizer` intégré dans `AgenticIngestor`/`IngestGovernanceTask`, `IngestionReport` enrichi de `sectionsAdded`/`sectionsTotal`, JSON enrichi, tâches Cucumber V-9.3/9.5/9.7/9.8 enregistrées. 10 JUnit5 + 3 Cucumber @epic_v_9_8 PASS ✅
| 143 | 2026-06-22 | EPIC V-9 | V-9.7 GovernanceOntologizer — enum `GovernanceSection` + classifieur par nom de fichier + 8 JUnit5 + 7 Cucumber @epic_v_9_7 PASS ✅ (V-9.6 déjà livré par EPIC Y-2). Under-EPIC 2 démarré.
| 142 | 2026-06-22 | EPIC V-9 | V-9.3 ScanAgent — `ScanAgent` scanner récursif `.agents/` + `ScannedFile` value object + 8 JUnit5 + 6 Cucumber @epic_v_9_3 PASS ✅ + refactor `IngestGovernanceTask.collectGovernanceFiles` (liste hardcoded supprimée). Under-EPIC 1 complet 5/5.
| 141 | 2026-06-22 | EPIC V-9 | V-9.5 ChunkValidator — 10 JUnit5 + 5 Cucumber @epic_v_9_5 PASS ✅ (anti-duplication V-9.1/V-9.2/V-9.4 vérifiée)
| 140 | 2026-06-22 | EPIC 8 | ExpertManifestReader + round-trip + Cucumber cross-borough — 8 JUnit5 + 2 round-trip + 6/6 @epic8 PASS ✅ (EPIC 8 clôturé 4/4)
| 139 | 2026-06-22 | EPIC V-6 | LlmProviderResolver factory déterministe + Cucumber @epic_v6_resolver — 9 JUnit5 + 3 Cucumber PASS ✅
| 138 | 2026-06-22 | EPIC V-6 | OllamaInstanceFactory + contrat N0 volumeTag + feature @epic_v6_ollama_cloud — 2 scénarios PASS ✅
| 137 | 2026-06-22 | EPIC V-6 | OllamaPoolKeyAdapter + câblage OllamaLlmProvider — 26/27 tests PASS ✅
| 136 | 2026-06-22 | EPIC V-6 | OllamaInstanceScanner actif + ports DDD + câblage resolver — 22 tests PASS ✅
| 135 | 2026-06-22 | EPIC PUB | N0 migration + codebase-plugin 0.0.3 publié Central (propagation en cours) ✅
| 134 | 2026-06-22 | EPIC PUB | Doublon + catalogue stale + N0 Central publiés ✅

| 133 | 2026-06-22 | EPIC TRAD | TranslationService N1 (contrat N0 + LlmTranslator + Fake + 8 JUnit5 + 6 Cucumber) ✅
| 132 | 2026-06-22 | EPIC V-LOCAL | V-LOCAL-4/5/6 terminés — 5 scénarios Cucumber + IngestGovernanceTask stub ✅
| 131 | 2026-06-22 | EPIC V-LOCAL | GovernanceContextLoader fallback + compliance ports ✅
| 130 | 2026-06-22 | EPIC L | Koog 1.0.0 replacement forwardTo ✅
| 129 | 2026-06-22 | EPIC V-LOCAL | GovernanceContextLoader + tests + docs ✅
| 128 | 2026-06-16 | Documentation Locale Vibecoding | LOCAL_VIBECODING_LOOP.adoc terminé
| 127 | 2026-06-16 | EPIC 8 Expert Exposure | 21/21 PASS ✅
| 126 | 2026-06-16 | EPIC 7 Expert Dispatcher | 60/60 PASS ✅
| 125 | 2026-06-14 | CR-14 BUILDING.md | TERMINE ✅
| 124 | 2026-06-14 | CR-12/13 Audit+Alias | TERMINE ✅
| 123 | 2026-06-14 | CR-10/11 Dependencies | TERMINE ✅
| 122 | 2026-06-14 | CR-8/9 Postgres+Kover | TERMINE ✅
| 121 | 2026-06-14 | CR-5/6/7 Root+CI+JVM | TERMINE ✅
| 120 | 2026-06-14 | CR-2/3/4 Timeout+Cache+Heap | TERMINE ✅
| 119 | 2026-06-14 | CR-1 Cucumber Factory | TERMINE ✅
| 118 | 2026-06-14 | OCR-3b FakeVision+CI | TERMINE ✅
|===

