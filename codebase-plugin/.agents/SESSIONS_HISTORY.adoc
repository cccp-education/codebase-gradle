= Sessions History — Codebase Gradle
:toc: left
:icons: font

 
=== Session 266 — 2026-09-19
* **EPICs** : Aucune (tâche autonomousSession non lancée en raison de problèmes de configuration multi-projet) — analyse du code et des tests effectués.
* **Livrables** :
** Rapport de session : `.agents/sessions/266-sujet.adoc`.
** Mise à jour du PROMPT_REPRISE.adoc pour la session 267.
* **Résultats** :
** Aucun task complété, mais meilleure compréhension de la configuration du build et de la structure des tâches.
** Tests unitaires et Cucumber pour autonomousSession passent.
* **Leçons** :
** La tâche autonomousSession est correctement enregistrée dans le plugin codebase lorsqu'on travaille directement dans ce projet.
** Le problème de découverte de la tâche est lié à la configuration multi-projet (includeBuild).
** Pour lancer la tâche, il faut travailler directement dans le projet codebase-plugin (sans includeBuild dans le racine).
** Archive session 266 créée (`.agents/sessions/266-sujet.adoc`)
 
== Session 164 — 2026-06-25

* **EPICs** : Aucun (borough dormant) — CI/CD + bugfix Cucumber préexistants
* **Livrables** :
** Exception CI/CD Ollama documentée dans `codebase-plugin/AGENT.adoc` + `AGENTS.adoc` racine (ports 11466-11467 + `qwen3:0.6b` pour tests d'intégration GitHub Actions uniquement)
** Workflow `.github/workflows/test.yml` enrichi : cache Docker `ollama/ollama:0.20.2` + cache models + 2 instances Ollama ports 11466-11467 + pull `qwen3:0.6b` + env vars `OLLAMA_TEST_PORT`/`OLLAMA_TEST_MODEL`
** `OllamaInstanceScannerTest.kt` + `OllamaLlmProviderTest.kt` : tests d'intégration portés vers ports CI dédiés via env vars, `assumeTrue`/`@EnabledIf` préserve skip hors CI
** Fix `DuplicateStepDefinitionException` : `Ocr1PoolSteps.poolUsageReset()` renommé `geminiKeyPoolUsageReset()` + `ocr.feature` step aligné ("the Gemini key pool usage is reset")
** Fix assertion stale `epic_v_pool.feature` : "5 distinct authorized models" → "2 distinct authorized models" (alignement AGENTS.adoc session 158)
** Archive session 164 créée (`.agents/sessions/164-ci-ollama-exception-cucumber-fix.adoc`)
* **Résultats** :
** `testFast` (PR gate CI) BUILD SUCCESSFUL — 324 tests, 0 failed
** Compilation verte après chaque baby step (5 vérifications)
** 7 fichiers modifiés : 118 insertions, 23 suppressions
** Bugs préexistants confirmés via `git stash` (non introduits par la session)
* **Leçons** :
** `testFast` échouait depuis session 159 (DuplicateStepDefinition) sans être remarqué — la CI GitHub Actions n'était pas active. Importance de valider `testFast` après chaque modification de steps Cucumber.
** Contrainte AGENTS.adoc modèles/ports légitime pour vibecoding production, doit céder devant réalité runner CI gratuit (RAM limitée, pas d'auth cloud). Exception documentée préserve la règle générale.
** Baby steps DDD/TDD/BDD : 1 fichier à la fois, compilation entre chaque, test ciblé sur runners impactés avant `testFast` complet.

== Session 165 — 2026-06-25

* **EPICs** : EPIC BOM (transverse — debug CI/CD + migration BOM)
* **Livrables** :
** Debug CI/CD en cascade (4 erreurs résolues en baby steps)
** Fix 1 : `gradle-wrapper.jar` dé-gitignoré + versionné (`.gitignore` corrigé)
** Fix 2 : `org.gradle.java.home` retiré des 2 `gradle.properties` (path SDKMAN inexistant sur CI)
** Fix 3 : Credentials nmcp optionnels dans `settings.gradle.kts` (fallback `?: ""` au lieu de `error()`)
** Fix 4 : Migration vers `workspace-bom:0.0.3` (source unique vérité versions)
** `codex-plugin:0.0.2` re-publié sur Central (OcrEngine/TesseractOcrEngine)
** `llm-pool-contracts:0.0.2` re-publié sur Central (volumeTag)
** 4 contracts N0 re-publiés en 0.0.2 (agent, codebase, opencode-session, i18n)
** `workspace-bom:0.0.3` publié sur Central (constraints internes activées)
** `libs.versions.toml` migré : deps sans version (gérées par BOM)
** `implementation(platform(libs.workspace.bom))` ajouté au `build.gradle.kts`
** Archive session 165 créée
* **Résultats** :
** Compilation locale verte avec BOM
** CI en attente propagation Central (15-30 min) — `Could not find workspace-bom:0.0.3`
** 4 commits poussés sur `main`
* **Leçons** :
** Wrapper jar DOIT être versionné (convention workspace : 4 autres boroughs le font)
** `org.gradle.java.home` ne doit jamais être hardcodé dans un fichier versionné
** `nmcpSettings` ne doit pas `error()` au settings phase — fallback optionnel
** BOM = source unique de vérité versions cross-borough (pattern Maven standard)
** Versions cross-borough doivent être publiées sur Central avant que les consommateurs puissent compiler en CI
** Planner-plugin (Manhattan) : borough dormant à structure cassée (catalog inaccessible depuis sous-module). À traiter séparément.

=== Session 167 — 2026-06-25
* **EPICs** : Aucun (fix CI)
* **Livrables** :
** `EpicV6CucumberRunner.kt` : filtre `@epic_v_6` → `@epic_v_6 and not @integration` (1 ligne)
** `.github/workflows/ingest-governance.yml` supprimé (workflow cassé depuis session 160, task inexistante)
** Archive session 167 créée
* **Tests** : cucumberTestEpicV6 vert (scénario @integration SKIPPED), testFast vert
* **CI** : Fast Tests ✅ (2m5s), Full Test Suite run 28196553990 en cours validation
* **Leçons** :
** Tag `@integration` sur feature ne suffit pas — chaque runner dédié doit aussi exclure `@integration` dans `FILTER_TAGS_PROPERTY_NAME`
** Workflow CI non testé localement avant commit = anti-pattern (ingest-governance.yml appelait task inexistante depuis session 160)
** `gh run view --log --job=<id>` = outil debug CI le plus efficace (stack traces complètes)

=== Sessions 168-188 — 2026-06-26 → 2026-08-17
* Voir INDEX.adoc table sessions 168-188 + archives `.agents/sessions/`.
* S-168 fix 19 échecs JUnit5 CI, S-169 CI-green, S-170 SEC-1, S-171 pgvector logs,
  S-172 C-BOM-FIX, S-173 pub Central 0.0.6, S-174 code review, S-175 clôture CR,
  S-184 VIBE-HARDENING US-0, S-185 US-1, S-186 US-2, S-187 US-3+4+5 TERMINÉ,
  S-188 maintenance + cadrage 2 EPICs (FT-PIPELINE + VIBE-HARDENING-2) + propagation 5 boroughs.

=== Session 189 — 2026-08-17
* **EPICs** : VIBE-HARDENING-2 (US-1 + US-2)
* **Livrables** :
** US-1 S2 Allowlist `TaskDiscoveryRegistrar` : `TASK_ALLOWLIST` + `TASK_DENYLIST` + `validateTaskName` companion (pattern `ExecGradleTool.validateGradleTask`). `registerAll` filtre via validateTaskName. 18 unit allowlist + 6 tests existants réécrits (vibecode/qualityGate devenus denied — tasks métier plugin non auto-enregistrées, sécuritaire).
** US-2 S3 Garde taille `write_file`/`edit_file` : `MAX_WRITE_CHARS = 1_000_000` const + 3 checks (content, newString, resulting updated) → `IllegalArgumentException` avant writeText. Pattern `read_file MAX_OUTPUT_CHARS` mirror. +5 unit.
** Archive session 189 créée.
* **Tests** : Gate `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 105 tests (1 fail préexistant C4 US-3 GradleTaskArgsMapperTest). Non-régression ExecShellTool (9) + ExecGradleTool (15) VERTS.
* **Fichiers** : 4 modifiés + 1 nouveau (AllowlistTest). +157 lignes prod/test.
* **Leçons** :
** Allowlist deny-by-default pour tasks découvertes : la deny-list n'est qu'un second ride pour les composed commands qui passent l'allowlist (`:plugin:publishToMavenCentral`). Pour les noms purs (`clean`, `purge`), c'est l'allowlist qui rejette — message "not in allowlist".
** Changement comportemental US-1 sécuritaire mais majeur : tasks métier du plugin (vibecode, qualityGate, generatePlan, etc.) ne sont plus auto-enregistrées comme tools `gradle_*`. Un agent LLM ne doit pas piloter la boucle agentique elle-même via tools découverts (récursion dangereuse).
** Pattern S-113 (multi-US 1 session) applicable car US-1 et US-2 indépendants et triviaux.
* **Zéro bump BOM, zéro secret, zéro régression.** 2/8 US VIBE-HARDENING-2.

=== Session 199 — 2026-08-24
* **EPICs** : CDX-RAG-SOCLE (cross-borough Brooklyn pilote, US-1)
* **Livrables** :
** US-1 Domaine `codebase.store` créé (5 prod + 3 test, ~330 lignes) : migration verbatim `codex.store.CodexVectorStore` → `codebase.store.RagVectorStore` + `StoreStatements` (ex-`IngestStatements`) + `IngestIndexing` + types autonomes `RetrieveResult`/`DocumentChunk`. Tables pgvector INCHANGÉES (Loi Économie d'Encre).
** Pré-requis bump versions : codex 0.0.5 → mavenLocal, codebase `libs.versions.toml` BOM 0.0.22→0.0.24 + codex 0.0.3→0.0.5, codebase 0.0.8 republié mavenLocal.
** Archive session 199 créée.
* **Tests** : 25/25 characterization tests PASSED (6 `IngestIndexingTest` + 9 `StoreStatementsTest` + 10 `RagVectorStoreTest`), mirror des tests codex. Gate `codebase.rag.*` non-régression VERT. `compileKotlin`/`compileTestKotlin` VERT.
* **Fichiers** : 8 nouveaux (5 prod `codebase.store` + 3 test), 2 modifiés (`libs.versions.toml` bump).
* **Leçons** :
** Verbatim migration délibérée (pas refactor) : characterization tests mirroir verrouillent le contrat comportemental avant RAG-2 (bascule call sites).
** Types autonomes (`DocumentChunk`/`RetrieveResult` dans `codebase.store`, pas d'import `codex.tasks.*`) — prérequis pour RAG-4 (retrait `libs.codex.plugin`).
** Bump préventif avant de coder évite le drift pendant RAG-1/RAG-3.
* **Zéro bump BOM (pré-requis), zéro secret, zéro régression.** EPIC CDX-RAG-SOCLE 2/6 US.

=== Session 205 — 2026-08-25
* **EPICs** : CDX-RAG-SOCLE (US-2, US-3, US-4, US-5 terminés — confirmation via backlog update)
* **Livrables** : Mise à jour backlog + INDEX pour marquer EPIC TERMINÉ 6/6 US. Vérification zone sale parallèle Brooklyn (aucune modification non committée). Tests package `codebase.store.*` tous verts. Build compile.
* **Zéro code prod modifié, zéro bump BOM, zéro secret.** EPIC CDX-RAG-SOCLE TERMINÉ 6/6 US.

=== Session 206 — 2026-08-25
* **EPICs** : CB-STORE-UNIFY ✅ CADUQUE + hardening `VectorStore.initSchema()`
* **Livrables** :
** Audit pré-coding révèle cadrage CB-STORE-UNIFY existant trop simpliste : APIs très différentes (`RagVectorStore` search-only R2DBC vs `VectorStore` ingestion+query JDBC), schémas distincts (`codex_documents`/`codex_chunks` vs `documents`/`chunks`), consommateurs profonds (OcrIngestTask, MultiChannelContextGraph utilise les deux, KoogAugmentedContextGraph, KoogPlanningOrchestrator, VectorQueryService, EmbeddingPipeline, PgVectorConfig + 7 tests), usages sémantiquement distincts (corpus documentaire codex vs code source workspace).
** Discussion pilote confirme architecture N2→N1 (codex ingère, codebase stocke + RAG, flèche unique, zéro cycle) — les deux stores légitimes servent des domaines distincts. ÉPIC clos comme CADUQUE.
** Hardening ciblé baby-step TDD strict RED → GREEN → REFACTOR : `initSchema()` devient alias `ensureSchema()` (non-destructeur, `CREATE TABLE IF NOT EXISTS`), `clean()` publique ajoutée (destructeur test-only, isolé du chemin prod). 3 `@BeforeEach` mis à jour (`VectorStoreTest`, `OcrIngestTaskTest`, `OcrIngestSteps`). `PrepareContextUnitTest` inchangé (setup one-shot idempotent, flag `datasetIndexed`).
* **Tests** : 33/33 VectorStoreTest (dont 2 nouveaux tests non-destructeur) + OcrIngestTaskTest + 5/5 PrepareContextUnitTest + 3 cucumber dédiés (cucumberTestEpicOcr + cucumberTestRagSocle + cucumberTestOcrContracts) VERTS. Zéro régression.
* **Fichiers** : 1 prod (`VectorStore.kt` — `initSchema()` alias `ensureSchema()` + `clean()` + KDoc hardening), 3 tests (`VectorStoreTest.kt`, `OcrIngestTaskTest.kt`, `OcrIngestSteps.kt` — `@BeforeEach` use `clean()`).
* **Leçons** :
** Audit pré-coding obligatoire : cadrage existant prévoyait "remplacer les appels par searchBlocking (API identique)" — faux. L'audit a évité un EPIC lourd (~15-20 pts) qui aurait cassé l'indexation du code source.
** Architecture N2→N1 valide : codex ingère, codebase stocke + RAG, flèche unique. Les deux stores légitimes servent des domaines distincts. La "consolidation" n'était pas nécessaire.
** Hardening ciblé vs refactoring global : le `DROP CASCADE` destructif était le seul vrai risque. Isolé en `clean()` test-only, `initSchema()` devient non-destructeur. Défense en profondeur sans casser l'existant.
* **Zéro bump BOM, zéro secret, zéro régression.** EPIC CB-STORE-UNIFY ✅ CADUQUE.

=== Session 208 — 2026-08-25
* **EPICs** : MEMPHIS-BOM-FIX (cadrage cross-borough, US-1 ✅)
* **Livrables** :
** Option A S-205 : propagation repo1 `codebase-plugin:0.0.9` confirmée HTTP 200, POM propre (zéro codex-plugin résiduel, ocr-contracts:0.0.1 + codebase-contracts présents). Fix S-204 propagé Central.
** PROMPT_REPRISE rafraîchi S-205 → S-208 (clôture S-206 CB-STORE-UNIFY CADUQUE + S-207 migration .agents/). Options A/B marquées FAITES.
** Cross-borough : CAP-TRANSCRIPT US-5 déjà codée (commit `0842c33` S-119, feature + runner + steps + CucumberTaskSpec 15ème). BACKLOG Détroit stale (`⬜ TODO`).
** Bug bloquant découvert : BOM MEMPHIS catalogue `slider-plugin = { module = "education.cccp:slider", ... }` (ligne 261) référence ancien artifactId `slider` (max 0.0.16 Central) au lieu de `slider-plugin` (artifactId réel depuis aplatissement S-067, 0.0.18 publié Central). BOM 0.0.24/0.0.26 Central référencent `slider:0.0.18` → 404.
** Cascade bloquante : capsule → BOM 0.0.27 (fix) → codebase 0.0.9 → BOM 0.0.24 importé → `slider:0.0.18` 404. `cucumberTestTranscript` PASSED avec fix catalogue (résolution unique), mais `test` (ProjectBuilder) FAILED 79/917 — `NoClassDefFoundError InvokerHelper` (conflit classpath induit par résolution double slider/slider-plugin).
** EPIC MEMPHIS-BOM-FIX cadré 5 US : US-1 ✅ catalogue MEMPHIS fixé + bump BOM 0.0.27 + publishToMavenLocal VERT, US-3 publier BOM 0.0.27 Central, US-4 republier codebase 0.0.10, US-5 re-appliquer fix capsule + valider check.
* **Fichiers** : `workspace-bom/gradle/libs.versions.toml` (ligne 261 slider → slider-plugin), `workspace-bom/build.gradle.kts` (version 0.0.26 → 0.0.27), `codebase-gradle/codebase-plugin/PROMPT_REPRISE.adoc` (S-208), `.agents/INDEX.adoc` (session 208 + EPIC table), `.agents/sessions/208-cross-borough-memphis-bom-fix.adoc` (rapport).
* **Leçons** :
** Aplatissement structure borough (slider S-067 `slider/` → `slider-plugin/`) a changé l'artifactId Maven (`slider` → `slider-plugin`). Le BOM MEMPHIS n'a pas suivi — dette technique latente révélée par tentative de build capsule.
** BOM importée transitivement (codebase 0.0.9 POM importe BOM 0.0.24) crée une cascade de contraintes. Fix catalogue seul insuffisant : il faut republier les consommateurs directs (codebase 0.0.10) pour casser la chaîne BOM périmée.
** Rollback capsule en clôture : le fix catalogue appliqué avant republication codebase provoque une régression `test` (conflit résolution). Préserver le baseline > appliquer un fix partiel.
* **Zéro code production modifié (catalogue + doc), zéro secret.** US-3/4/5 À FAIRE (publications Central accord pilote).

=== Session 209 — 2026-08-25
* **EPICs** : MEMPHIS-BOM-FIX US-4 TERMINÉ (cross-borough MEMPHIS + Detroit)
* **Livrables** :
** codebase-plugin 0.0.9 → 0.0.10 (bump version build.gradle.kts).
** BOM 0.0.24 → 0.0.27 (catalogue libs.versions.toml ligne 9).
** Fix POM Central : `kotlin-stdlib-jdk8` version explicite ajoutée au
   catalogue (oubli EPIC C-BOM-FIX S-172 — Central Portal rejette deps sans
   version, 1er upload rejeté `"Dependency version information is missing for
   dependency: org.jetbrains.kotlin:kotlin-stdlib-jdk8"`). Remplacement
   `kotlin("stdlib-jdk8")` → `libs.kotlin.stdlib.jdk8` (build.gradle.kts:40).
** `compileKotlin --rerun-tasks` VERT, `publishToMavenLocal` VERT. POM local
   vérifié : `kotlin-stdlib-jdk8:2.4.10` + importe BOM 0.0.27.
** Publication Central : `codebase-plugin:0.0.10` HTTP 200, POM Central
   importe BOM 0.0.27 ✅ (cascade `slider:0.0.18` 404 cassée).
** Cross-borough : US-3 BOM 0.0.27 Central (MEMPHIS S-024, bundle
   `648c037e`) + US-5 capsule fix (Detroit S-120, 917 unit 0 FAIL +
   cucumberTestTranscript 4/4 PASSED + retrait testImplementation(libs.slider)
   Groovy 3.x conflit ProjectBuilder).
* **Fichiers** : `build.gradle.kts` (version 0.0.10 + libs.kotlin.stdlib.jdk8),
   `gradle/libs.versions.toml` (BOM 0.0.27 + kotlin-stdlib-jdk8 entry).
* **Leçons** :
** Central Portal enforcement : les deps sans version explicite dans le POM
   sont rejetées. Le BOM en `implementation(platform())` ne propage pas ses
   contraintes aux consommateurs transitifs (problème C-BOM-FIX S-172).
   `kotlin("stdlib-jdk8")` échappe au catalogue — toujours utiliser
   `libs.kotlin.stdlib.jdk8` pour POM Central.
** Bug Groovy 3.x (capsule S-120) : `testImplementation(libs.slider)` amenait
   `asciidoctor-gradle-jvm-slides` → `jrubygradle-resolver` → `groovy-nio:3.0.17`
   qui cassait ProjectBuilder (Groovy 4). Retrait — aucun test n'importait
   slider. Le `compileOnly` suffit pour le runtime.
* **EPIC MEMPHIS-BOM-FIX TERMINÉ 5/5 US.** Zéro code production, zéro secret.

== Session 211 — 2026-09-07

* **EPICs** : Aucun (synchronisation gouvernance post-S-210 — gouvernance pure)
* **Type** : governance (pattern S-057/S-063)
* **Livrables** :
** PROMPT_REPRISE réécrit S-208 → S-211 : mission MEMPHIS-BOM-FIX listant
   US-3/4/5 "À FAIRE" purgée (EPIC terminé 5/5 S-209) — faux positif
   d'ouverture corrigé.
** INDEX : entrée Session 211 (table gouvernance agentique) + header
   2026-09-07.
** État git post-S-210 consigné : version locale 0.0.11 (bump sync
   post-Central 0.0.10, commit `58670ad`), align conventions-plugin 0.0.4
   (`ba2fa1c`/`0911bf7`), fix OCR propriété morte `ocrEngine
   Provider<OcrEngine>` retirée (commit `fb19b7c` — décoration Gradle
   impossible).
** Rapport de session : `.agents/sessions/211-sync-gouvernance-post-210.adoc`.
** Archive : entrée ajoutée COMPLETED_TASKS_ARCHIVE_2026-08.adoc.
* **Fichiers** : `PROMPT_REPRISE.adoc`, `.agents/INDEX.adoc`,
  `.agents/SESSIONS_HISTORY.adoc`, `.agents/sessions/211-*.adoc`,
  `.agents/archives/COMPLETED_TASKS_ARCHIVE_2026-08.adoc`.
* **Leçons** :
** Le hook de fermeture (6 étapes) doit être exécuté à CHAQUE clôture —
   PROMPT_REPRISE figé à S-208 pendant 3 sessions a créé un faux positif
   de mission "À FAIRE" sur un EPIC terminé.
** Les commits post-session sans rapport (bump sync, align conventions)
   doivent être consignés à l'ouverture suivante, pas ignorés.
* Zéro code production, zéro bump BOM, zéro secret.
