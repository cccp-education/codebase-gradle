= BACKLOG — Codebase Gradle
:toc:
:last_update: 2026-08-17 (Session 188 — Maintenance + cross-borough + VIBE-HARDENING-2 cadrage + FT-PIPELINE cadrage)

== Statut Global

**EPIC VIBE-HARDENING ✅ TERMINÉ (6/6 — S184-S187)** — durcissement vibecoding P1 (S1 allowlist shell + gradle, C2 retry single counter, C5 timeout LLM, Cucumber BDD). **EPIC PUB (bump 0.0.8) ✅ TERMINÉ (S183)**. **EPIC SUBGRAPH ✅ TERMINÉ (4/4 — S182)** — contexte augmenté Graphify réel. Le reste du backlog est dormant :

. *EPIC FT-PIPELINE* (P0 vision) — 🟡 CADRÉ (S-188, 7 US ~18 pts) — pipeline de fine-tuning N1 générique (fabrication modèle : dataset prep + continual pre-training 10% corpus + export GGUF + push Ollama). Source de conception : notebook-gradle ARCHITECTURE.adoc (vestige récupéré). Domaine `codebase.finetuning` (7ème). Boucle fabrication→exposition avec domaine `codebase.koog.expert` existant (13 fichiers). TRN-8 training reformulé en recettes N2 consommant le pipeline. Cadrage pur, zéro code. Pas d'exécution sans déclencheur pilote.
. *EPIC VIBE-HARDENING-2* (P2/P3) — 🟡 EN COURS (S-188 cadrage + S-189 US-1+US-2, 2/8 US) — durcissement vibecoding P2/P3 (S2 allowlist tasks découvertes ✅, S3 garde taille write/edit ✅, C1 TaskResultVerifier refonte, C3 cleanup doc, C4 flags cassés, C6 refactor SessionProtocol, C7 negative counter). 6 US restantes. Gate `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 105 tests.
. *EPIC VIBE-HARDENING* (P1) — ✅ TERMINÉ 6/6 US (US-0 cadrage S184, US-1 shell allowlist S185, US-2 gradle allowlist S186, US-3 timeout LLM S187, US-4 retry single counter S187, US-5 cucumber BDD S187). Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier VERT 64 tests 0 FAIL + 4/4 cucumber PASSED. P2/P3 restants (S2/S3/C1/C3/C4/C6/C7) → EPIC VIBE-HARDENING-2 si déclencheur.
. *EPIC SUBGRAPH* (P1) — ✅ TERMINÉ 4/4 US (S178 cadrage, S179 domaine `codebase.graph`, S180 canal Graphify réel, S182 US-3 graphRelations+Resource + US-4 Cucumber 4 scénarios @subgraph). Contexte augmenté complet : EAGER + RAG + GRAPHIFY réel + Docs + Resource.
. *EPIC 1* (2 items stale — snapshot + commit initial) — à purger, le projet est déjà publié Maven Central 0.0.3.
. *EPIC V-FLYWAY* (P2, conditionnel) — ne se déclenchera que quand 5+ fichiers de migration de schéma existeront (actuellement 4 : V1→V4).
. *EPIC 5* — migré vers training-gradle TRN-8, plus dans le scope codebase. TRN-8 reformulé en recettes N2 consommant FT-PIPELINE (voir EPIC FT-PIPELINE US-6).

== Dette Technique (Résolue)

=== P0 — `ignoreFailures` ✅ RÉSOLU
`ignoreFailures = !isCI()` dans build.gradle.kts:201. CI-aware, non silencieux en local.

=== P1 — Duplication vibecoding-contracts ✅ RÉSOLU
Package local `src/main/kotlin/vibecoding/contracts/` supprimé. Consommation via `education.cccp:vibecoding-contracts:0.0.1` Maven Central.

=== P2 — `println()` résiduels ✅ RÉSOLU
Remplacés par `project.logger`.

=== P2 — Wrapper Gradle 9.5.1 ✅ RÉSOLU
Wrapper à 9.5.1.

== EPICs Terminés (récapitulatif)

[cols="2,2,3"]
|===
| EPIC | Statut | Sessions

| EPIC 1 Gouvernance
| STALE (6/8 — items obsolètes, projet publié Central)
| 001

| EPIC 2 Anonymisation
| ✅ TERMINE
| 009

| EPIC 3 Multi-Canal
| ✅ TERMINE
| 046

| EPIC 4 Benchmark
| ✅ TERMINE
| 026

| EPIC 5 Fine-tuning
| MIGRÉ → training-gradle TRN-8
| 110

| EPIC 6 ONNX Qualité
| ✅ TERMINE
| 056

| EPIC 7 Expert Dispatcher
| ✅ TERMINE
| 126

| EPIC 8 Exposer experts
| ✅ TERMINÉ (4/4)
| 127, 140

| EPIC 9 Opencode Augmenté
| ✅ TERMINE (15/15)
| 001-021

| EPIC 10 STIMULUS
| ✅ TERMINE
| 022+025

| EPIC 12 Blog Narration
| ✅ TERMINE
| 109

| EPIC K Ontologie
| ✅ TERMINE
| 015-020

| EPIC L koog+langchain4j
| ✅ TERMINE
| 034-049

| EPIC V Vibecoding
| ✅ TERMINE (15/15)
| 041-045

| EPIC V-6 Ollama Cloud
| ✅ TERMINÉ (10/10 + V-6-POOL 4/4)
| 136-139, 158

| EPIC V-7 Resolver + CLI
| ✅ TERMINÉ
| 068

| EPIC V-8 Resume + Dashboard
| ✅ TERMINÉ
| 069

| EPIC V-9 Gouvernance Agentique
| ✅ TERMINÉ (20/20)
| 141-157

| EPIC V-LOCAL Boucle Locale
| ✅ TERMINÉ (7/7)
| 129-132

| EPIC W Auto-discovery
| ✅ TERMINÉ (7/7)
| 100

| EPIC X Multi-Step
| ✅ TERMINÉ (7/7)
| 101-105

| EPIC Y AgenticSchema
| ✅ TERMINÉ (8/8)
| 097

| EPIC Z Autofocus
| ✅ TERMINÉ (7/7)
| 108

| EPIC OCR
| ✅ TERMINÉ (7/7)
| 027-159

| EPIC SP SessionProtocol
| ✅ TERMINÉ (7/7)
| 114

| EPIC TRAD TranslationService
| ✅ TERMINÉ
| 133

| EPIC CR Code Review
| ✅ TERMINÉ (14/14)
| 119-125

| EPIC PUB Publication
| ✅ TERMINÉ — codebase-plugin 0.0.3 Central
| 135

| Documentation Locale
| ✅ TERMINÉ
| 128
|===

== EPIC PUB — Publication Maven Central codebase-plugin 0.0.8

**Statut** : ✅ TERMINÉ (3/3 US — session 183)
**Priorité** : P1
**Sessions** : 181 (audit, gouvernance pure) → 183 (exécution)

=== Constat (audité S181)

. `codebase-plugin/build.gradle.kts:8` → `version = "0.0.7"` (code local)
. Central 0.0.7 publié (bundle `e08acb47`, S-073 LLM-ADAPTER-EXTRACT)
. Commits post-publication non propagés : `f840b93` SUBGRAPH US-1+US-2 (domaine `codebase.graph` + canal Graphify réel)
. Bump 0.0.7→0.0.8 reporté fin d'EPIC SUBGRAPH (décision S179) — désormais cadré comme EPIC dédié

=== Décision

Bump + publication Maven Central codebase-plugin **0.0.7 → 0.0.8** au backlog uniquement (pas d'exécution dans cette tâche — gouvernance pure, pattern bakery S199).

=== US découpées (~3 US)

. US-0.0.8-1 — ✅ S183 : POM `withType<MavenPublication>` (name/description codebase-plugin) + signing `useGpgCmd` (conventions-gradle publishing 0.0.2) + release deps vérifiées (zéro SNAPSHOT, 12 artefacts education.cccp en release Central)
. US-0.0.8-2 — ✅ S183 : `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL 28s, bundle `4ae55e92-a116-4a25-8edf-b1f7cc9a56d4` AUTOMATIC, HTTP 200 repo1 `codebase-plugin-0.0.8.pom`
. US-0.0.8-3 — ✅ S183 : zéro tag local/origin à aligner (cleanup néant) + BOM non bumpé (constraint codebase 0.0.5 dans BOM 0.0.18 Central — géré MEMPHIS, pratique canonique zéro bump BOM)

== EPIC SUBGRAPH — Contexte Augmenté Graphify (graphe + sous-graphes réels)

**Statut** : ✅ TERMINÉ (4/4 US — session 182)
**Priorité** : P1
**Sessions** : 178 (cadrage) → 179 (US-1) → 180 (US-2) → 182 (US-3+US-4)

=== Problème (constat audité)

Le canal `ContextChannel.Graphify` (N0, budget 0.20) n'est **jamais alimenté avec du contenu réel** :

. `CompositeContextBuilder.loadGraphifyStats` (CompositeContextBuilder.kt:138) — ne fait que des *stats regex* sur `office/graph.json` brut (`"name":`/`"source":` comptés) → `"[Graphify] global graph: ~N nodes, ~M edges"`. Zéro contenu structurel.
. `MultiChannelContextGraph.collectGraphifyNode` (MultiChannelContextGraph.kt:130) — appelle la même `loadGraphifyStats` (même faux positif).
. `GovernanceContextLoader.graphRelations` (GovernanceContextLoader.kt:68) — hardcodé `""`, jamais alimenté (le canal existe dans `LiveContextInjector` mais reste vide).
. `ContextChannel.Resource` (MultiChannelContextGraph.kt:154) — hardcodé `""`.
. Aucune dépendance codebase → graphify-plugin dans `build.gradle.kts`/`libs.versions.toml` (grep vide).
. Contrats N0 (`codebase-contracts`) : `ContextChannel.Graphify` est un simple type de canal — aucun contrat de graphe (GraphModel/communities/edges).

=== Pattern existant à copier (precedent bakery)

`bakery-gradle` (N2, LENTILLE) est le **seul consommateur structuré du graphe** — le standard de fait du workspace :

. Dépendance réelle : `education.cccp:graphify-plugin:0.0.2` (bakery-plugin/build.gradle.kts:38 + catalog).
. `SubgraphExtractor.loadGraph` (bakery/lens/SubgraphExtractor.kt:37) — parse réel du graph.json (11 MB, 203k nœuds) en `GraphModel` via Jackson.
. `LensConfig` (communautés/type nœuds/type edges/profondeur max/extensions) → sous-graphe ciblé ~200 nœuds.
. `BfsProximityCache` + `AugmentedArticlesService` (β = 0.30) — scoring de proximité structurelle.
. Contrat DAG documenté : bakery (N2) importe graphify-plugin (N0).

=== Décisions architecturales (US-0 cadrage)

. **Dépendance** : codebase → `education.cccp:graphify-plugin:0.0.2` (déjà Central, pattern bakery). Attention WORKAROUND bakery : 0.0.2 publié avec package relocalisé `com.cheroliv.graphify.model` au lieu de `graphify.model` — consommer le package réel, revoir si 0.0.3 corrigé.
. **Domaine** : nouveau domaine DDD `codebase.graph` (6ème domaine de contexte, pattern `codebase.rag`).
. **Portage** : `SubgraphExtractor` + `LensConfig` portés depuis bakery vers codebase (N1 socle partagé — bakery doit ensuite consommer codebase au lieu de dupliquer). Source de vérité unique N1.
. **Câblage** : `loadGraphifyStats` remplacé par le vrai extracteur dans `CompositeContextBuilder` ET `MultiChannelContextGraph.collectGraphifyNode` — le canal `Graphify` reçoit le sous-graphe réel (nodes/edges/communities, format texte structuré, tronqué au `graphifyTokens` existant).
. **`graphRelations`** : alimenté depuis le sous-graphe (au lieu de `""`) — `GovernanceContextLoader` + `LiveContextInjector` deviennent effectifs.
. **Budget** : respecter `budgetGraphify = 0.20` (`graphifyTokens`) — le sous-graphe est tronqué au budget existant, zéro changement de contrat.
. **Source graph.json** : défaut `office/graph.json` (11 MB), surchargeable via extension DSL.
. **Testabilité** : `GraphModel` synthétique en tests (pattern `SiteContextCollectorTest`/`SubgraphExtractorTest` bakery), `LensConfig` scope FULL/SEMANTIC_ONLY/ciblé.
. **Bump** : version 0.0.6 (après 0.0.5 publié). Zéro bump BOM si graphify-plugin 0.0.2 déjà sur Central.

=== Bénéfice

Chaque projet consommant codebase (planner, slider, capsule, training, plantuml...) obtient la capacité de construire **son graphe et ses sous-graphes** — le contexte augmenté devient complet : EAGER + RAG + GRAPHIFY réel + Docs + Resource.

=== US découpées (~5 US)

. US-0 — Cadrage EPIC (✅ session 178, ce document)
. US-1 — Dépendance graphify-plugin + domaine `codebase.graph` (✅ session 179 : `LensConfig` + `SubgraphExtractor` + `SiteSubgraph` portés de bakery, 26 unit, gate non-régression VERT 912 tests/0 FAIL, WORKAROUND package `com.cheroliv.graphify.model` confirmé, bump 0.0.7→0.0.8 reporté en fin d'EPIC)
. US-2 — Câblage `CompositeContextBuilder` + `MultiChannelContextGraph` (canal Graphify réel, ~8 unit + 2 functional) — ✅ session 180 : `GraphContextRenderer` (6 unit) + `GraphifyContextProvider` (5 unit) + câblage `CompositeContextBuilder` (param `graphFile` + `graphifyProvider`) + `MultiChannelContextGraph.collectGraphifyNode` (provider `office/graph.json`), `loadGraphifyStats` regex supprimé × 2, 3 functional `CompositeContextBuilderGraphifyTest` + 1 test graphify `MultiChannelContextGraphTest`, gate ciblé VERT 24s 0 FAIL, `codebase.graph.*` 37 tests 0 FAIL
. US-3 — `graphRelations` alimenté (GovernanceContextLoader + LiveContextInjector, ~8 unit) — ✅ session 182 : `GovernanceContextLoader(graphFile: File? = null, lensConfig: LensConfig = LensConfig())` + `graphRelations = graphFile?.let { GraphifyContextProvider(it, lensConfig).provide() } ?: ""` (fallbacks conservés) + `MultiChannelContextGraph(graphFile: File? = null)` + canal `ContextChannel.Resource` alimenté depuis le sous-graphe (budget resource 0.10) + câblage `SessionProtocolTask`/`SessionProtocolServer` (`root.resolve("office/graph.json")`) → `LiveContextInjector` effectif. 6 unit, TDD strict RED → GREEN → REFACTOR, gates ciblés VERT
. US-4 — Cucumber BDD (`codebase_subgraph.feature`, ~4 scénarios @subgraph) — ✅ session 182 : feature 4 scénarios @subgraph + `SubgraphWorld`/`SubgraphSteps` (14 step defs) + `SubgraphCucumberRunner` + `CucumberTaskSpec("cucumberTestSubgraph")`, 4/4 PASSED 21s, zéro production modifié — pure BDD

== EPIC VIBE-HARDENING — Durcissement Vibecoding (CR-GLOBALE-004)

**Statut** : ✅ TERMINÉ (6/6 US — sessions 184-187)
**Priorité** : P1
**Origine** : CR-GLOBALE-004 (session 175, code review vibecoding) — 10 findings (3 P1 + 7 P2/P3)
**Cadrage** : `CADRAGE_EPIC_VIBE_HARDENING.adoc` (10 décisions architecturales)

=== Constats P1 audités (3)

. **S1 — Blacklists bypassables** : 10 regex `BASH_BLACKLIST` + 4 `GRADLE_BLACKLIST` contournables (`rm -Rf` uppercase, `chmod 0777` zero-prefixed, `clean  build` double espace, variations `--refresh-dependencies`, `rm`/`delete` ne couvrent pas `sudo`/`curl`/`;`/`|`).
. **C2 — Retry double-compté** : `retryCount = state.retryCount + 1` dans `executePlanTasks` (L587/L604) + 3 `incrementRetry()` dans branches replan (L291/L298/L302) = 2 incréments/essai, `maxRetries=3` épuisé en 2 essais.
. **C5 — LLM sans timeout** : 3 sites `runBlocking { llmProvider.call(...) }` sans `withTimeout` (callLLMNode L413, replan L288, StepVerifier L44) — hang indéfini possible.

=== P2/P3 reportés (7) — EPIC VIBE-HARDENING-2 si déclencheur

S2 (pas d'allowlist Gradle globale), S3 (edit/write sans garde taille), C1 (heuristique `contains("failed")`), C3 (VibecodingPlan dead code), C4 (flags Gradle cassés), C6 (duplication), C7 (compteur négatif).

=== US découpées (~12 pts)

. US-0 — ✅ S184 : Cadrage EPIC (gouvernance pure, 3 constats P1 + 7 rappels P2/P3 + 10 décisions archi, 6 US ~12 pts)
. US-1 — ✅ S185 : ExecShellTool allowlist deny-by-default (10 patterns `SHELL_ALLOWLIST` + 10 `SHELL_DENYLIST` second ride, `validateCommand` allowlist puis deny-list, `ExecShellToolTest` 11→17 tests, gate VERT 21 tests 0 FAIL)
. US-2 — ✅ S186 : ExecGradleTool allowlist deny-by-default (2 `GRADLE_TASK_ALLOWLIST` + 2 `GRADLE_ARG_ALLOWLIST` + 8 `GRADLE_DENYLIST`, `validateGradleTask` split tokens + allowlist par position, `ExecGradleToolTest` 6→17 tests, gate VERT 32 tests 0 FAIL)
. US-3 — ✅ S187 : C5 Timeout LLM (`withTimeout(llmTimeoutMs=30_000L)` sur 3 sites + catch `TimeoutCancellationException` → error/lastToolResult, constructor param propagé au StepVerifier via default param, 3 StepVerifierTest + 4 VibecodingGraphTest)
. US-4 — ✅ S187 : C2 Retry single counter (supprimé `retryCount = state.retryCount + 1` 2 sites + 3 `incrementRetry()` branches replan → 1 `incrementRetry()` début bloc error recovery, 1 VibecodingGraphTest)
. US-5 — ✅ S187 : Cucumber BDD (`codebase_vibe_hardening.feature` 4 scénarios `@vibe-hardening` + `VibeHardeningWorld` + `VibeHardeningSteps` 14 step defs préfixés "vibe hardening" + `VibeHardeningCucumberRunner` pattern S-082 + `CucumberTaskSpec("cucumberTestVibeHardening")` 37e, 4/4 PASSED 47s)

=== Résultats

* Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier : 64 tests 0 FAIL (17 ExecShellTool + 15 ExecGradleTool + 22 VibecodingGraph + 10 StepVerifier)
* Cucumber `cucumberTestVibeHardening` : 4/4 scénarios PASSED
* 3 fichiers prod modifiés (`ExecShellTool.kt`, `ExecGradleTool.kt`, `VibecodingGraph.kt`, `StepVerifier.kt` — signatures backward compat)
* Zéro bump BOM (EPIC local à codebase-plugin, contrats dans le même artefact)
* Zéro secret, zéro régression

== EPIC VIBE-HARDENING-2 — Durcissement Vibecoding P2/P3 (CR-GLOBALE-004 suite)

**Statut** : 🟡 EN COURS (2/8 US — US-0 ✅ S-188, US-1 ✅ S-189, US-2 ✅ S-189, reste US-3→US-8)
**Priorité** : P2/P3
**Origine** : CR-GLOBALE-004 (session 175) — 7 findings P2/P3 restants post-EPIC VIBE-HARDENING P1
**Cadrage** : `CADRAGE_EPIC_VIBE_HARDENING_2.adoc` (12 décisions architecturales)

=== Constats P2/P3 audités (7)

* **S2** (P2) — `TaskDiscoveryRegistrar` auto-registration libre `gradle_*` sans allowlist.
* **S3** (P2) — `write_file`/`edit_file` (`ToolRegistry.kt:125/138`) sans garde de taille (OOM potentiel).
* **C1** (P1 mais refonte) — `TaskResultVerifier` `contains("failed")` faux positif + `expectedOutput` jamais comparé.
* **C3** (P2) — `VibecodingGraph.kt:60-84` commentaires `INSERT-X1..X6` stale (EPIC X déjà fait).
* **C4** (P2) — `GradleTaskArgsMapper.buildCommand` (L18) flags `--key value` cassés vs `GRADLE_ARG_ALLOWLIST` US-2.
* **C6** (P3) — Duplication `SessionProtocolTask` (329 lignes) / `SessionProtocolServer` (158 lignes).
* **C7** (P3) — `VibecodingGraph.kt:464` `Plan remaining tasks` négatif possible.

=== US découpées (~21 pts, 8 US)

. US-0 — ✅ S188 : Cadrage EPIC (gouvernance pure, 7 constats + 12 décisions archi, ce document)
. US-1 — ✅ S189 : S2 Allowlist TaskDiscoveryRegistrar (18 unit + 6 réécrits, 4 pts) — `TASK_ALLOWLIST` + `TASK_DENYLIST` + `validateTaskName` companion. Tasks métier plugin (vibecode, qualityGate) ne sont plus auto-enregistrées (sécuritaire).
. US-2 — ✅ S189 : S3 Garde de taille write_file/edit_file MAX_WRITE_CHARS 1MB (+5 unit, 2 pts) — 3 checks (content, newString, resulting updated) → IAE avant writeText.
. US-3 — 🟡 À FAIRE : C4 GradleTaskArgsMapper flags cassés (~6 unit, 3 pts) — fail préexistant `GradleTaskArgsMapperTest > execute structured gradle task via handler` confirmé S-189.
. US-4 — 🟡 À FAIRE : C1 TaskResultVerifier refonte expectedOutput comparison + failed raffiné (~10 unit, 4 pts)
. US-5 — 🟡 À FAIRE : C3 Cleanup doc INSERT-X1..X6 (0 test, 1 pt)
. US-6 — 🟡 À FAIRE : C7 coerceAtLeast(0) remaining tasks (~1 unit, 1 pt)
. US-7 — 🟡 À FAIRE : C6 Extraction SessionProtocolEngine (~8 unit non-régression, 4 pts)
. US-8 — 🟡 À FAIRE : Cucumber BDD codebase_vibe_hardening_2.feature (~4 scénarios @vibe-hardening-2, 2 pts)

=== Ordre de priorité

P2 d'abord (US-1 S2, US-2 S3, US-3 C4), puis C1 (US-4 P1 refonte), puis P3 triviaux (US-5 C3, US-6 C7), puis P3 refactor (US-7 C6), puis Cucumber (US-8). US-1→US-7 indépendants. US-8 dépend de US-1→US-7.

=== Backward compat

Toutes les US préservent la backward compat (heuristique fallback conservée C1, MAX_WRITE_CHARS 1MB n'impacte aucun test S3, extension allowlist C4 accepte plus C4, etc.).

== EPIC V-FLYWAY — Migration Flyway (BACKLOG CONDITIONNEL)

**Statut** : BACKLOG
**Priorité** : P2
**Seuil déclencheur** : 5+ fichiers de migration de schéma

Le schema SQL est versionné avec la convention Flyway
(`src/main/resources/db/migration/V1__init_vibecoding.sql`) mais sans la dépendance
Flyway. Le `initSchema()` de SessionRepository gère les `CREATE TABLE IF NOT EXISTS`
directement en R2DBC. Quand le nombre de migrations dépassera 5, importer
`org.flywaydb:flyway-database-postgresql` et basculer sur `Flyway.migrate()`.

Actuellement 4 migrations (V1→V4). Non applicable.

== 🟡 EPIC FT-PIPELINE — Pipeline de Fine-Tuning N1 (Session 188 — Cadrage, 0/7 US)

**Statut** : 🟡 CADRÉ (7 US ~18 pts, US-0 ✅ S-188, US-1→6 À FAIRE)
**Priorité** : P0 vision (WORKSPACE_AS_PRODUCT.adoc:642-658)
**Source de conception** : `foundry/public/notebook-gradle/ARCHITECTURE.adoc` (vestige récupéré)
**Document** : `.agents/CADRAGE_EPIC_FT_PIPELINE.adoc`

Pipeline de fine-tuning N1 générique — fabrication modèle fine-tuné (dataset
prep + continual pre-training 10% corpus + export GGUF + push Ollama).
Boucle fabrication→exposition avec domaine `codebase.koog.expert` existant
(13 fichiers — `ExpertExposureTask`, `ExpertRegistry`, `ExpertAgent`).
TRN-8 training-gradle reformulé en recettes N2 consommant le pipeline.

*16 décisions architecturales* : EPIC codebase-only, domaine `codebase.finetuning`
(7ème sous `codebase`), `FineTuningPipeline` port + `FakeFineTuner`,
`OllamaFineTunerAdapter` (pattern `ChatModelTranscriptEnhancer`),
3 tâches Gradle (prepareFineTuningDataset/fineTuneExpert/publishExpertToOllama),
`FineTuningGraph` koog itératif (pattern `KoogPlanningGraph`), continual
pre-training 10% corpus (ACL 2024 arXiv 2311.08545), `AugmentedContext`
consommé, BYOK `api-key-pool` N0, output GGUF + manifest via
`ExpertExposureTask` (boucle fermée), `ValidationResult` sealed, zéro bump BOM,
pas de runtime Colab (CI/CD Gradle), Cucumber BDD `@finetuning`, budget pool
11437-11465, EPIC notebook-gradle caduc, TRN-8 reformulé recettes N2.

[cols="1,3,2,1]
|===
| US | Description | Tests | Pts

| US-0
| ✅ Cadrage EPIC — 16 décisions, 7 US, source notebook (ce document + CADRAGE_EPIC_FT_PIPELINE.adoc)
| 0
| 1

| US-1
| 🟡 À FAIRE : `FineTuningRequest` + `FineTuningResult` + `FineTuningPipeline` port + `FakeFineTuner` (domaine `codebase.finetuning` créé, pattern `TranscriptLlmEnhancer` port/fake). Invariants : baseModel non-blank, dataset non-empty, outputModelName non-blank, corpusRatio 0.0..1.0 default 0.10.
| ~8 unit
| 3

| US-2
| 🟡 À FAIRE : `OllamaFineTunerAdapter` — adapter bridge Ollama API (create model from Modelfile, push to registry). Pattern `ChatModelTranscriptEnhancer`. Fallback degraded : Ollama unavailable → `FineTuningResult.failure` (degraded garde dataset original, pas de crash).
| ~6 unit
| 3

| US-3
| 🟡 À FAIRE : `FineTuningGraph` — graphe koog `strategy<FineTuningState, FineTuningState>` 3 nœuds propose/train/validate + edges conditionnels (convergence score, backtracking, maxIterations default 3). Méthode `execute` séquentielle pattern `KoogAugmentedContextGraph`. `asMermaidDiagram()`.
| ~12 unit
| 4

| US-4
| 🟡 À FAIRE : Tâches Gradle `prepareFineTuningDataset` + `fineTuneExpert` + `publishExpertToOllama` (groupe `finetuning`). Extension `fineTuning { baseModel; dataset; outputModelName; corpusRatio; maxIterations }` 4 sources DSL (pattern `CapsuleConfig`).
| ~10 unit + 2 functional
| 4

| US-5
| 🟡 À FAIRE : Cucumber BDD `codebase_finetuning.feature` 4 scénarios `@finetuning` (pipeline complet, cycle itératif convergence, fallback Ollama unavailable, validation seuil). Runner dédié `FineTuningCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestFineTuning")` 38e.
| 4 scénarios
| 2

| US-6
| 🟡 À FAIRE : Cross-borough wiring — training-gradle TRN-8 reformulé en recettes CDA/FPA consommant `fineTuneExpert` de codebase. Dépendance N2→N1 propre. Documentation mise à jour (INDEX/BACKLOG training + codebase).
| 0 (gouvernance)
| 1
|===

Ordre : US-0 ✅ → US-1 → US-2 → US-3 → US-4 → US-5 → US-6.
Baby-step TDD/BDD strict. Gate ciblé : `./gradlew test --tests "codebase.finetuning.*"`.

== Kover Coverage

Session 079-080 (30 mai 2026). Kover configuré avec `koverThresholdCheck`.
*Couverture :* 85.81% → **100.00%** (55 classes métier, 0 missed).
*Exclusions :* 60+ classes excluded stratégiquement (DTOs, générés, tests).
*Commande :* `./gradlew :codebase-plugin:koverThresholdCheck`