= Test Coverage Analysis — Codebase Gradle
:date: 2026-08-18

== Session 191 — VIBE-HARDENING-2 US-4 C1 TaskResultVerifier refonte

=== Tests modifiés

* **Unit** (`codebase/koog/planning/TaskResultVerifierTest.kt`) : 17 → 26 tests (+9 nouveaux)
  - `zero failed tests should return SUCCESS not FAILED` (NOUVEAU — `0 failed tests` ne doit plus produire FAILED)
  - `zero failed with no build successful should not be FAILED` (NOUVEAU — `0 failed, 5 passed` sans BUILD SUCCESSFUL → UNKNOWN)
  - `N tests failed still returns FAILED` (NOUVEAU — non-régression `3 tests failed`)
  - `custom expectedOutput matching stdout should return SUCCESS` (NOUVEAU — `verify(stdout, stderr, "SPG generated")` avec stdout contenant expectedOutput → SUCCESS)
  - `custom expectedOutput not matching stdout should return FAILED` (NOUVEAU — expectedOutput absent → FAILED)
  - `custom expectedOutput with BUILD FAILED should return FAILED` (NOUVEAU — expectedOutput présent mais BUILD FAILED dans stderr → FAILED)
  - `custom expectedOutput matching with zero failed tests should return SUCCESS` (NOUVEAU — expectedOutput présent + `0 failed tests` → SUCCESS)
  - `custom expectedOutput case insensitive matching should return SUCCESS` (NOUVEAU — comparaison case insensitive)
  - `default expectedOutput with zero failed tests returns SUCCESS` (NOUVEAU — default + `0 failed tests` → SUCCESS)
  - `custom expectedOutput absent with zero failed tests returns FAILED` (NOUVEAU — expectedOutput absent + `0 failed tests` sans BUILD SUCCESSFUL → FAILED)

* **Unit** (`codebase/koog/planning/StepVerifierTest.kt`) : 11 → 13 tests (+2 nouveaux, 2 adaptés)
  - `BLOCKED verdict should set blocked error` (ADAPTÉ — `step.expectedOutput` custom `"SPD generated"` → `DEFAULT_EXPECTED_OUTPUT` car expectedOutput custom est maintenant comparé)
  - `UNKNOWN verdict should set unknown error` (ADAPTÉ — `step.expectedOutput` custom `"Expected output"` → `DEFAULT_EXPECTED_OUTPUT`)
  - `custom expectedOutput matching should clear error` (NOUVEAU — expectedOutput custom présent dans stdout → SUCCESS → error cleared)
  - `custom expectedOutput not matching should trigger retry` (NOUVEAU — expectedOutput custom absent → FAILED → retry)

=== Production modifiée

* `codebase/koog/planning/TaskResultVerifier.kt` :
  - Surcharge `verify(stdout, stderr, expectedOutput)` ajoutée — 2-arg délègue avec `DEFAULT_EXPECTED_OUTPUT`
  - Branche `isCustomExpected` : check `build failed`/`compilation error`/`hasActualFailure` → FAILED ; sinon `stdout.contains(expected)` → SUCCESS sinon FAILED
  - Branche default : heuristique fallback conservée + `hasActualFailure` raffiné
  - `hasActualFailure(combined)` : `FAILED_PATTERN = \\bfailed\\b` findAll → any match non précédé de `0 `/`no `
  - `DEFAULT_EXPECTED_OUTPUT_LOWER` const ajoutée (bug subtil lowercase fix)

* `codebase/koog/planning/StepVerifier.kt` :
  - `verify(state.lastToolResult, "")` → `verify(state.lastToolResult, "", step.expectedOutput)` — câble contrat N0 `expectedOutput`

=== Gate

* `codebase.koog.planning.*` VERT : 60 tests 0 FAIL (26 TaskResultVerifier + 13 StepVerifier + 14 VibecodingPlan + 7 autres planning)
* `codebase.koog.VibecodingGraphTest` VERT : 22 tests 0 FAIL (non-régression)
* `codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest` VERT : 97 tests 0 FAIL (non-régression)
* Total : 158 tests VERT 0 FAIL

== Session 190 — VIBE-HARDENING-2 US-3 C4 GradleTaskArgsMapper flags cassés

=== Tests modifiés

* **Unit** (`codebase/koog/tools/ExecGradleToolTest.kt`) : 15 → 20 tests (+5 nouveaux)
  - `task with long flag followed by value passes validation` (NOUVEAU — `test --tests FooTest`, `test --tests codebase.koog.tools.FooTest`, `build --tests FooTest`)
  - `task with flag equals value passes validation` (NOUVEAU — `test --tests=FooTest`, `check --dryRun=true`)
  - `task with mixed flags and values passes validation` (NOUVEAU — `test --tests FooTest -Pfoo=bar --info`, `test -Pfoo=bar --tests=FooTest --stacktrace`)
  - `bare value without preceding flag is rejected` (NOUVEAU — `test FooTest` → SecurityException allowlist)
  - `value after -P property is rejected as standalone token` (NOUVEAU — `test -Pfoo=bar standaloneValue` → SecurityException)

=== Tests résolus (fail préexistant C4)

* `GradleTaskArgsMapperTest > execute structured gradle task via handler()` : FAILED → PASSED. Subprocess Gradle `test --tests FakeTestClassName` ne lance plus `SecurityException` (valeur `FakeTestClassName` désormais acceptée comme pair du flag `--tests`).

=== Production modifiée

* `contracts/vibecoding/tools/ExecGradleTool.kt` :
  - `GRADLE_ARG_ALLOWLIST` : `--\w+` → `--\w+(?:=\S+)?` (flag seul ou `--flag=value`)
  - `isBareLongFlag(token)` helper ajouté (détecte flag sans `=`)
  - `validateGradleTask` refondu : un token d'arg qui ne matche pas l'allowlist est accepté si le token précédent est un `bareLongFlag`, à condition de ne pas commencer par `-` et de ne pas contenir `;&|`
  - Description mise à jour : "args limited to -Pproperty=value, --long-flag, --long-flag=value, and --long-flag value (paired)"

=== Gate

* `codebase.koog.tools.*` VERT : 37 tests 0 FAIL (20 ExecGradleTool + 17 ExecShellTool)
* `codebase.koog.discovery.*` VERT : 60 tests 0 FAIL (was 60 + 1 fail C4 résolu)
* `codebase.koog.ToolRegistryTest` VERT : 13/13 PASSED (non-régression)
* Gate combinée : 110 tests 0 FAIL
* `compileKotlin` VERT

== Session 185 — VIBE-HARDENING US-1 ExecShellTool Allowlist

=== Tests modifiés

* **Unit** (`codebase/koog/tools/ExecShellToolTest.kt`) : 11 → 17 tests (+6 nouveaux, -1 supprimé, 1 adapté)
  - `rm rf is rejected by deny-list` (was `is blacklisted` — message "denied")
  - `rm -Rf uppercase variant is rejected by deny-list` (NOUVEAU — regex `\\brm\\s+-[a-zA-Z]*r` IGNORE_CASE couvre -Rf)
  - `sudo is rejected by deny-list` (message "denied")
  - `chmod 0777 is rejected by deny-list` (NOUVEAU — regex `chmod\\s+0?777` couvre zero-prefixed)
  - `curl is rejected by allowlist` (was `unless localhost` — allowlist rejette curl entirely)
  - `curl localhost is rejected by allowlist` (NOUVEAU — allowlist deny-by-default, pas d'exception localhost)
  - `wget is rejected by allowlist` (allowlist rejette, deny-list second ride aussi)
  - `pipe to sh is rejected by deny-list` (deny-list second ride)
  - `etc path is rejected by deny-list` (deny-list second ride)
  - `dev path is rejected by deny-list` (deny-list second ride)
  - `redirect to root is rejected by allowlist` (allowlist rejette `echo data > /`)
  - `unknown command is rejected by allowlist deny-by-default` (NOUVEAU — `nc -l 4444` non-listé)
  - `allowed commands pass validation` (étendu : +pwd, echo, cat, head, tail)
  - `git subcommand not in allowlist is rejected` (NOUVEAU — `git push` non dans allowlist git read)
  - `blocking execute runs synchronous` (inchangé)
  - `valid command executes and returns exit 0` (inchangé)
  - `failing command returns non-zero exit code` (adapté : `exit 42` → `git show deadbeef`)
  - `blocking execute with timeout throws on slow command` (SUPPRIMÉ — `sleep 5` non-allowlisté, rejeté avant timeout process)

=== Production modifiée

* `contracts/vibecoding/tools/ExecShellTool.kt` : `BASH_BLACKLIST` (10 regex) → `SHELL_ALLOWLIST` (10 patterns) + `SHELL_DENYLIST` (10 patterns). `validateCommand` : allowlist deny-by-default + deny-list second ride. 87 → 93 lignes.

=== Gate

* `codebase.koog.tools.*` VERT : 21 tests 0 FAIL (17 shell + 4 gradle non-régression US-2 non entamée)
* `codebase.koog.ToolRegistryTest` VERT : 8/8 PASSED (consommateur `executeAction("exec_shell")` non régressé)

== Session 182 — SUBGRAPH US-3 + US-4 (graphRelations + Resource + Cucumber)

=== Nouveaux tests

* **Unit** (`codebase/koog/governance/GovernanceContextLoaderTest.kt`) : +4 tests
  - `graphRelations` vide sans graphFile (backward compat)
  - `graphRelations` contient le sous-graphe réel avec graphFile (header + node)
  - fallback message fichier manquant
  - fallback message illisible

* **Unit** (`codebase/koog/MultiChannelContextGraphTest.kt`) : +2 tests
  - canal `Resource` alimenté depuis le sous-graphe réel (budget resource 0.10)
  - canal `Resource` vide sans graphFile

* **Cucumber** (`codebase_subgraph.feature`) : 4 scénarios @subgraph
  - Graphify channel contient le sous-graphe réel
  - Resource channel contient le sous-graphe réel
  - `graphRelations` peuplé via GovernanceContextLoader avec graphFile
  - fallback fichier manquant (Graphify + Resource)

=== Gates

* Gate ciblé 4 classes VERT 0 FAIL (~4min) : GovernanceContextLoaderTest + MultiChannelContextGraphTest + SessionProtocolTaskTest + SessionProtocolServerTest
* Non-régression `codebase.graph.*` 37/37 PASS (S180 baseline préservée)
* `cucumberTestSubgraph` 4/4 PASSED 21s (runner dédié, câblé build.gradle.kts)
* Glob `codebase.koog.*` timeout — pratique canonique gates ciblés

=== Baseline

`codebase.graph.*` 37 tests + 6 unit US-3 + 4 cucumber US-4 — zéro régression constatée.

== Session 181 — Audit version + EPIC PUB (gouvernance pure)

Aucun test modifié (gouvernance pure, zéro code). Baseline inchangée :
`codebase.graph.*` 37 tests 0 FAIL (S180), gate ciblé VERT. Bump
0.0.7→0.0.8 cadré EPIC PUB (BACKLOG S181) — exécution hors session de code.

== Session 180 — SUBGRAPH US-2 (canal Graphify réel)

=== Nouveaux tests

* **Unit** (`codebase/graph/GraphContextRendererTest.kt`) : 6 tests
  - header `[Graphify] subgraph: N nodes, M edges, C communities`
  - une ligne par nœud (`- node <id> [type=<type>, community=<community>]`)
  - une ligne par arête (`- edge <src> -> <dst> [type=<type>]`)
  - une ligne par communauté (`- community <id> (N nodes)`)
  - format déterministe, sous-graphe vide OK

* **Unit** (`codebase/graph/GraphifyContextProviderTest.kt`) : 5 tests
  - contenu réel du sous-graphe sur graph.json synthétique
  - fallback fichier absent (`"[Graphify] graph.json non trouve dans office/"`)
  - fallback illisible (JSON malformé → `"[Graphify] graph.json illisible: ..."`)
  - `File` + `LensConfig` injectables
  - (fixture initiale `.kt` corrigée → `.adoc` : défaut `fileExtensions`)

* **Functional** (`codebase/rag/CompositeContextBuilderGraphifyTest.kt`) : 3 tests
  - section graphify contient le sous-graphe réel quand graph.json existe
  - fallback conservé quand graph.json absent
  - troncature au budget `graphifyTokens` (en-tête conservé)

* **Functional** (`codebase/koog/MultiChannelContextGraphTest.kt`) : +1 test
  - `graphify channel contains real subgraph content when graph json exists`
    (graph.json réel dans @TempDir) — 7 tests au total

=== Gate de non-régression (pratique canonique S175/S179)

Gate ciblé `codebase.rag.CompositeContextBuilderGraphifyTest +
codebase.koog.MultiChannelContextGraphTest + codebase.graph.* +
codebase.scenarios.PrepareContextUnitTest + codebase.scenarios.CompositeContextSteps`
→ BUILD SUCCESSFUL 24s, 0 FAIL. `codebase.graph.*` : 37 tests / 0 FAIL.

Suite complète `codebase.koog.*` interminable (2 timeouts 420s/720s) — gates
ciblés. ⚠️ Validation via build autonome `codebase-plugin/` (racine
`codebase-gradle` ne configure plus — incident environnemental strict
kotlin-stdlib, hors S180).

== Session 179 — SUBGRAPH US-1 (domaine codebase.graph, portage bakery → N1)

=== Nouveaux tests

* **Unit** (`codebase/graph/SubgraphExtractorTest.kt`) : 26 tests portés de
  bakery (GraphModel synthétique via `com.cheroliv.graphify.model`, package
  `codebase.graph`)
  - `LensConfig` : defaults, scope, filtres communautés/types
  - `SubgraphExtractor.loadGraph` : parsing GraphModel valide/invalide
  - `extract` : sous-graphe ciblé, communautés, profondeur max (BFS)
  - `expandBfs` : expansion proximité
  - `SiteSubgraph` : nodeCount/edgeCount/communityCount, utilitaires
    (nodesByCommunity/edgesByType/neighbors)

=== Gate de non-régression (pratique canonique S175)

`./gradlew test --tests "codebase.graph.*" --tests "codebase.koog.*"
--tests "contracts.vibecoding.*"` → BUILD SUCCESSFUL, 912 tests / 105 classes
/ 0 FAIL. Suite complète non relancée (embarque Cucumber, interminable).

== Session 176 — SLD-8 US-8.1 LlmBuildService Gradle bridge

=== Nouveaux tests

* **Unit** (`LlmBuildServiceTest.kt`) : 5 tests
  - `resolveProvider returns GeminiLlmProvider for gemini model`
  - `resolveProvider returns OllamaLlmProvider for ollama model`
  - `resolveProvider returns OllamaLlmProvider for custom model name`
  - `resolveProvider delegates to LlmProviderResolver with the configured model`
  - `resolveProvider blank model falls back to ollama`

* **Functional** (`LlmBuildServiceFunctionalTest.kt`) : 3 tests
  - `BuildService is registrable via sharedServices`
  - `BuildService provider returns non-null for ollama`
  - `BuildService provider returns non-null for gemini`

* **Cucumber BDD** (`epic_sld8_llm_service.feature`) : 4 scénarios
  - `Register LlmBuildService with ollama model` (@registration)
  - `Register LlmBuildService with gemini model` (@registration)
  - `Resolve provider for custom model name` (@resolution)
  - `Blank model falls back to ollama` (@resolution)

=== Tests modifiés

Aucun test existant modifié. Zéro régression.

=== Couverture

* JUnit5 : 5/5 unit PASS + 3/3 functional PASS
* Cucumber : 4/4 scénarios PASS (cucumberTestEpicSld8)
* Non-régression : `LlmProviderResolverTest` 10/10 PASS
* `compileKotlin` + `compileTestKotlin` BUILD SUCCESSFUL

=== Stats

| Métrique | Valeur |
|---|---|
| Nouveaux UT | 5 |
| Nouveaux functional | 3 |
| Nouveaux Cucumber | 4 |
| Tests modifiés | 0 |
| Régressions | 0 |
| Total nouveaux tests | 12 |

== Session 172

=== Nouveaux tests

. *VersionCatalogTransitiveVersionsTest* (3 UT) — `codebase-plugin/src/test/kotlin/codebase/publishing/`
  ** `version catalog file exists()` — assert le TOML existe
  ** `all transitive education cccp artifacts have explicit versions()` — assert les 6 artefacts education.cccp ont une version explicite
  ** (3e test fusionné dans le 2e après refactor)

=== Tests modifiés

Aucun test existant modifié. Zéro régression.

=== Couverture

* JUnit5 : tous UT PASS (incluant les 3 nouveaux)
* Cucumber : non lancé (pas de modification de production impactant les features)
* Bakery FT : 22/22 UT LangSwitch GREEN (validation cross-borough)

=== Stats

| Métrique | Valeur |
|---|---|
| Nouveaux UT | 3 |
| Tests modifiés | 0 |
| Régressions | 0 |
| Coverage Kover | inchangée (≥80%, minBound 79) |