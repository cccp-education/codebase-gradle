= PROMPT_REPRISE — Codebase (Queens N1) — Session 188
:session: 188
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 187 — EPIC VIBE-HARDENING TERMINÉ 6/6 US (US-3 timeout LLM withTimeout 30s sur 3 sites callLLMNode/replan/StepVerifier + US-4 retry single counter suppression double incrémentation executePlanTasks + bloc error + US-5 Cucumber BDD 4 scénarios @vibe-hardening). Gate `codebase.koog.tools.*` VERT 64 tests 0 FAIL + 4/4 cucumber PASSED. Zéro bump BOM. Borough DORMANT.

== Contexte

- ✅ EPIC VIBE-HARDENING TERMINÉ 6/6 US (S-184→S-187) — durcissement vibecoding (allowlist shell+gradle deny-by-default, timeout LLM 30s, retry single counter, Cucumber BDD)
- ✅ EPIC SUBGRAPH TERMINÉ 4/4 US (S-178-182)
- ✅ EPIC PUB TERMINÉ 3/3 US (S-183) — codebase-plugin 0.0.8 Maven Central
- Version : codebase-plugin 0.0.8 Central
- BOM Central 0.0.18 contient constraint codebase-plugin 0.0.5 — bump BOM MEMPHIS (hors scope)
- ⚠️ Build via `codebase-plugin/` autonome (racine `codebase-gradle` ne configure plus)

== Mission Session 188

Borough Queens DORMANT. Aucune mission prioritaire — attendre déclencheur pilote.

Déclencheurs possibles :
- EPIC VIBE-HARDENING-2 (P2/P3 : S2 auto-registration libre, S3 edit/write taille garde, C1 TaskResultVerifier scoring sémantique, C3 VibecodingPlan dead code, C4 GradleTaskArgsMapper flags, C6 duplication SessionProtocolServer, C7 Plan remaining négatif)
- Cross-borough : un borough N2 qui demande une évolution du socle N1
- Maintenance : bump version, CVE, dépendance cassée

== Rappels

- BABY-STEP TDD strict si code — jamais de code sans test rouge d'abord
- 1 fichier à la fois — modifier, valider (`compileTestKotlin`), puis suivant
- Anglais uniquement dans le code (OSS Apache 2.0)
- Gate tests ciblé, JAMAIS `./gradlew test` global (Cucumber interminable)
- Ports Ollama 11437-11465 (jamais 11434-11436)
- Zéro bump BOM sauf si contrats N0 modifiés (EPIC local = pas de bump)