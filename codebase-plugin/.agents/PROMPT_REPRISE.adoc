= Prompt de Reprise — Codebase (Queens N1) — Session 266
:session: 266
:borough: Queens — codebase-gradle (N1, socle LLM pool + RAG pgvector + SVO)
:session_precedente: 265 — **CB-PAGE-PROVENANCE US-1 (N1 transport) ✅** — `DocumentChunk.pages` + `StoreStatements.provenanceSchema()`/`initSchema` pages TEXT + `insertChunkWithDoubt` bind 8 (bind count 7→8) + `RagVectorStore` bind + SELECT read/parse (`searchSimilar` + `searchSimilarWithDoubt`) + `RetrieveResult.pages`. TDD strict RED (6 `Unresolved reference 'pages'` + functional `No parameter with name 'pages'`) → GREEN. Round-trip réel testcontainers `RagVectorStorePageProvenanceTest` 2/2 + gate `@doubt-quality` **9/9** (step feature `binds 7 → 8`) + `@corpus-ingest` 4/4 + `@rag-socle` 6/6 + `publishToMavenLocal` VERT (0.0.16).

== Mission Session 266 — à définir (ordre pilote)

Nombreux candidats (critical path MVP0) :

. **CB-PAGE-PROVENANCE US-2 (N2 transport codex, Brooklyn)** — prochaine US
   de l'EPIC dans l'ordre du cadrage (US-1 → US-2 → US-3 → US-4). Session
   trans borough : prévoir cadrage + implémentation côté codex
   (`CodexIngestTask.pageProvenanceFile` `@InputFiles` tolérante + jointure
   exacte SHA-256 + attach avant `ingestWithDoubt`).
. **Génération réelle A2** (accord pilote — LLM metered) : `autonomousSession`
   sur training/Newark avec le RAG peuplé + port planner prouvé S-264.
   Première mission réelle qui consomme le contexte augmenté.
. **RENAME Vague 4 transverse** (bakery → capsule → plantuml → graphify/codex,
   chacune transfusion `TRANSFER_LEXIQUE_FORMATION.adoc` d'abord).

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...` (JDK 25).
* Version locale **0.0.16** (Central 0.0.16, bundle `db5e1158`). BOM/catalog
  **0.0.50**. `publishToMavenLocal` VERT S-265.
* **EPIC CB-PAGE-PROVENANCE** : US-0 ✅ (cadrage S-225), **US-1 ✅ S-265**,
  US-2 → US-4 🟡. Doc `CADRAGE_EPIC_CB_PAGE_PROVENANCE.adoc` (côté codex).
  Prochaine : US-2 (N2 transport, Brooklyn).
* `codebase.store.*` VERT (Round-trip réel + 3 suites Cucumber dédiées).
* 45 cucumber runners dédiés ; koverVerify VERT (check complet 27m06s S-264).
* Wiring SVO prouvé S-264 : `autonomousSession` + `PlannerPort` (planner
  sous-agent), task codebase `generateAugmentedPlan`.

== Échecs préexistants (orthogonaux, ne pas confondre)

* 2 × `OcrTaskTest` (`detectMimeType octet-stream for pdf`, `isImageFile
  false for pdf`) — drift S-211, **stash-confirmés identiques à HEAD S-264**.
* Suite globale `test` non relancée S-265 (delta confiné `codebase.store`,
  preuve round-trip réel + suites dédiées) — relance complète si un prochain
  delta la justifie.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la fois.
* Git autorisé (décision pilote 2026-09-17) — zéro secret commité.
* Publication Central = accord pilote explicite (Règle 2).
* Dogfood-first : la preuve réelle prime sur les fakes (S-264).

== References

* `.agents/sessions/265-cb-page-provenance-us1.adoc`
* `.agents/sessions/264-dogfood-a2-2-bugs-integration-rename-v4-doc.adoc`
* `codex-gradle/codex-plugin/.agents/CADRAGE_EPIC_CB_PAGE_PROVENANCE.adoc`
* `training-gradle/.agents/TRANSFER_LEXIQUE_FORMATION.adoc` (transfusion)