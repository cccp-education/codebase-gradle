= PROMPT_REPRISE — Codebase (Queens N1) — Session 184
:session: 184
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 183 — EPIC PUB TERMINÉ 3/3 US : publication Maven Central codebase-plugin 0.0.8 (nmcp AUTOMATIC, bundle `4ae55e92`, HTTP 200 repo1). US-0.0.8-1 POM/signing/release deps vérifiés (zéro SNAPSHOT). US-0.0.8-2 `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL 28s. US-0.0.8-3 tags néant + BOM non bumpé (MEMPHIS). EPIC SUBGRAPH ✅ (S178-182) — contexte augmenté Graphify réel partout.

== Contexte

- ✅ EPIC SUBGRAPH TERMINÉ 4/4 US (S178 cadrage, S179 domaine codebase.graph, S180 canal Graphify, S182 US-3+US-4) — contexte augmenté Graphify réel : canal Graphify + canal Resource + graphRelations
- ✅ EPIC PUB TERMINÉ 3/3 US (S183) — codebase-plugin 0.0.8 publié Maven Central (bundle 4ae55e92, HTTP 200)
- Domaine `codebase.graph` : LensConfig + SubgraphExtractor + SiteSubgraph + GraphContextRenderer + GraphifyContextProvider (graphFile/lensConfig injectables), dépendance `education.cccp:graphify-plugin:0.0.2` (WORKAROUND package `com.cheroliv.graphify.model`)
- Version : codebase-plugin 0.0.8 Central (0.0.7 → 0.0.8, commits post-0.0.7 propagés : SUBGRAPH US-1→4)
- BOM Central 0.0.18 contient constraint codebase-plugin 0.0.5 — bump BOM à la discrétion MEMPHIS (hors scope Queens)
- ⚠️ Incident environnemental : racine `codebase-gradle` ne configure plus (conflit strict kotlin-stdlib 2.3.21 embedded vs workspace-bom) — valider via le build autonome `codebase-plugin/`

== Mission Session 184

Backlog : EPIC VIBE-HARDENING (P1, issu CR-GLOBALE-004) — durcissement vibecoding :
. P1 : S1 allowlist deny-by-default, C2 retry un seul compteur, C5 timeout LLM
. P2 : C3/C4, S2/S3
Ou retour borough DORMANT (maintenance uniquement) si non priorisé.

## Rappels

- BABY-STEP TDD strict — jamais de code sans test rouge d'abord
- Anglais uniquement dans le code (OSS Apache 2.0)
- Gate tests ciblé, JAMAIS `./gradlew test` global (Cucumber interminable)
- Ports Ollama 11437-11465 (jamais 11434-11436)