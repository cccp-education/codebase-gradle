= Prompt de Reprise — Codebase (Queens N1) — Session 208
:session: 208
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 207 — Migration .agents/ racine → codebase-plugin/.agents/ (pattern S-105 codex). Un seul dossier gouvernance désormais, 198 sessions, archives fusionnées. Baseline préservée (GovernanceContextLoaderTest 7/7, EndSessionBlogTaskTest 2/2, zéro source modifiée).

== Mission Session 208

Borough DORMANT — tous EPICs terminés (V-9, OCR, CR, VIBE-HARDENING,
VIBE-HARDENING-2, FT-PIPELINE, CDX-RAG-SOCLE, CDX-OCR-CONTRACTS,
CB-STORE-UNIFY CADUQUE). Maintenance uniquement.

=== Options

. **Maintenance** : publication, code review, dette technique ciblée.
. **Cadrer un nouvel EPIC** si besoin identifié (ex: extension RagVectorStore
  pour ingestion documentaire côté socle, si codex délègue plus).
. **Cross-borough** : assister un autre borough (Brooklyn codex,
  Manhattan planner, etc.) si demandé.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* codebase-plugin 0.0.9 = Maven Central (S-204). Dep N0 `ocr-contracts:0.0.1`.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* 41 cucumber runners dédiés dans `build.gradle.kts` (pattern S-082).
* `VectorStore.initSchema()` = non-destructeur (S-206). Tests use `clean()`.
* Gouvernance unifiée dans `codebase-plugin/.agents/` (S-207).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.