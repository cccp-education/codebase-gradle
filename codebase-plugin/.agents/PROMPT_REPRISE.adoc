= Prompt de Reprise — Codebase (Queens N1) — Session 259
:session: 259
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 258 — SVO-3 TERMINÉ (cross-borough Manhattan : adapter PlannerPort + BuildService `plannerPortService`)

== Mission Session 259

EPIC SVO 🟡 EN COURS — US restantes :

. **SVO-4** (3 pts, Queens) : tâche `autonomousSession` — resolve prompt
   (`--prompt` > défaut borough via CompositeContextBuilder.buildScoped +
   GovernanceContextLoader) → `PlannerPort` (résolution par nom canonique
   `plannerPortService` via gradle.sharedServices + fallback NoOp —
   `PlannerPortResolver` object pur testable) → boucle `VibecodingGraph` →
   état d'avancement (SessionRepository + audit JSONL) → resumption prompt
   (SVO-2 SessionResumptionPlanner) écrit dans `build/vibecoding/`.
   Options `--prompt`, `--borough`, `--maxChainedSessions` (défaut 3, D9).
. **SVO-5** (3 pts) : chaînage brainstorming si backlog fini
   (BacklogDetector.isComplete) + Cucumber BDD `autonomous_session.feature`
   4 scénarios (prompt transféré, défaut borough, backlog restant →
   resumption, backlog fini → brainstorming) — fakes in-memory pattern
   S-082/S-088.

=== Alternatives (sur demande pilote)

. **RENAME vague document** (transfusion d'abord, 3 pts + OCR-QUALITY US-3).
. **Maintenance** : publication, code review, dette technique.

== Contexte technique

* SVO-1 ✅ S-256 : `PlannerPort` fun interface (`codebase.koog.plannerport`,
  fallback NoOp) branché KoogAugmentedContextGraph/KoogPlanningOrchestrator,
  `PlannerIntegration.kt` supprimé, `libs.planner.plugin` retiré — cycle
  N1↔N2 mort.
* SVO-2 ✅ S-257 : domaine `codebase.koog.svo` — `BacklogDetector` (parse
  checkboxes + tables AsciiDoc, stale marker, héritage heading neutre,
  isComplete) + `SessionResumptionPlanner` (prompt déterministe sans LLM,
  BRAINSTORM si vide). 24 unit.
* SVO-3 ✅ S-258 : côté Manhattan — `planning.adapter` :
  `KoogIntentionPlannerAdapter` implémente le port,
  `PlannerPortBuildService`/`PlannerPortRegistry` exposé sous le nom
  canonique **`plannerPortService`**, wiring `registerPlannerPort`.
  planner-plugin 0.0.2 republicié mavenLocal.
* Gate ciblé : jamais `./gradlew test` global sans permission (2 fails
  préexistants OcrTaskTest drift pdf S-211). Suites ciblées :
  `--tests "codebase.koog.svo.*"` etc.
* Gouvernance unifiée dans `codebase-plugin/.agents/` (S-207).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.