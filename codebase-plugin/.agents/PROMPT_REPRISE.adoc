= Prompt de Reprise — Codebase (Queens N1) — Session 195
:session: 195
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 194 — EPIC FT-PIPELINE US-2 TERMINÉ (port `FineTuningPipeline` + `FineTuningRequest`/`FineTuningResult` sealed + `FakeFineTuner` + `OllamaFineTunerAdapter` + port interne `OllamaRegistryClient` + impl HTTP `OllamaHttpRegistryClient`). Baby-step TDD strict. 7 fichiers créés (6 prod + 1 fake test + 2 test). 43/43 unit PASSED (8 config + 19 merger + 11 pipeline + 5 adapter). `compileKotlin` VERT, zéro régression, zéro secret, zéro bump BOM. EPIC FT-PIPELINE 2/7 US.

== Mission Session 195

EPIC FT-PIPELINE en cours (2/7 US). Prochaine US-3.

=== Option A — EPIC FT-PIPELINE US-3 (P0 vision, graphe koog itératif)

US-3 (4 pts, ~12 unit) : `FineTuningGraph` — graphe koog
`strategy<FineTuningState, FineTuningState>` 3 nœuds propose/train/validate
+ edges conditionnels (convergence score, backtracking, maxIterations
default 3). Méthode `execute` séquentielle pattern
`KoogAugmentedContextGraph`. `asMermaidDiagram()`. Domaine
`codebase.finetuning` enrichi. Baby-step TDD strict. Document :
`.agents/CADRAGE_EPIC_FT_PIPELINE.adoc` US-3.

=== Option B — Maintenance / déclencheur pilote

Borough DORMANT jusqu'à déclencheur pilote.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) — US-1 ✅ S-193, US-2 ✅
  S-194, US-3→6 À FAIRE.
* Domaine `codebase.finetuning` (7ème) — 6 prod + 2 test + 1 fake
  (FineTuningConfig, FineTuningConfigMerger, FineTuningRequest,
  FineTuningResult, FineTuningPipeline, OllamaRegistryClient,
  OllamaHttpRegistryClient, OllamaFineTunerAdapter + FakeFineTuner test).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.