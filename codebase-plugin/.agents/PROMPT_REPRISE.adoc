= Prompt de Reprise — Codebase (Queens N1) — Session 265
:session: 265
:borough: Queens — codebase-gradle (N1, socle LLM pool + RAG pgvector + SVO)
:session_precedente: 264 — Dogfood Newark A2 ✅ (2 bugs P1 d'intégration découverts+corrigés : `PlannerPort` jamais consommé → NoOp silencieux ; collision `generatePlan` codebase↔planner bloquant SVO D5 → task codebase renommée `generateAugmentedPlan`) + RENAME Vague 4 document ✅ (cross-borough, transfusion + neutralisation)
:session_pre_precedente: 263 — Fix rotation pool Ollama Cloud (`usage limit`) + test d'intégration plage réelle

== Mission Session 265 — à la demande du pilote

Ordre pilote S-264 : **4 → 1 → 3** — **les 3 missions ✅ TERMINÉES**.
Mission 3 : **publication codebase-plugin 0.0.16 Central** (accord pilote
obtenu) + bump cascade MEMPHIS (BOM/catalog **0.0.50**) — bundle MEMPHIS
`547e3e46`, bundle codebase `db5e1158` (AUTOMATIC).

Missions candidates (ordre critical path MVP0) :

. **Génération réelle A2** (sur accord pilote — LLM metered) : lancer
   `autonomousSession` sur training/Newark avec le RAG référentiel peuplé
   + port planner résolu (prouvé S-264). C'est la première mission réelle
   qui consomme le contexte augmenté.
. **RENAME Vague 4 transverse** (suite document ✅ S-264) : bakery →
   capsule → plantuml → graphify/codex, chacune transfusion d'abord
   (`TRANSFER_LEXIQUE_FORMATION.adoc`).
. **Drift gouvernance racine** : ✅ corrigé S-264 (PROMPT_REPRISE racine
   synchronisé + synthèse BACKLOG racine mis à jour). Le drift catalogue
   local codebase (`workspace-bom 0.0.27` → **0.0.50**) a été réaligné au
   bump S-264.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...` (JDK 25).
* Version locale **0.0.16** (Central 0.0.16, bundle `db5e1158`). Conventions
  0.0.4. BOM/catalog **0.0.50**. `publishToMavenLocal` VERT S-264.
* **Wiring SVO prouvé** : `AutonomousSessionTask.resolvePlannerPort()` résout
  le `PlannerPort` par nom canonique `plannerPortService` (adapter planner
  `KoogIntentionPlannerAdapter`) ; NoOp dégradé si rien n'est branché.
* **Contrat cross-borough** : un consommateur applique `education.cccp.codebase`
  + `education.cccp.planner` (les 2 coexistent depuis S-264). codebase
  `generateAugmentedPlan` (ex-`generatePlan`) ; planner garde `generatePlan`.
* 45 cucumber runners dédiés (dont `cucumberTestAutonomousSession`).
* koverVerify VERT (check complet 27m06s S-264).
* Catalogue local codebase : `workspace-bom = "0.0.50"` (realigné S-264,
  drift 0.0.27 → 0.0.50).

== Échecs préexistants (orthogonaux, ne pas confondre)

* 2 × `OcrTaskTest` (`detectMimeType octet-stream for pdf`, `isImageFile
  false for pdf`) — drift S-211, **stash-confirmés identiques à HEAD S-264**.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la fois.
* Git autorisé (décision pilote 2026-09-17) — zéro secret commité.
* Publication Central = accord pilote explicite (Règle 2).
* Dogfood-first : exercer le wiring cross-borough réel (le dogfood S-264 a
  révélé 2 bugs invisibles aux tests unitaires).

== References

* `.agents/sessions/264-dogfood-a2-2-bugs-integration-rename-v4-doc.adoc`
* `.agents/CADRAGE_EPIC_SVO_SESSION_ORCHESTREE.adoc` (cadrage S-255)
* `training-gradle/.agents/TRANSFER_LEXIQUE_FORMATION.adoc` (transfusion)
