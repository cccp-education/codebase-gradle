= Prompt de Reprise — Codebase (Queens N1) — Session 267
:session: 267
:borough: Queens — codebase-gradle (N1, socle LLM pool + RAG pgvector + SVO)
:session_precedente: 266 — Analyse et préparation pour le lancement d'autonomousSession (Mission Génération réelle A2) : tâche non trouvée en raison de problèmes de configuration du build multi-projet. Analyse du code et des tests effectués.
:session_pre_precedente: 265 — CB-PAGE-PROVENANCE US-1 (N1 transport) ✅

== Mission Session 267 — à la demande du pilote

Ordre pilote S-266 : **1 → 2 → 3** — 
Mission 1 : **Génération réelle A2** (sur accord pilote — LLM metered) : 
    Travailler directement dans le projet codebase-plugin (sans includeBuild dans le racine) pour lancer la tâche autonomousSession.
    Utiliser la commande : `./gradlew autonomousSession --borough=newark --maxChainedSessions=1 --no-daemon`
    Si la tâche réussit, passer aux missions suivantes.
    C'est la première mission réelle qui consomme le contexte augmenté.
Mission 2 : **RENAME Vague 4 transverse** (suite document ✅ S-264) : bakery → capsule → plantuml → graphify/codex, chacune transfusion d'abord (`TRANSFER_LEXIQUE_FORMATION.adoc`).
Mission 3 : **Drift gouvernance racine** : ✅ corrigé S-264 (PROMPT_REPRISE racine synchronisé + synthèse BACKLOG racine mis à jour). Le drift catalogue local codebase (`workspace-bom 0.0.27` → **0.0.50**) a été réaligné au bump S-264.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...` (JDK 25).
* Version locale **0.0.16** (Central 0.0.16, bundle `db5e1158`). Conventions 0.0.4. BOM/catalog **0.0.50**. `publishToMavenLocal` VERT S-264.
* **Wiring SVO prouvé** : `AutonomousSessionTask.resolvePlannerPort()` résout le `PlannerPort` par nom canonique `plannerPortService` (adapter planner `KoogIntentionPlannerAdapter`) ; NoOp dégradé si rien n'est branché.
* **Contrat cross-borough** : un consommateur applique `education.cccp.codebase` + `education.cccp.planner` (les 2 coexistent depuis S-264). codebase `generateAugmentedPlan` (ex-`generatePlan`) ; planner garde `generatePlan`.
* 45 cucumber runners dédiés (dont `cucumberTestAutonomousSession`).
* koverVerify VERT (check complet 27m06s S-264).
* Catalogue local codebase : `workspace-bom = "0.0.50"` (realigné S-264, drift 0.0.27 → 0.0.50).

== Échecs préexistants (orthogonaux, ne pas confondre)

* 2 × `OcrTaskTest` (`detectMimeType octet-stream for pdf`, `isImageFile false for pdf`) — drift S-211, **stash-confirmés identiques à HEAD S-264**.

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la fois.
* Git autorisé (décision pilote 2026-09-17) — zéro secret commité.
* Publication Central = accord pilote explicite (Règle 2).
* Dogfood-first : exercer le wiring cross-borough réel (le dogfood S-264 a révélé 2 bugs invisibles aux tests unitaires).

== References

* `.agents/sessions/266-sujet.adoc`
* `.agents/sessions/265-tentative-autonomousSession-SVO.adoc`
* `.agents/sessions/264-dogfood-a2-2-bugs-integration-rename-v4-doc.adoc`
* `.agents/CADRAGE_EPIC_SVO_SESSION_ORCHESTREE.adoc` (cadrage S-255)
* `training-gradle/.agents/TRANSFER_LEXIQUE_FORMATION.adoc` (transfusion)