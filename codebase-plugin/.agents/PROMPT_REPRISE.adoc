= Prompt de Reprise — Codebase (Queens N1) — Session 194
:session: 194
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 193 — EPIC FT-PIPELINE US-1 TERMINÉ (domaine `codebase.finetuning` 7ème créé : `FineTuningConfig` data class 5 champs + invariants + `FineTuningConfigMerger` 4 sources ENV < props < YAML < CLI). Baby-step TDD strict. 4 fichiers créés (2 prod + 2 test). 27/27 unit PASSED (8 config + 19 merger). `compileKotlin` VERT, zéro régression, zéro secret, zéro bump BOM. EPIC FT-PIPELINE 1/7 US.

== Mission Session 194

EPIC FT-PIPELINE en cours (1/7 US). Prochaine US-2.

=== Option A — EPIC FT-PIPELINE US-2 (P0 vision, adapter Ollama)

US-2 (3 pts, ~6 unit) : `OllamaFineTunerAdapter` — adapter bridge Ollama API
(create model from Modelfile, push to registry). Pattern `ChatModelTranscriptEnhancer`.
Fallback degraded : Ollama unavailable → `FineTuningResult.failure` (degraded
garde dataset original, pas de crash). Document :
`.agents/CADRAGE_EPIC_FT_PIPELINE.adoc` US-2.

=== Option B — Maintenance / déclencheur pilote

Borough DORMANT jusqu'à déclencheur pilote.

== Contexte technique

* Build autonome : `cd codebase-plugin && ./gradlew ...`.
* Version alignée : codebase-plugin 0.0.8 = Maven Central = BOM 0.0.19.
* EPIC FT-PIPELINE (P0 vision, 7 US ~18 pts) — US-1 ✅ S-193, US-2→6 À FAIRE.
* Domaine `codebase.finetuning` (7ème) ouvert S-193 (FineTuningConfig + merger).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Gate ciblé : jamais `./gradlew test` global (Cucumber interminable).
* Zéro secret, zéro token dans le code ou les logs.
