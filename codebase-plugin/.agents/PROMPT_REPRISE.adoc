= Prompt de Reprise — Codebase (Queens N1) — Session 261
:session: 261
:borough: Queens — codebase-gradle (N1, RAG socle pgvector partagé)
:session_precedente: 260 — Audit lion92/codebase-gradle fork (no-op, zéro code, fork stéré)

== Mission Session 261

Borough DORMANT (tous EPICs terminés). Maintenance uniquement — RAG,
contexte augmenté, bout en bout. La prochaine session sera déterminée
par le pilote.

EPIC SVO ✅ TERMINÉ 6/6 US (S-255→S-259). Missions candidates (ordre
critical path) :

. **Dogfood Newark A2 (sur demande pilote)** — la première mission réelle
   d'`autonomousSession` consomme le contexte augmenté : résoudre le port
   planner (wiring `education.cccp.planner` + `plannerPortService`) et
   lancer la tâche sur un borough réel. Critical path MVP0.
. **RENAME vague document** (sur demande pilote) — transfusion d'abord
   (`TRANSFER_LEXIQUE_FORMATION.adoc` table § 2), puis neutralisation
   document-gradle (3 pts + OCR-QUALITY US-3).
. **Maintenance** : publication 0.0.15 Central (le HEAD contient
   SVO-1/2/3/4/5), support boroughs consommateurs.

== Contexte technique

* SVO-1/2/3 ✅ (S-256/257/258) : `PlannerPort` + `BacklogDetector` +
  `SessionResumptionPlanner` + adapter Manhattan (`plannerPortService`).
* SVO-4 ✅ S-259 : domaine `codebase.koog.svo` — `SessionPromptResolver`
  (CLI > défaut borough PROMPT_REPRISE+backlog), `PlannerPortResolver`
  (duck-typed `port()` sur registrations, nom canonique
  `plannerPortService`, fallback NoOp), `AutonomousSessionOrchestrator`
  (boucle chaînée pure : audit JSONL, D8 brainstorming exécuté, D9 borné 3,
  resumption prompt `build/vibecoding/resumption-prompt.adoc`),
  `AutonomousSessionTask` (adaptateur mince, options CLI
  `--prompt/--borough/--maxChainedSessions/--maxActions/--model`), tâche
  12e wiring `CodebasePlugin`. 45 unit SVO 0 FAIL + 4 functional.
* SVO-5 ✅ S-259 : chaînage brainstorming D8 dans l'orchestrateur +
  `autonomous_session.feature` 4/4 PASSED (45e runner
  `cucumberTestAutonomousSession`, fakes in-memory pattern S-082/S-088).
* Pièges Cucumber Expression documentés (S-259) : `/` dans un step =
  alternative (→ reformuler), nombre en tête = paramètre.
* Version : 0.0.14 mavenLocal (Central 0.0.14 — publication 0.0.15 à
  l'accord pilote). BOM 0.0.37. Conventions 0.0.4.
* Gate ciblé : jamais `./gradlew test` global sans permission (2 fails
  préexistants OcrTaskTest drift pdf S-211). Suites ciblées :
  `--tests "codebase.koog.svo.*"` etc.
* Gouvernance unifiée dans `codebase-plugin/.agents/` (S-207).

== Règles absolues

* Baby-step TDD/BDD/DDD strict (RED → GREEN → REFACTOR), 1 fichier à la
  fois, séquentiel pur (interdiction @xp/subagents/parallélisation).
* Zéro commit/push sans permission explicite du pilote.
* Zéro secret, zéro token dans le code ou les logs.
* RENAME : transfuser la logique métier dans training AVANT toute purge.

== References

* `.agents/sessions/259-svo4-svo5-autonomous-session-chainup-bdd.adoc` (rapport S-259)
* `.agents/CADRAGE_EPIC_SVO_SESSION_ORCHESTREE.adoc` (cadrage S-255 — 10 décisions)
* `training-gradle/training-plugin/.agents/TRANSFER_LEXIQUE_FORMATION.adoc` (transfusion)