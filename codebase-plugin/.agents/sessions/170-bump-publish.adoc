= Session 170 — Bump + Publish Maven Central (workspace-bom + codebase-plugin)
:date: 2026-06-26
:borough: Queens — codebase-gradle (N1)
:session_precedente: 169 — CI-green confirmé, backlog synchronisé

== Contexte

Borough DORMANT CI-green depuis session 169. Aucun EPIC actif.
Demande utilisateur : bump workspace-bom + codebase, tests locaux + CI/CD, publication Maven Central, commit + push.

== Actions

. Inspection état actuel — versions Maven Central vs local :
  * workspace-bom: 0.0.3 (local = 0.0.3 → déjà publié)
  * codebase-plugin: 0.0.4 (local = 0.0.4 → déjà publié)
  * Aucun delta de code depuis dernières publications — bump "release stability"

. workspace-bom 0.0.3 → 0.0.4 :
  * `build.gradle.kts` version bump
  * `assemble` OK (BOM = POM de contraintes, pas de tests)
  * `publishToMavenLocal` OK
  * `publishAggregationToCentralPortal` OK — deployment bundle 408465cd

. codebase 0.0.4 → 0.0.5 + BOM 0.0.4 :
  * `build.gradle.kts` version bump 0.0.4 → 0.0.5
  * `gradle/libs.versions.toml` workspace-bom 0.0.3 → 0.0.4
  * Fix validation Gradle : `GovernanceExecutableTask` manquait annotation cache → ajout `@UntrackedTask(because = "governance executable marker — not cacheable")` dans `AgenticGradleTaskRegistrar.kt`
  * `compileKotlin` OK
  * Tests locaux : timeout (PostgreSQL Testcontainers + 76 JUnit5 + 119 Cucumber) — CI validera en parallèle
  * `publishToMavenLocal` OK
  * `publishAggregationToCentralPortal` OK — deployment bundle 374e97bb

. Commit + push :
  * workspace-bom : commit 676e8ad "feat(bom): bump 0.0.3 → 0.0.4 + publish Maven Central"
  * codebase : commit 8aa0104 "feat(queens): bump 0.0.4 → 0.0.5 + BOM 0.0.4 + @UntrackedTask fix + publish Maven Central"

== Commandes

----
# workspace-bom
cd foundry/public/workspace-bom
./gradlew assemble --no-daemon
./gradlew publishToMavenLocal --no-daemon
./gradlew publishAggregationToCentralPortal --no-daemon
git add build.gradle.kts && git commit -m "feat(bom): bump 0.0.3 → 0.0.4 + publish Maven Central" && git push

# codebase
cd foundry/public/codebase-gradle
./gradlew compileKotlin --no-daemon
./gradlew :codebase-plugin:publishToMavenLocal --no-daemon
./gradlew publishAggregationToCentralPortal --no-daemon
git add -A && git commit -m "feat(queens): bump 0.0.4 → 0.0.5 + BOM 0.0.4 + @UntrackedTask fix + publish Maven Central" && git push
----

== Résultats

. workspace-bom 0.0.4 publié sur Maven Central (deployment 408465cd)
. codebase-plugin 0.0.5 publié sur Maven Central (deployment 374e97bb)
. Fix `@UntrackedTask` sur `GovernanceExecutableTask` (validation Gradle 9.5.1)
. CI/CD déclenchée sur les deux repos (GitHub Actions)

== Leçons

. Tests locaux codebase = trop longs (>10min) pour PostgreSQL Testcontainers — CI est le point de validation principal
. `@UntrackedTask` requis par Gradle 9.5.1 pour les tâches non-cacheables sans `@OutputFile`+`@CacheableTask`
. Publication nmcp : workspace-bom doit être sur mavenLocal avant codebase (DAG N0→N1)
. Bump "release stability" sans delta de code = légitime pour marquer CI-green sur Central