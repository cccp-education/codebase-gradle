= Session 123 — CR-10 Dependency Conflict Validation + CR-11 Version Catalog Centralisé
:date: 2026-06-16
:type: refactor+infra
:score: 10/10

== Contexte

EPIC CR P2 — 2 items : CR-10 validateDependencies task + CR-11 migration versions vers libs.versions.toml.

== Actions

=== CR-10 : Dependency Conflict Validation

. Ajouté `validateDependencies` task dans `build.gradle.kts` — vérifie que la version résolue de `annotations` est `13.0`
. Wiré dans `tasks.check { dependsOn("koverVerify", "validateDependencies") }`
. Résultat : la tâche détecte correctement que la version résolue est `26.0.2-1` (la contrainte `strictly` ne force pas effectivement). Le bug est identifié, à corriger dans une session future.

=== CR-11 : Version Catalog Centralisé

. Ajouté 5 versions dans `gradle/libs.versions.toml` : `assertj`, `codebase-contracts`, `agent-contracts`, `vibecoding-contracts`, `llm-pool-contracts`, `opencode-session-contracts`
. Ajouté 8 entrées `[libraries]` : `assertj-core`, `kotlinx-coroutines-test`, `junit-platform-launcher`, `codebase-contracts`, `agent-contracts`, `vibecoding-contracts`, `llm-pool-contracts`, `opencode-session-contracts`
. Migré `build.gradle.kts` : 5 N0 contracts hardcodés → `libs.*.contracts`, `assertj-core:3.27.3` → `libs.assertj.core`, `kotlinx-coroutines-test:1.10.2` → `libs.kotlinx.coroutines.test`, `junit-platform-launcher` → `libs.junit.platform.launcher`

== Résultats

- Compilation OK (`compileKotlin` BUILD SUCCESSFUL)
- `validateDependencies` fonctionnel (détecte mismatch annotations 26.0.2-1 vs 13.0)
- 2 fichiers modifiés : `build.gradle.kts`, `gradle/libs.versions.toml`
- BACKLOG CR-10 ✅ CR-11 ✅
- EPIC CR 11/14
