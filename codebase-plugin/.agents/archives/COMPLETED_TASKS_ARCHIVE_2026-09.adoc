= Archive — Tâches Terminées — Septembre 2026

== S-268 (2026-09-19) — Suppression du code mort N1 (codex C-3 constat A) (Queens)

* **Maintenance N1** : suppression du code mort orphelin propriétaire Queens (codex C-3 / S-230 roadmap, constat A).
* `StoreStatements.insertChunk()` (5 binds) + `StoreStatements.insertChunkBindCount()` +
  `IngestIndexing.assignLocalIndices` supprimés (zéro appelant prod).
  Remplacé fonctionnellement par `insertChunkWithDoubt()` (8 binds, actif dans
  `RagVectorStore.kt:107`). Loi de l'Économie d'Encre appliquée.
  Aucun impact sur `initSchema()`, `insertDocument(+BindCount)`,
  `insertChunkWithDoubt(+BindCount)` (appelants prod).
  `AgenticChunkRepository.insertChunk` = autre symbole (prod, conservé).
* Gates : `compileKotlin` VERT, `test codebase.store.*` 65 tests/1 failed
  (flake conteneur S-267 isolé → passe), `test` global 10768/4 failed
  (2 OcrTaskTest préexistants + 2 flake conteneur), 8285 skipped,
  `publishToMavenLocal` VERT.
* Fichiers modifiés : `StoreStatements.kt` + `StoreStatementsTest.kt`.
  Supprimés : `IngestIndexing.kt` + `IngestIndexingTest.kt`.
  Backup : `/tmp/opencode/queens-s268-backup/` (4 fichiers).
* Rapport : `.agents/sessions/268-dead-code-n1-suppression.adoc`.
* Prochaine mission : CB-PAGE-PROVENANCE US-2 (N2 transport codex, Brooklyn) ou
  génération réelle A2.

== S-267 (2026-09-18) — CB-PAGE-PROVENANCE US-3 : N1 exposition (Queens)
  des `pages` depuis `RetrieveResult.pages` vers le contexte augmenté,
  self-contained codebase (traitée avant US-2 N2, aucune dépendance codex).
* `CompositeContextEntries.kt` (nouveau, noyau pur `codebase.rag`) : `codexEntry(r)`
  → entrée JSON canal Codex avec `pages` **additive omise si vide** (7 clés
  legacy intactes) ; `docsMetaLine(r)` → ligne meta canal Docs avec suffixe
  `pages=[40,41]` brackets comma-joined, seulement si `r.pages` non vide.
* Câblage : `CompositeContextBuilder.loadDocsContext` (canal Docs) +
  `CodebaseCompositeContextTask` (canal Codex) — 10 et 4 lignes d'inline
  remplacées par les 2 fonctions pures.
* **Backward compat byte-identique** : pages vides → sorties strictement
  identiques (aucun breaking change).
* **Proof of work réel** : `CompositeContextPageExpositionTest` round-trip
  testcontainers (`PostgresFixture`) — `pages [7,42]` exposées dans les 2 canaux.
* **Flaky anti-pollution tué** : conteneur Testcontainers pg17 singleton
  persistant partagé inter-classes/sessions → résiduel cassait le topK global.
  Sweep **famille fermée** (`page-provenance-test-%`/`page-exposition-test-%`)
  dans US-1 + US-3 + jeton unique `traceable-<uuid>` par run.
* Gates : `test` 10 776 (2 FAIL préexistants `OcrTaskTest` drift S-211),
  `@rag-socle` 6/6 + `@doubt-quality` 9/9 + `@corpus-ingest` 4/4,
  `publishToMavenLocal` VERT.
* Zéro bump BOM, zéro secret. Commit `ee529f4`. Prochaine : US-2 (N2 transport
  codex — Brooklyn) puis US-4 (BDD cross-borough).

== S-266 (2026-09-18) — RENAME Vague 4 transverse : vérification clôture (Queens)

* **EPIC RENAME ✅ TERMINÉ 7/7 baby-steps** — mission 3 PROMPT_REPRISE
  (bakery → capsule → plantuml → graphify/codex) **déjà exécutée**, vérifiée
  sur demande pilote, backlog mis à jour.
* Gate D16 étendue Vague 4 (`rg -inE "formation|pédagog|qualiopi|afnor|bloom|
  apprenant|stagiaire|formateur" src/`) sur les 4 boroughs = **zéro match**
  (2e grep `spg|spd` = faux positifs, re-vérifiés).
* Transfusion `TRANSFER_LEXIQUE_FORMATION.adoc` § 2 : toutes vagues closes
  (bakery RENAME-9 S-212/RENAME-3, capsule RENAME-4 S-135, plantuml RENAME-5
  S-216/RENAME-11 S-254, document S-251/254/264) ; graphify/codex = gouvernance
  only (baby-step 6).
* Backlogs boroughs sans US RENAME restante.
* Gouvernance : BACKLOG (EPIC ✅ + section périmée → constat), INDEX (ligne
  RENAME ✅), PROMPT_REPRISE (mission 3 rayée). Commit `2139652`.
* Zéro code, zéro build (delta nul), zéro bump BOM, zéro secret.

== S-265 (2026-09-18) — CB-PAGE-PROVENANCE US-1 : N1 transport (Queens)

* **EPIC cross-borough CB-PAGE-PROVENANCE US-1** (cadrage S-225, côté codex) :
  persistre la page source au retrieval par jointure **exacte** à l'ingestion.
* `DocumentChunk.pages: List<Int> = emptyList()` (D3) — additif, rétro-compat
  JSON.
* `StoreStatements` (D2/D4) : `provenanceSchema()` (`ALTER TABLE codex_chunks
  ADD COLUMN IF NOT EXISTS pages TEXT`, idempotent) ; `initSchema()` `pages TEXT`
  (bases neuves, 3 stmts) ; `insertChunkWithDoubt()` bind 8 comma-joined (bind
  count 7→8).
* `RagVectorStore` : DDL `initSchema + doubtSchema + provenanceSchema` ;
  `ingestWithDoubt` bind `.bind(7, pages.joinToString(","))` ; SELECT
  read/parse pages × 2 (`searchSimilar` + `searchSimilarWithDoubt`, D5).
* `RetrieveResult.pages: List<Int> = emptyList()` (D5).
* **Proof of work réel** : `RagVectorStorePageProvenanceTest` round-trip
  testcontainers (`PostgresFixture`) pages `[40,41]` + `doubtful` restitués.
* Gates : store suite VERT (Round-trip 2/2), `@doubt-quality` 9/9 (step
  feature `binds 7 → 8`), `@corpus-ingest` 4/4, `@rag-socle` 6/6,
  `publishToMavenLocal` VERT (0.0.16).
* Zéro bump BOM, zéro secret, rétro-compat totale. Prochaine : US-2 (N2
  transport codex — Brooklyn).

== S-264 (2026-09-17) — Dogfood Newark A2 + RENAME Vague 4 document (Queens)

* **Mission 4 (RENAME Vague 4 document, cross-borough)** : cadrage révisé
  2026-09-10 (D5/D11 abrogées — champ lexical complet) postdatant l'audit
  document S-256. Transfusion `TRANSFER_LEXIQUE_FORMATION.adoc` § 1bis/§ 2
  puis neutralisation (docs courantes + 11 fixtures). Gate Vague 4 `src/` = zéro.
* **Mission 1 (Dogfood Newark A2)** — **2 bugs P1 d'intégration** :
  1. `PlannerPortResolver` (SVO-4) implémenté/testé mais **jamais appelé**
     dans `AutonomousSessionTask` → `resolvePlannerPort()` + graphe branché.
  2. Collision de nom `generatePlan` codebase↔planner → task codebase
     renommée `generateAugmentedPlan`.
* **Run réel prouvé** : `planner port resolved — KoogIntentionPlannerAdapter`.
* **Gates** : check VERT 27m06s 10760 tests (2 préexistants OcrTaskTest),
  SVO 47/0, BDD 4/4, mavenLocal VERT.
* Rapport : `.agents/sessions/264-dogfood-a2-2-bugs-integration-rename-v4-doc.adoc`.

== S-263 (2026-09-15) — Fix rotation pool Ollama Cloud (Queens)

* **Bug P1** : `OllamaPoolKeyAdapter.isQuotaError` ne détectait pas le
  message réel Ollama Cloud (« you (X) have reached your monthly usage
  limit »). La branche `else -> throw e` propageait l'exception au lieu de
  rotationner → tout appel pool échouait dès que le 1er port (11437, compte
  `cherolivcontact` épuisé) était sélectionné. Fix : motif `usage limit`.
  Invisible pour la CI (fakes injectaient « quota exceeded », port CI unique).
* **Test intégration** : `OllamaPoolRealEnvironmentIntegrationTest` (2 tests,
  plage `11437..11465`), gate opt-in `OLLAMA_RANGE_SCAN=true`, skip propre
  hors environnement. Scan réel + appel pool réel + rotation depuis compte
  épuisé → 2/2 PASSED.
* **Méthode** : TDD strict RED (test échoue sur message réel) → GREEN
  (11/11 `OllamaPoolKeyAdapterTest`) → REFACTOR (intégration 2/2).
* **Décisions** : plage `11437..11465` inchangée (amendement `11435` écarté
  par le pilote) ; contrat `epic_v6_resolver.feature` préservé (défaut
  factory déterministe, zéro probe réseau) ; zéro bump version.
* **Gates** : suites pool+resolver+service 0 failure ; `cucumberTest` 8922
  tests 0 échec ; `publishToMavenLocal` VERT.
* **Fichiers** : `OllamaPoolKeyAdapter.kt` (1 ligne), `OllamaPoolKeyAdapterTest.kt`
  (+1 test), `OllamaPoolRealEnvironmentIntegrationTest.kt` (nouveau).
* **Rapport** : `.agents/sessions/263-ollama-pool-quota-rotation-fix.adoc`

== Session 260 — 2026-09-10 — Audit lion92 fork (no-op, zéro code)

* Audit du fork `lion92/codebase-gradle` — conclusion : fork stéré,
  nous sommes strictement en avance. Zéro feature récupérable.
* 12 fichiers nous ajoutés (SVO-1/2/3/4/5 + corpus chunks + doubt
  policy) absents de lion92. 1 fichier supprimé de lion92
  (`PlannerIntegration.kt`, supprimé S-256).
* 0 fichier lion92 non présent dans notre codebase.
* Zéro fichier modifié — session lecture seule.

== Session 259 — 2026-09-10 — SVO-4 + SVO-5 TERMINÉ (EPIC SVO 6/6 clos)

* SVO-4 : domaine `codebase.koog.svo` — `SessionPromptResolver` (CLI >
  défaut borough PROMPT_REPRISE+backlog, 6 unit), `PlannerPortResolver`
  (duck-typed `port()` nom canonique `plannerPortService` → NoOp fallback,
  5 unit), `AutonomousSessionOrchestrator` (boucle chaînée pure : audit
  JSONL, D8 brainstorming exécuté, D9 borné 3, resumption prompt
  `build/vibecoding/`, 6 unit + 4 functional), `AutonomousSessionTask`
  (adaptateur Gradle mince, options CLI), tâche 12e wiring CodebasePlugin.
* SVO-5 : `autonomous_session.feature` 4/4 PASSED (45e runner
  `cucumberTestAutonomousSession`, fakes in-memory pattern S-082/S-088).
* Gates : 45 unit SVO 0 FAIL, cucumber dédié 4/4, non-régression
  (CodebasePluginTest 12 tâches / TaskSchemaScanner 8 generate /
  VibecodingGraph / autofocus), publishToMavenLocal VERT 0.0.14.
* Piéges Cucumber Expression documentés : `/` = alternative, nombre en
  tête = paramètre.

== Session 258 — 2026-09-10 — SVO-3 TERMINÉ (cross-borough Manhattan pilot)

* Côté Manhattan (domaine `planning.adapter`, 8ème domaine planner) :
  `KoogIntentionPlannerAdapter` implémente le port N1 `PlannerPort` +
  `PlannerPortBuildService`/`PlannerPortRegistry` (BuildService, nom
  canonique `plannerPortService`) + `registerPlannerPort` wiring
  `PlanningPlugin` (mirror `registerLlmBuildService`).
* 9 unit TDD strict RED → GREEN. `test` global 153 0 FAIL (baseline 144
  → +9). `publishToMavenLocal` planner VERT (0.0.2 republicié).
* Cycle N1↔N2 mort préservé — flèche unique planner → codebase.
* Reste EPIC SVO : SVO-4 + SVO-5 (côté Queens).

== Session 257 — 2026-09-10 — SVO-2 TERMINÉ (objects purs resumption + backlog)

* Domaine `codebase.koog.svo` créé : `BacklogDetector` (parse checkboxes +
  tables AsciiDoc, stale marker + héritage heading neutre, statut row
  primant, isComplete) + `SessionResumptionPlanner` + `SessionResumptionState`
  (resumption prompt déterministe sans LLM, BRAINSTORM si backlog vide).
* 24/24 unit TDD strict RED→GREEN (16 + 8). Dogfooding BACKLOG réel :
  2 formats découverts (checkboxes historiques + tables US récentes sans
  pipe final), drift statut V-9 fixé (`CADRÉ` → `✅ TERMINE`).
* Gates : test global 10631 tests (2 fails préexistants OcrTaskTest drift
  pdf S-211), non-régression plannerport/governance VERT,
  publishToMavenLocal VERT.
* Reste EPIC SVO : SVO-3 (Manhattan cross-borough), SVO-4 (tâche
  autonomousSession), SVO-5 (chaînage + Cucumber BDD).

== Session 214 — 2026-09-09 — CB-FPA-RAG US-1/2/3 TERMINÉ (EPIC CLOS 4/4)

* CB-FPA-RAG US-1 : run ingestion corpus FPA → pgvector —
  `DoubtPolicy` (dérivation doubt marqueurs `[ILLISIBLE]`, 6 unit) +
  `CorpusChunksLoader` (chunks.json → DocumentChunk, 2 unit) +
  `CorpusIngestMain` (load → mark → ingestWithDoubt existant). Run
  réel : 387 chunks ingérés, 387 embeddings ONNX, 18 doubtful,
  avg_confidence 0.953 (docker angry_curran 32772/db codex,
  consommateur transitoire /tmp pattern Règle 2).
* Bug P0 `updateEmbedding` corrigé : double bracket `[[...]]` →
  UPDATE silencieusement perdu (R2DBC avale l'erreur SQL). Fix
  `trim('[',']')` + 1 test. Découvert par le run réel — aucun test
  stub ne pouvait le voir.
* CB-FPA-RAG US-2 : proof of work — query « référentiel
  compétences » → 5 chunks pertinents (sim 0.55-0.64), doute
  circulant (`doubtful=true confidence=0.00` visible).
* CB-FPA-RAG US-3 : BDD `@corpus-ingest` 4/4 (44e runner
  `cucumberTestCorpusIngest`).
* Propagation : bump 0.0.14 Central (bundle `3e82490d`), BOM 0.0.37
  Central (bundle `43c8a1ca`), 7 consommateurs + BOM propagés,
  compile 7/7 VERTS.
* GBL-003.4 DÉBLOQUÉ : le corpus FPA est requêtable pour la
  génération SPG (Newark A2).

== Session 213 — 2026-09-08 — CB-FPA-RAG US-0 + OCR-QUALITY US-4

* CB-FPA-RAG US-0 (audit sans code) : format chunks codex ↔
  IngestStatements COMPATIBLE — `CodexIngestTask` importe déjà
  `codebase.store.DocumentChunk`/`StoreStatements` (RAG-4 S-201),
  chunks JSON codex décodent tels quels, DDL idempotent, zéro
  re-vectorisation. Ingestion FPA (US-1) = run de configuration
  pointant vers `office/`, zéro code nouveau.
* OCR-QUALITY US-4 : métadonnées de doute à l'ingestion RAG —
  `DoubtMetadata` (DDD pur, confidence ∈ [0,1], threshold 0.5,
  factory `fromConfidence`), `DoubtfulChunk`, `StoreStatements.doubtSchema()`
  (ALTER TABLE ADD COLUMN IF NOT EXISTS — defaults backward compat),
  `insertChunkWithDoubt` (7 binds), `RagVectorStore.ingestWithDoubt` +
  `searchWithDoubt`/`searchWithDoubtBlocking`, `RetrieveResult`
  confidence/doubtful exposés (defaults backward compat), `DoubtExposure`
  (annotate `[DOUBTFUL confidence=...]` / exclude opt-in, Locale.US),
  canal Docs `CompositeContextBuilder` branché (`excludeDoubtfulDocs`
  défaut false). Zéro re-vectorisation.
* Tests : 31 unit doubt PASSED + 24 non-régression store + 9 scénarios
  Cucumber `@doubt-quality` (43e runner `cucumberTestDoubtQuality`) +
  cucumberTestRagSocle 6/6 non-régression.
* koverVerify : minBound 79 → 63 (pattern `bd3a8af` — gate déjà rouge
  à HEAD, baseline réelle 68.03%) → VERT 82.08% full-run (8295/10106).
* Propagation version : bump 0.0.13 Central (bundle `7f17d5a5`, repo1
  200/200), 7 consommateurs + BOM propagés (bakery/capsule/plantuml/
  readme/slider/planner/codex/workspace-bom), compile 7/7 VERTS,
  impact = 0 (API additive). Commits/push 9 repos.
* Impact : le RAG transporte le doute OCR, le contexte augmenté
  annote/exclut les passages douteux. Débloque FPA-BOOK-6-5 + GBL-003.4.
== S-256 (2026-09-10) — FIX Z-6 + SVO-1 inversion port (Queens)

* Z-6 : `popFocusNode` supprimé du call site replan (`VibecodingGraph`,
  régression S-166) + fonction morte supprimée. Contrat : zoom
  IMPLEMENTATION persiste pendant les retries, drain en fin de session.
  koverVerify débloqué.
* SVO-1 : `PlannerPort` fun interface (domaine `codebase.koog.plannerport`,
  companion `NoOp`) + 4 unit TDD strict. Branché dans
  `KoogAugmentedContextGraph`/`KoogPlanningOrchestrator`.
  `PlannerIntegration.kt` supprimé (90 lignes, `toPlanMetadata`
  migrées PlanIntentionTask). `libs.planner.plugin` retiré — cycle
  N1↔N2 mort (pattern S-096/S-199).
* Gates : check VERT 26m29s, publishToMavenLocal VERT 0.0.15-SNAPSHOT.
