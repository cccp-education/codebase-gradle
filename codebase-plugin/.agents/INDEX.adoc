= INDEX.adoc — Codebase Gradle (Queens N1)
:date: 2026-08-18
:session_en_cours: 191
:session_precedente: 190 — EPIC VIBE-HARDENING-2 US-3 C4 GradleTaskArgsMapper flags cassés (P2, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR). `GRADLE_ARG_ALLOWLIST` étendu `--\w+` → `--\w+(?:=\S+)?` (flag seul ou `--flag=value`) + `isBareLongFlag(token)` helper + `validateGradleTask` refondu : un token d'arg qui ne matche pas l'allowlist est accepté si le token précédent est un `bareLongFlag`, à condition de ne pas commencer par `-` et de ne pas contenir `;&|`. 5 nouveaux tests `ExecGradleToolTest` (3 positifs pair flag+value/flag=value/mixed + 2 négatifs bare value/standalone token). **Fail préexistant C4 résolu** : `GradleTaskArgsMapperTest > execute structured gradle task via handler()` PASSED. Gate combinée `codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 110 tests 0 FAIL. Zéro bump BOM, zéro secret, zéro régression. 3/8 US VIBE-HARDENING-2.
:derniere_session: 190 — EPIC VIBE-HARDENING-2 US-3 C4 GradleTaskArgsMapper flags cassés. `GRADLE_ARG_ALLOWLIST` étendu + `isBareLongFlag` + `validateGradleTask` accepte pair flag+value (defense in depth préservée). Fail préexistant C4 résolu. 5 unit + 110 gate VERT. 3/8 US VIBE-HARDENING-2. Borough DORMANT.
:prochaine_session: 191 — EPIC VIBE-HARDENING-2 US-4 C1 TaskResultVerifier refonte (P1, ~4 pts, ~10 unit — heuristique `contains("failed")` raffinée + comparaison `expectedOutput` quand non-default) OU US-5 C3 cleanup doc INSERT-X1..X6 (P3, 1 pt) OU EPIC FT-PIPELINE US-1 (P0 vision, domaine `codebase.finetuning`, ~8 unit 3 pts) sur déclencheur pilote. Borough DORMANT.

== Roadmap EPICs

[cols="2,3,2"]
|===
| EPIC | Statut | Session

| EPIC 1 Gouvernance
| STALE (6/8 — items obsolètes, projet publié Central 0.0.3)
| 001

| EPIC 2 Anonymisation
| ✅ TERMINE
| 009

| EPIC 3 Multi-Canal
| ✅ TERMINE
| 046

| EPIC 4 Benchmark
| ✅ TERMINE
| 026

| EPIC 5 Fine-tuning
| MIGRÉ → training
| 110

| EPIC 6 ONNX Qualité
| ✅ TERMINE
| 056

| EPIC 7 Expert Dispatcher
| ✅ TERMINE
| 126

| EPIC 8 Exposer experts
| ✅ TERMINÉ (4/4 — ExpertManifestReader + round-trip + Cucumber cross-borough, session 140)
| 127, 140

| EPIC V-6 Ollama Cloud
| ✅ TERMINÉ (10/10 — resolver→factory déterministe + scanner fallback livré session 139)
| 136-139

| EPIC V-9 Gouvernance Agentique
| ✅ TERMINÉ (20/20 — V-9.1→V-9.20 clôturé session 157)
| 141-157

| EPIC 9 Opencode Augmenté
| ✅ TERMINE
| 001-021

| EPIC 10 STIMULUS
| ✅ TERMINE
| 022+025

| EPIC 12 Blog Narration
| ✅ TERMINE
| 109

| EPIC K Ontologie
| ✅ TERMINE
| 015-020

| EPIC L koog+langchain4j
| ✅ TERMINE
| 034-049

| EPIC V Vibecoding
| ✅ TERMINE
| 041-045

| EPIC W Auto-discovery
| ✅ TERMINE
| 100

| EPIC X Multi-Step
| ✅ TERMINE
| 101-105

| EPIC Y AgenticSchema
| ✅ TERMINE
| 097

| EPIC OCR
| ✅ TERMINÉ (7/7 — OCR-1 6/6 + OCR-2 5/5 + OCR-2b/3/3b/4 complets, session 159)
| 027-159

| EPIC CR Code Review
| ✅ TERMINE
| 14/14

| Documentation Locale
| ✅ TERMINE
| Session 128

| EPIC SP SessionProtocol
| ✅ TERMINE
| 114

| EPIC Z Autofocus
| ✅ TERMINE
| 108

| EPIC V-LOCAL Boucle Locale
| ✅ TERMINÉ (7/7)
| 129-132

| EPIC TRAD TranslationService
| ✅ TERMINÉ
| 133

| EPIC PUB Publication
| ✅ TERMINÉ — codebase-plugin 0.0.3 publié Central, propagation terminée
| 135

| EPIC SUBGRAPH Contexte Graphify
| ✅ TERMINÉ (4/4 — US-0 cadrage S178, US-1 domaine codebase.graph S179, US-2 canal Graphify S180, US-3 graphRelations + Resource S182, US-4 Cucumber 4 scénarios S182)
| 178-182

| EPIC VIBE-HARDENING
| ✅ TERMINÉ (6/6 — US-0 cadrage S184, US-1 shell allowlist S185, US-2 gradle allowlist S186, US-3 timeout LLM S187, US-4 retry single counter S187, US-5 cucumber BDD S187) — durcissement vibecoding P1 (S1 allowlist, C2 retry, C5 timeout LLM)
| 184-187

| EPIC VIBE-HARDENING-2
| 🟡 EN COURS (3/8 US — US-0 cadrage S188, US-1 allowlist TaskDiscoveryRegistrar S189, US-2 MAX_WRITE_CHARS S189, US-3 GradleTaskArgsMapper flags cassés S190) — durcissement vibecoding P2/P3 (S2 ✅, S3 ✅, C4 ✅, reste C1 TaskResultVerifier, C3 cleanup doc, C6 refactor, C7 negative)
| 188-190

| EPIC FT-PIPELINE
| 🟡 CADRÉ (7 US ~18 pts — US-0 cadrage S188, US-1→6 À FAIRE) — pipeline de fine-tuning N1 générique (fabrication modèle : dataset prep + continual pre-training 10% corpus + export GGUF + push Ollama). Source conception : notebook-gradle (vestige récupéré). Domaine `codebase.finetuning` (7ème). Boucle fabrication→exposition avec domaine `codebase.koog.expert` existant. TRN-8 training reformulé recettes N2.
| 188
|===

== Sessions Récentes

| Session | Date | EPIC | Résultat
| 190 | 2026-08-18 | VIBE-HARDENING-2 | **US-3 C4 GradleTaskArgsMapper flags cassés TERMINÉ** (P2, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR). `ExecGradleTool.kt` : `GRADLE_ARG_ALLOWLIST` étendu `--\w+` → `--\w+(?:=\S+)?` (flag seul ou `--flag=value`) + `isBareLongFlag(token)` helper (détecte flag sans `=` précédant une valeur séparée) + `validateGradleTask` refondu : un token d'arg qui ne matche pas l'allowlist est accepté si le token précédent est un `bareLongFlag`, à condition de ne pas commencer par `-` et de ne pas contenir `;&|` (defense in depth préservée). `ExecGradleToolTest.kt` : +5 tests (3 positifs pair flag+value/flag=value/mixed + 2 négatifs bare value/standalone token), 15→20 tests. **Fail préexistant C4 résolu** : `GradleTaskArgsMapperTest > execute structured gradle task via handler()` PASSED (subprocess Gradle `test --tests FakeTestClassName` ne lance plus `SecurityException`). Description `ExecGradleTool` mise à jour. Gate combinée `codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 110 tests 0 FAIL (20 ExecGradleTool + 17 ExecShellTool + 60 discovery + 13 ToolRegistry). `compileKotlin` VERT. Zéro bump BOM, zéro secret, zéro régression. 3/8 US VIBE-HARDENING-2.
| 189 | 2026-08-17 | VIBE-HARDENING-2 | **US-1 + US-2 TERMINÉS** (pattern S-113 multi-US 1 session). US-1 S2 Allowlist TaskDiscoveryRegistrar : `TASK_ALLOWLIST` (2 regex bare tasks + project:task) + `TASK_DENYLIST` (6 regex publishToMavenCentral/publishAggregation/clean/purge/wrapper/bootstrap) + `validateTaskName` companion (allowlist deny-by-default + deny-list second ride) + `registerAll` filtre via validateTaskName. 18 unit allowlist + 6 tests existants réécrits sécuritaires. US-2 S3 Garde taille write_file/edit_file : `MAX_WRITE_CHARS = 1_000_000` const + 3 checks → `IllegalArgumentException` avant writeText. +5 unit. Gate `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 105 tests (1 fail préexistant C4 US-3). 2/8 US VIBE-HARDENING-2.
| 188 | 2026-08-17 | FT-PIPELINE + VIBE-HARDENING-2 + cross-borough | **Maintenance + cross-borough audit + cadrage EPIC VIBE-HARDENING-2 + cadrage EPIC FT-PIPELINE + propagation 5 boroughs** (gouvernance pure + bump catalogues consommateurs). Phase 1 maintenance : INDEX/BACKLOG/SESSIONS_HISTORY/PROMPT_REPRISE synchronisés. Phase 2 cross-borough audit : 6 consommateurs audités. Phase 3 cadrage VIBE-HARDENING-2 : 7 constats P2/P3, 12 décisions, 8 US ~21 pts. Phase 4 cadrage FT-PIPELINE : 16 décisions archi, 7 US ~18 pts, source notebook-gradle récupérée, domaine `codebase.finetuning` (7ème), boucle fabrication→exposition avec domaine expert existant. Phase 5 propagation : 5 boroughs bumpés codebase 0.0.8 (planner/slider/capsule/codex/training — 4 compile VERT, codex fail préexistant). 3 bugs découverts signalés (MEMPHIS slider 0.0.8 non publié, ABIDJAN codex kotlin-stdlib, capsule contracts 0.1.0 local-only). Zéro fichier production codebase modifié. Rapports : sessions/188-*.adoc + CADRAGE_EPIC_FT_PIPELINE.adoc + CADRAGE_EPIC_VIBE_HARDENING_2.adoc.
| 187 | 2026-08-17 | VIBE-HARDENING | **US-3+US-4+US-5 TERMINÉ — EPIC VIBE-HARDENING CLOS 6/6 US** (pattern S-113, 3 US en 1 session). US-3 C5 Timeout LLM : `StepVerifier.kt` constructor param `llmTimeoutMs: Long = DEFAULT_LLM_TIMEOUT_MS (30_000L)` + companion + wrap `withTimeout(llmTimeoutMs)` sur site replan L44 (catch `TimeoutCancellationException` → `lastToolResult = "LLMTimeout: ${ms}ms exceeded"`) ; `VibecodingGraph.kt` constructor param `llmTimeoutMs` déplacé AVANT `stepVerifier` (default param propagation) + wrap `withTimeout` sur 2 sites (callLLMNode L413 → `withError("LLMTimeout")` terminal, error recovery replan L288 → `clearError().nextIteration().copy(lastToolResult="LLMTimeout...")` récupérable). US-4 C2 Retry single counter : supprimé `retryCount = state.retryCount + 1` 2 sites `executePlanTasks` (L587 isFailure + L604 catch) + 3 `incrementRetry()` branches replan → 1 `incrementRetry()` début bloc error recovery. US-5 Cucumber BDD : `codebase_vibe_hardening.feature` 4 scénarios `@vibe-hardening` (shell allowlist deny, gradle allowlist allow, LLM timeout, retry single counter) + `VibeHardeningWorld` (PicoContainer) + `VibeHardeningSteps` (14 step defs préfixés "vibe hardening", bug S-088 évité) + `VibeHardeningCucumberRunner` (pattern S-082, filter `@vibe-hardening and not @integration`) + `CucumberTaskSpec("cucumberTestVibeHardening")` 37e cucumber dédié. 4/4 scénarios PASSED 47s. Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier VERT 64 tests 0 FAIL (17 ExecShellTool + 15 ExecGradleTool + 22 VibecodingGraph + 10 StepVerifier). Zéro bump BOM (EPIC local), zéro secret, zéro régression. 3 fichiers prod modifiés (`ExecGradleTool.kt`, `VibecodingGraph.kt`, `StepVerifier.kt` — signatures backward compat).
| 186 | 2026-08-17 | VIBE-HARDENING | **US-2 ExecGradleTool allowlist deny-by-default TERMINÉ** (baby-step TDD strict RED → GREEN → REFACTOR, pattern US-1 shell à copier). 4 regex `GRADLE_BLACKLIST` (bypassables : `clean  build` double espace, variations `--refresh-dependencies`, `rm`/`delete` ne couvre pas `sudo`/`curl`/`;`/`|`) remplacées par `GRADLE_TASK_ALLOWLIST` (2 patterns : bare tasks build/compileKotlin/compileTestKotlin/test/testFast/check/assemble/jar/publishToMavenLocal/tasks + project:task form `:?[\w-]+:[\w-]+`) + `GRADLE_ARG_ALLOWLIST` (2 patterns : `-P[\w.]+=\\S+` + `--\\w+`) + `GRADLE_DENYLIST` (8 patterns second ride : clean build, --refresh-dependencies, rm, delete, sudo, curl, semicolon, pipe). `validateGradleTask` : split tokens par `\\s+`, 1er token vs TASK_ALLOWLIST, reste vs ARG_ALLOWLIST, puis deny-list sur chaîne complète (defense in depth). `ExecGradleToolTest` 6→17 tests (5 allowlist pass, 9 deny-list + deny-by-default, 2 @integration préservés, 1 supprimé sleep). Gate `codebase.koog.tools.*` VERT 32 tests 0 FAIL (17 shell + 15 gradle). `ToolRegistryTest` 8/8 non-régression. 1 fichier prod modifié (`ExecGradleTool.kt` 62→71 lignes), 1 fichier test modifié (`ExecGradleToolTest.kt`). Zéro bump BOM.
| 185 | 2026-08-17 | VIBE-HARDENING | **US-1 ExecShellTool allowlist deny-by-default TERMINÉ** (baby-step TDD strict RED → GREEN → REFACTOR). `BASH_BLACKLIST` (10 regex bypassables) remplacé par `SHELL_ALLOWLIST` (10 patterns : git read, rg, ls, find ., mkdir /tmp, echo, cat, head/tail, ./gradlew, pwd) + `SHELL_DENYLIST` second ride (10 patterns : rm -[rR], sudo, curl, wget, chmod 0?777, pipe sh/bash, /etc/, /dev/, redirect /). `validateCommand` : allowlist passe d'abord (deny-by-default), deny-list en second ride (defense in depth). Messages d'exception : "denied" au lieu de "blacklisted". `ExecShellToolTest` 11→17 tests (6 nouveaux : `rm -Rf` uppercase, `chmod 0777` zero-prefixed, `curl localhost` rejeté allowlist, `unknown command` rejeté deny-by-default, `git push` subcommand non-allowlistée, `redirect to root` ; 1 supprimé : `sleep 5` timeout — commande non-allowlistée rejetée avant timeout process ; 1 adapté : `exit 42` → `git show deadbeef` allowed non-zero exit). Gate `codebase.koog.tools.*` VERT 21 tests 0 FAIL (17 shell + 4 gradle non-régression US-2 non entamée). `ToolRegistryTest` 8/8 PASSED (consommateur `executeAction("exec_shell")` non régressé). 2 fichiers modifiés (`ExecShellTool.kt` 87→93 lignes, `ExecShellToolTest.kt` 123→139 lignes). Zéro bump BOM. Zéro secret.
| 184 | 2026-08-17 | VIBE-HARDENING | **US-0 Cadrage EPIC VIBE-HARDENING (gouvernance pure, zéro code)**. 3 constats P1 audités : S1 (10 regex `BASH_BLACKLIST` + 4 `GRADLE_BLACKLIST` bypassables — `rm -Rf`, `chmod 0777`, `clean  build` double espace), C2 (`retryCount` double-compté — `executePlanTasks` L587/L604 + bloc error L291/L298/L302 = 2 incréments/essai, maxRetries=3 épuisé en 2 essais), C5 (3 sites `runBlocking { llmProvider.call(...) }` sans timeout — callLLMNode L413, replan L288, StepVerifier L44). 7 rappels P2/P3 (S2/S3/C1/C3/C4/C6/C7). 10 décisions architecturales : allowlist deny-by-default 10 patterns shell + 8 gradle + deny-list second ride, domaine `contracts.vibecoding.tools` conservé, un seul compteur retry via `withRecoverableError` (déjà correct L46-47), `withTimeout(llmTimeoutMs=30_000L)` configurable catch `TimeoutCancellationException`, backward compat préservée (tests existants mis à jour allowlist), baby-step TDD strict 1 fichier à la fois, zéro bump BOM (artefact local), Cucumber BDD `codebase_vibe_hardening.feature` 4 scénarios `@vibe-hardening` US-5 runner dédié `VibeHardeningCucumberRunner`, pas de nouvelle tâche Gradle (durcissement interne), P2/P3 reportés EPIC VIBE-HARDENING-2. 6 US ~12 pts (US-0 ✅, US-1 shell allowlist ~14 unit 3 pts, US-2 gradle allowlist ~6 unit 2 pts, US-3 timeout LLM ~8 unit 3 pts, US-4 retry un seul compteur ~6 unit 2 pts, US-5 cucumber ~4 scénarios 2 pts). Document `CADRAGE_EPIC_VIBE_HARDENING.adoc`. Pattern S-057/S-063. `compileKotlin` VERT pré-cadrage (build autonome `codebase-plugin/`). Working tree propre.
| 182 | 2026-08-16 | SUBGRAPH | **US-3 + US-4 TERMINÉS — EPIC SUBGRAPH CLÔTURÉ 4/4**. US-3 : `GovernanceContextLoader(graphFile: File? = null, lensConfig: LensConfig = LensConfig())` — `graphRelations = graphFile?.let { GraphifyContextProvider(it, lensConfig).provide() } ?: ""` ; `MultiChannelContextGraph(graphFile: File? = null)` — canal `ContextChannel.Resource` alimenté depuis le sous-graphe réel (même provider que Graphify, budget resource 0.10) ; câblage `SessionProtocolTask.loadGovernanceContext()` + `SessionProtocolServer.resolveAgentContext()` (`root.resolve("office/graph.json")`) → `LiveContextInjector` effectif. 6 unit (4 GovernanceContextLoaderTest + 2 MultiChannelContextGraphTest), TDD strict RED (compileTestKotlin `No parameter with name 'graphFile'`) → GREEN → REFACTOR. US-4 : `codebase_subgraph.feature` 4 scénarios @subgraph (Graphify réel, Resource réel, graphRelations peuplé, fallback fichier manquant) + `SubgraphWorld` + `SubgraphSteps` (14 step defs) + `SubgraphCucumberRunner` + `CucumberTaskSpec("cucumberTestSubgraph")`. RED : UndefinedStepException + `CucumberExpressionException createAlternativeMayNotBeEmpty` (slash `/` final dans expression → alternation vide, corrigé). GREEN : 4/4 PASSED 21s, zéro production modifié. Gates ciblés VERT : 4 classes 0 FAIL (~4min) + `codebase.graph.*` 37/37 + SessionProtocol 0 FAIL. Glob `codebase.koog.*` timeout (pratique canonique gates ciblés S175/S179/S180). Zéro bump BOM. Working tree bump 0.0.8 + GradlePluginPortal non commités (diff pré-session, EPIC PUB S181).
| 181 | 2026-08-15 | PUB | **Audit version + EPIC PUB (gouvernance pure, zéro code)** — delta codebase-plugin 0.0.7 vs Central 0.0.7 (bundle e08acb47 S-073) + commits post-publication non propagés (`f840b93` SUBGRAPH US-1+US-2). Décision : bump 0.0.7→0.0.8 reporté fin d'EPIC SUBGRAPH (S179) → cadré EPIC PUB dédié (3 US PUB-0.0.8-1/2/3) ajouté au BACKLOG (session 181, `:last_update`). Zéro code modifié — gouvernance pure pattern bakery S199.
| 180 | 2026-08-15 | SUBGRAPH | **US-2 TERMINÉ — canal Graphify alimenté par le sous-graphe réel**. US-2a `GraphContextRenderer` (object pur, format texte structuré ligne par ligne compatible troncature, 6 unit). US-2b `GraphifyContextProvider` (File+LensConfig injectables, fallbacks conservés `"[Graphify] graph.json non trouve dans office/"` + `"illisible:"`, 5 unit — fixture corrigée `.kt`→`.adoc` : défaut fileExtensions). US-2c câblage `CompositeContextBuilder` (param `graphFile: File? = null` + `graphifyProvider`, `loadGraphifyStats` regex supprimé) + `MultiChannelContextGraph.collectGraphifyNode` (provider `rootDir/office/graph.json`, regex supprimé). 3 functional `CompositeContextBuilderGraphifyTest` + 1 nouveau test `MultiChannelContextGraphTest` (graph.json réel @TempDir, 7 total). Baby-step TDD strict RED → GREEN → REFACTOR. Gate ciblé VERT 24s 0 FAIL, `codebase.graph.*` 37 tests 0 FAIL. Suite complète koog interminable (2 timeouts 420s/720s — pratique canonique S175/S179 gates ciblés). ⚠️ Incident environnemental : racine `codebase-gradle` ne configure plus (même `:help` FAILED — strict kotlin-stdlib 2.3.21 embedded Gradle 9.6.1 vs workspace-bom 0.0.4 requires 2.3.20 sur buildscript classpath via plugin publié 0.0.3→codebase-plugin 0.0.5 mavenLocal, artefacts inchangés) — validation via build autonome `codebase-plugin/` (settings.gradle.kts propre). Zéro bump BOM. Bump 0.0.7→0.0.8 + Central reportés fin d'EPIC.
| 179 | 2026-08-15 | SUBGRAPH | **US-1 TERMINÉ — domaine `codebase.graph` ouvert** (portage bakery → N1). Dépendance `education.cccp:graphify-plugin:0.0.2` (catalog + implementation). `LensConfig` (scope/communities/nodeTypes/edgeTypes/maxDepth/fileExtensions + enum LensScope SUBGRAPH/FULL/SEMANTIC_ONLY) + `SubgraphExtractor` (loadGraph/extract/extractFromPath/expandBfs + SiteSubgraph : nodeCount/edgeCount/communityCount/nodeIds/communityIds/nodesByCommunity/edgesByType/nodesInCommunity/edgesOfType/neighbors). DSL Gradle Action + rules/rag LENS-2 NON portés (YAGNI). 26 unit portés de bakery (GraphModel synthétique, package `com.cheroliv.graphify.model` WORKAROUND confirmé). TDD strict RED → GREEN (13s) → REFACTOR. Gate non-régression VERT : 912 tests / 105 classes / 0 FAIL. Zéro bump BOM. Bump 0.0.7→0.0.8 + Central reportés fin d'EPIC.
| 178 | 2026-08-15 | SUBGRAPH | Cadrage EPIC SUBGRAPH (gouvernance pure, zéro code). Contexte augmenté Graphify réel : 4 constats (stats regex `loadGraphifyStats` CompositeContextBuilder.kt:138 + MultiChannelContextGraph.kt:130, `graphRelations` hardcodé `""` GovernanceContextLoader.kt:68, `ContextChannel.Resource` vide MultiChannelContextGraph.kt:154, zéro dépendance graphify-plugin). Precedent bakery identifié (SubgraphExtractor + LensConfig + BfsProximityCache, graphify-plugin 0.0.2, sous-graphe ~200 nœuds). 10 décisions architecturales (dépendance graphify-plugin 0.0.2 — WORKAROUND package relocalisé `com.cheroliv.graphify.model`, domaine `codebase.graph` 6ème, portage extracteur de bakery vers N1 socle, câblage canal Graphify budget 0.20, `graphRelations` alimenté, source office/graph.json surchargeable, GraphModel synthétique tests, bump 0.0.6, zéro bump BOM). 5 US découpées (~39 pts). Bénéfice : planner + tous consommateurs codebase construisent leur graphe/sous-graphes.
| 176 | 2026-08-05 | SLD-8 US-8.1 | LlmBuildService Gradle bridge + corrections architecture. Pattern « Plugin Indépendant + Racine Consommateur » restauré (article 0124). codebase-plugin/ autonome avec son catalog. BOM 0.0.16 + codebase 0.0.5 mavenLocal. plugin.serialization fix (codebase + planner). US-8.1 livrée TDD/BDD 3 niveaux (5 unit + 3 functional + 4 cucumber = 12 tests, 0 FAIL). 2 fichiers prod additifs (LlmServiceResolver + LlmBuildService), zéro modification existant.
| 175 | 2026-06-26 | CR | Clôture code review vibecoding CR-GLOBALE-004. 10 findings (S1-S3 sécurité, C1-C7) archivés GLOBAL_CODE_REVIEW.adoc. Gate tests koog verds (5m44s, Kotlin 2.4.10). Commit b12a092. Borough DORMANT maintenu.
| 170 | 2026-06-26 | PUB | Bump + publish Maven Central. workspace-bom 0.0.4 + codebase-plugin 0.0.5 publiés (nmcp). Fix @UntrackedTask sur GovernanceExecutableTask (validation Gradle 9.5.1). CI déclenchée sur les deux repos. Commit 8aa0104 push.
| 169 | 2026-06-26 | — | CI-green confirmé (run 28206228434 fast-tests ✅ + full-tests ✅ 33m50s). Backlog synchronisé : EPIC 1 stale purgé, EPIC OCR + CR stale corrigés. Borough DORMANT CI-green.
| 168 | 2026-06-26 | CI | 19 échecs JUnit5 full-tests résolus — @integration tag + EpicV6CucumberRunner excludes
| 167 | 2026-06-26 | CI | V-6 runner @integration fix — EpicV6CucumberRunner excludes @integration
| 159 | 2026-06-23 | EPIC OCR-1/OCR-2 | OCR-1/OCR-2 restants TERMINÉS (9/9 items) — HTTP 429 detection + rotation auto, env var mapping GEMINI_API_KEY_1..N, multi-compte Google, Tesseract fallback, 35 tests + 11 Cucumber PASS
| 158 | 2026-06-23 | EPIC V-6-POOL | V-6-POOL terminé 4/4 : AUTHORIZED_MODELS aligné sur AGENTS.adoc (2 modèles `gpt-oss:120b-cloud` + `gemma4:31b-cloud`) dans `OllamaInstanceFactory` + `OllamaInstanceScanner`, `LlmProviderResolver` cycle les 2 modèles sur `OLLAMA_POOL_PORTS` au lieu de `DEFAULT_MODEL` statique, feature Cucumber `epic_v6_pool.feature` (3 scénarios @epic_v6_pool) + runner + step `includes model`, tous tests unitaires + Cucumber PASS, zéro régression V-6 ollama_cloud + V-6 resolver ✅
| 155 | 2026-06-23 | EPIC V-9 | V-9.19 Auto-detection of new governance files — `GovernanceFileSnapshot`/`GovernanceFileDiff`/`GovernanceFileChangeDetector`/`JsonGovernanceStateStore`/`IncrementalReport` DDD, mode incrémental câblé `IngestGovernanceTask` (diff snapshot précédente → `ingestFiltered`), extension DSL `incremental.set(true)` + CLI `-Pcodebase.governance.incremental=true`, 15 unit + 7 fonctionnel + 5 Cucumber @epic_v_9_19 PASS, régressions V-9.3→V-9.18 vertes ✅. Contrainte Ollama globale sauvegardée `AGENTS.adoc` + `AGENT.adoc`, 52/52 inférences PASS.
| 154 | 2026-06-23 | EPIC V-9 | V-9.18 Compilation vers Gradle tasks — `AgenticGradleTaskRegistrar` enregistre GRADLE_TASK/CONSTRAINT_CHECK/VALIDATION comme tâches Gradle dynamiques (`runProcedure_*`, `enforceRule_*`, `validate_*`), câblage `IngestGovernanceTask`, enrichissement `AgenticChunker` verbes taxonomiques, 7 JUnit5 + 3 fonctionnels + 3 Cucumber @epic_v_9_18 PASS, régressions V-9.3→V-9.17 vertes ✅
| 153 | 2026-06-23 | EPIC V-9 | V-9.17 Ingestion Summary DSL — extension Gradle `codebaseGovernance { strictValidation.set(true) }` + propriété projet `codebase.governance.strictValidation`, `GovernanceSummaryConfig` DDD + `CodebaseGovernanceExtension`, câblage `IngestGovernanceTask.governanceConfig`, 3 JUnit5 + 4 fonctionnels + 3 Cucumber @epic_v_9_17 PASS, régressions V-9.x vertes ✅
| 152 | 2026-06-23 | EPIC V-9 | V-9.16 StrictValidationMode — option Gradle `strictValidation` dans `IngestGovernanceTask`, levée de `GradleException` si chunks invalides en mode strict, 3 JUnit5 + 3 Cucumber @epic_v_9_16 PASS, régression V-9.4 batch TAXONOMIE_WORKSPACE corrigée, régressions V-9.x vertes ✅
| 151 | 2026-06-23 | EPIC V-9 | V-9.15 Invalid Chunk Quarantine — port `InvalidChunkRepository` + implémentations in-memory/R2DBC (migration V4) + service `InvalidChunkQuarantine` câblé dans `AgenticIngestor`, `IngestionReport.invalidChunks` + JSON, 12 JUnit5 + 2 Cucumber @epic_v_9_15 PASS, régressions V-9.x vertes ✅
| 150 | 2026-06-23 | EPIC V-9 | V-9.14 Auto-activation du hook d'interdiction — `GovernanceEnforcementWirer` câblé dans `VibecodingTask` et `SessionProtocolTask`, 5 JUnit5 + 2 fonctionnels + 2 Cucumber @epic_v_9_14 PASS, zéro régression V-9.x ✅
| 149 | 2026-06-23 | EPIC V-9 | V-9.13 ToolRegistry enforcement hook — `EnforcementHook` contrat N0, `buildEnforcementHook()` N1, ToolRegistry bloque nativement exec_shell/exec_gradle, 8 JUnit5 + 3 fonctionnels + 5 Cucumber @epic_v_9_13 PASS, zéro régression V-9.x ✅
| 148 | 2026-06-23 | EPIC V-9 | V-9.12 AgenticExecutor — `AgenticExecutor` PRE_HOOK runner, `IngestionReport.executables`, câblage `IngestGovernanceTask.lastExecutor`, 8 JUnit5 + 5 Cucumber @epic_v_9_12 PASS, zéro régression V-9.x ✅
| 147 | 2026-06-23 | EPIC V-9 | V-9.11 AgenticCompiler exécutable — `ExecutableArtifact` + `ArtifactPayload` + `ExecutionResult`, `AgenticCompiler.compileExecutable()`, `AgenticChunkEnforcement` consomme PreHookPayload, 8 JUnit5 + 4 Cucumber @epic_v_9_11 PASS, zéro régression V-9.3/9.5/9.7/9.8/9.9/9.10/AgenticCompilerTest/AgenticChunkEnforcementTest ✅

| 146 | 2026-06-23 | EPIC V-9 | V-9.10 ChunkValidationReport — `ChunkValidationErrorType`, `lineStart`/`lineEnd`, `validationErrorsByType` JSON, 3 JUnit5 + 1 test fonctionnel + 3 Cucumber @epic_v_9_10 PASS ✅
| 145 | 2026-06-23 | EPIC V-9 | V-9.9 ChunkValidationGate — `ChunkValidator` câblé dans `AgenticIngestor`, `IngestionReport` enrichi `chunksInvalid`/`validationErrors`, JSON enrichi, 3 JUnit5 + 2 Cucumber @epic_v_9_9 PASS, régressions V-9.3/9.5/9.7/9.8 vertes. BACKLOG.adoc synchronisé ✅
| 144 | 2026-06-23 | EPIC V-9 | V-9.8 Governance ingestion report — `GovernanceOntologizer` intégré dans `AgenticIngestor`/`IngestGovernanceTask`, `IngestionReport` enrichi `sectionsAdded`/`sectionsTotal`, JSON enrichi, tâches Cucumber V-9.3/9.5/9.7/9.8 enregistrées. 10 JUnit5 + 3 Cucumber @epic_v_9_8 PASS ✅
| 143 | 2026-06-22 | EPIC V-9 | V-9.7 GovernanceOntologizer — 8 JUnit5 + 7 Cucumber @epic_v_9_7 PASS ✅ (V-9.6 déjà livré par EPIC Y-2)
| 142 | 2026-06-22 | EPIC V-9 | V-9.3 ScanAgent — 8 JUnit5 + 6 Cucumber @epic_v_9_3 PASS ✅ + refactor IngestGovernanceTask
| 141 | 2026-06-22 | EPIC V-9 | V-9.5 ChunkValidator — 10 JUnit5 + 5 Cucumber @epic_v_9_5 PASS ✅ (anti-duplication V-9.1/V-9.2/V-9.4 vérifiée)
| 140 | 2026-06-22 | EPIC 8 | ExpertManifestReader + RoundTrip + Cucumber cross-borough — 8 JUnit5 + 2 round-trip + 6/6 Cucumber @epic8 PASS ✅ (EPIC 8 clôturé 4/4)
| 139 | 2026-06-22 | EPIC V-6 | LlmProviderResolver factory déterministe + Cucumber @epic_v6_resolver — 9 JUnit5 + 3 Cucumber PASS ✅
| 138 | 2026-06-22 | EPIC V-6 | OllamaInstanceFactory + contrat N0 volumeTag + feature @epic_v6_ollama_cloud — 2 scénarios PASS ✅
| 137 | 2026-06-22 | EPIC V-6 | OllamaPoolKeyAdapter + câblage OllamaLlmProvider — 26/27 tests PASS ✅
| 136 | 2026-06-22 | EPIC V-6 | OllamaInstanceScanner actif + ports DDD + câblage resolver — 22 tests PASS ✅
| 135 | 2026-06-22 | EPIC PUB | N0 migration + codebase-plugin 0.0.3 publié Central (propagation en cours) ✅
| 134 | 2026-06-22 | EPIC PUB | Doublon + catalogue stale + N0 Central publiés ✅
| 133 | 2026-06-22 | EPIC TRAD | TranslationService N1 (contrat N0 + LlmTranslator + Fake + 8 JUnit5 + 6 Cucumber) ✅
| 132 | 2026-06-22 | EPIC V-LOCAL | V-LOCAL-4/5/6 terminés — 5 scénarios Cucumber + IngestGovernanceTask stub ✅
| 131 | 2026-06-22 | EPIC V-LOCAL | GovernanceContextLoader fallback + compliance ports ✅
| 130 | 2026-06-22 | EPIC L | Koog 1.0.0 replacement forwardTo ✅
| 129 | 2026-06-22 | EPIC V-LOCAL | GovernanceContextLoader + tests + docs ✅
| 128 | 2026-06-16 | Documentation Locale Vibecoding | LOCAL_VIBECODING_LOOP.adoc terminé
| 127 | 2026-06-16 | EPIC 8 Expert Exposure | 21/21 PASS ✅
| 126 | 2026-06-16 | EPIC 7 Expert Dispatcher | 60/60 PASS ✅
