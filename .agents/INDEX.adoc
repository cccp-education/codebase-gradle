= INDEX — Codebase Gradle
:toc: left
:icons: font

*Date de creation* : 2026-06-16
*Derniere mise a jour* : 2026-08-25 (session 205 — EPIC CDX-RAG-SOCLE TERMINÉ 6/6 US (confirmation via backlog update), EPIC CDX-OCR-CONTRACTS TERMINÉ 5/5 US (session 202), RAG-4 FINAL, l'inversion N1→N2 est morte. Prérequis parallèles : MEMPHIS S-104 ocr-contracts mavenLocal + codex S-104 typealiases bump 0.0.6). EPIC FT-PIPELINE TERMINÉ 7/7 US (S-188→S-198). EPIC VIBE-HARDENING TERMINÉ 6/6 US (S-184→S-187). EPIC VIBE-HARDENING-2 TERMINÉ 8/8 US (S-188→S-192).

== Regles Absolues

Lire AGENT.adoc — Directives completes pour le travail, seguridad, methodologie, architecture.

== Etat du Projet

=== Gouvernance Agentique

| Statut | Description |
|--------|-------------|
| 🆕 | Bootstrap initialisation — AGENT.adoc, INDEX.adoc, SESSION_CHECKLIST.adoc crées |
| 📋 | Backlog cadré — EPIC Y (Agentic Literature Compiler) complet, EPIC V-9 terminé |
| ✅ | Vibecoding complet — Fichiers agentiques et gouvernance analysés/structurés (EPIC V-9) |
| ✅ | Session 129 — Reprise EPIC PUB : preflight publication OK (build + validate + MavenLocal)
| ✅ | Session 130 — Koog 1.0.0 : replacement forwardTo (5 fichiers, build OK)
| ✅ | Session 131 — GovernanceContextLoader fallback dans SessionProtocolTask/Server + compliance ports/modèles
| ✅ | Session 132 — EPIC V-LOCAL finalisé (V-LOCAL-4 Cucumber 5 scénarios + V-LOCAL-5 doc + V-LOCAL-6 IngestGovernanceTask stub)
| ✅ | Session 133 — EPIC TRAD TranslationService N1 (contrat N0 + LlmTranslator + Fake + 8 JUnit5 + 6 Cucumber) — débloque bakery BKY-I18N-MIG US-4
| ✅ | Session 134 — Doublon FakeAgenticChunkRepository supprimé (14/14 PASS) + catalogue stale supprimé + `opencode-session-contracts:0.0.1` + `i18n-contracts:0.0.1` restaurés et publiés sur Maven Central via nmcp
| ✅ | Session 135 — `codebase-plugin:0.0.3` HTTP 200 sur Maven Central, EPIC PUB clôturé, publication alignée nmcp
| ✅ | Session 136 — `OllamaInstanceScanner` actif + ports DDD + câblage `LlmProviderResolver`
| ✅ | Session 137 — `OllamaPoolKeyAdapter` + câblage `OllamaLlmProvider`, rotation proactive quota/connexion
| ✅ | Session 138 — `OllamaInstanceFactory` + contrat N0 `volumeTag` + feature Cucumber `@epic_v6_ollama_cloud` (2 scénarios, zéro réseau)
| ✅ | Session 140 — EPIC 8 Expert Exposure clôturé 4/4 : `ExpertManifestReader` (read/readOrEmpty/findByDomain) + 8 JUnit5 + 2 round-trip JUnit5 + 1 scénario Cucumber cross-borough (6/6 @epic8 PASS)
| ✅ | Session 142 — EPIC V-9.3 ScanAgent livré : `ScanAgent` scanner récursif `.agents/` (ignore build/.git/.gradle/node_modules) + `ScannedFile` value object DDD + 8 JUnit5 + 6 Cucumber @epic_v_9_3 PASS + refactor `IngestGovernanceTask.collectGovernanceFiles` (liste hardcoded supprimée, 5 tests préservés). Under-EPIC 1 complet 5/5.
| ✅ | Session 145 — EPIC V-9.9 ChunkValidationGate livré (`ChunkValidator` câblé dans `AgenticIngestor`, `IngestionReport` enrichi `chunksInvalid`/`validationErrors`, 3 JUnit5 + 2 Cucumber @epic_v_9_9 PASS, régressions V-9.3/9.5/9.7/9.8 vertes)
| ✅ | Session 146 — EPIC V-9.10 ChunkValidationReport livré (`ChunkValidationErrorType`, `lineStart`/`lineEnd`, `validationErrorsByType` JSON, 3 JUnit5 + 1 test fonctionnel + 3 Cucumber @epic_v_9_10 PASS, zéro régression V-9.x)
| ✅ | Session 153 — EPIC V-9.17 Ingestion Summary DSL livré : extension `codebaseGovernance { strictValidation.set(true) }` + propriété `codebase.governance.strictValidation`, `GovernanceSummaryConfig` DDD + `CodebaseGovernanceExtension`, câblage `IngestGovernanceTask.governanceConfig`, 3 JUnit5 + 4 fonctionnels + 3 Cucumber @epic_v_9_17 PASS, régressions V-9.3/9.5/9.7/9.8/9.9/9.10/9.11/9.13/9.14/9.15/9.16 vertes
| ✅ | Session 156 — EPIC V-9.20 Chunk diff incrémental entre sessions livré : `ChunkSnapshot`/`ChunkDiff`/`ChunkChangeDetector`/`JsonChunkStateStore`/`ChunkIncrementalReport` DDD, câblage `IngestGovernanceTask.lastChunkIncrementalReport` + `mergeChunkSnapshots` + JSON `chunkIncremental`, `GovernanceSummaryConfig.chunkIncremental` + `CodebaseGovernanceExtension.chunkIncremental` + CLI `-Pcodebase.governance.chunkIncremental=true`, `GovernanceIngestor.IngestResult` enrichi `chunkSnapshot`, `CodebasePlugin` refactor 3 propriétés CLI via `project.provider`, 40 JUnit5 + 5 fonctionnels + 5 Cucumber @epic_v_9_20 PASS, régressions V-9.3→V-9.19 vertes
| ✅ | Session 157 — Clôture EPIC V-9 : 20/20 items V-9.1→V-9.20 terminés, 5 Under-EPICs complets. Marquage CLOSED dans INDEX + BACKLOG (codebase-plugin + .agents), bilan rédigé (73 sessions, ~200+ JUnit5 + ~60+ Cucumber). `.agents/BACKLOG.adoc` aligné + 3 anti-patterns résolus. `compileKotlin` vert.
| ✅ | Session 158 — EPIC V-6-POOL terminé 4/4 : AUTHORIZED_MODELS aligné sur AGENTS.adoc (2 modèles `gpt-oss:120b-cloud` + `gemma4:31b-cloud`) dans `OllamaInstanceFactory` + `OllamaInstanceScanner`, `LlmProviderResolver` cycle les 2 modèles sur `OLLAMA_POOL_PORTS` au lieu de `DEFAULT_MODEL` statique, feature Cucumber `epic_v6_pool.feature` (3 scénarios @epic_v6_pool) + runner + step `includes model`, tous tests unitaires + Cucumber PASS, zéro régression V-6 ollama_cloud + V-6 resolver.
| ✅ | Session 159 — EPIC OCR-1/OCR-2 restants TERMINÉS (9/9 items) : HTTP 429 detection + rotation auto, env var mapping, multi-compte Google, Tesseract fallback harmonisé boundary codex↔codebase, EPIC OCR complet 7/7.
| ✅ | Session 160 — Backlog & Roadmap cleanup : 4 fichiers gouvernance mis à jour (BACKLOG/INDEX/ROADMAP), tous EPICs marqués TERMINÉ, version bump 0.0.3→0.0.4, Maven Central 0.0.4 publié (deployment f4a91bf6), Plugin Portal soumis. Borough DORMANT.
| ✅ | Session 162 — README i18n 10 langues (consumer + plugin) : 20 fichiers créés (README.consommateurs/ + README.plugin/), badges Maven Central/Portal/CI/License, markers version 0.0.4/build/tests/coverage ≥80%, switcher i18n 10 langues, AR/UR RTL. Coordination des 9 autres boroughs (200 fichiers au total). Borough DORMANT.
| ✅ | Session 163 — Architecture README Plugin + Consommateur : README.plugin/ déplacé vers codebase-plugin/ (master EN à racine plugin), README.consommateurs/README.md remonté racine borough, liens i18n recréés (10 langues × 2 audiences = 20 fichiers).
| ✅ | Session 164 — Exception CI/CD Ollama documentée (ports 11466-11467 + qwen3:0.6b pour tests d'intégration GitHub Actions uniquement, hors plage rotation 11437-11465). Workflow test.yml enrichi (cache Docker + 2 instances Ollama + env vars OLLAMA_TEST_PORT/MODEL). Fix 2 bugs Cucumber préexistants : DuplicateStepDefinition Ocr1PoolSteps vs PoolSteps (step renommé "the Gemini key pool usage is reset"), assertion stale epic_v_pool.feature (5 modèles → 2). testFast vert.
| ✅ | Session 165 — Debug CI/CD en cascade + migration BOM. 4 fixes : (1) gradle-wrapper.jar dé-gitignoré + versionné, (2) org.gradle.java.home retiré des gradle.properties (path SDKMAN inexistant CI), (3) nmcp credentials optionnels (fallback "" au lieu error()), (4) migration vers workspace-bom:0.0.3 (source unique vérité). codex-plugin:0.0.2 + llm-pool-contracts:0.0.2 + 4 contracts 0.0.2 re-publiés Central. CI en attente propagation Central.
| ✅ | Session 166 — Fix 6 scénarios Cucumber CI `full-tests` rouge. (1) TaskResultVerifier reconnaît `DRY RUN` comme SUCCESS (+2 tests unitaires), (2) VibecodingGraph `executePlanTasks` ne marque plus tâches échouées comme exécutées + `checkProgressNode` utilise `drainAutofocusStack()` (préserve focusLevel), (3) 3 scénarios dépendants de l'environnement taggés `@integration` (pgvector stop, V-6 retry, US-9.10 EAGER). 12 tests JUnit5 préexistants rouges corrigés + Z-6 (4/4) + Z-7 (4/4) Cucumber verts. testFast vert.
| ✅ | Session 167 — Fix CI `full-tests` V-6 runner `@integration` + suppression workflow `ingest-governance.yml` cassé. `EpicV6CucumberRunner` filtre `@epic_v_6 and not @integration` (scénario ligne 76 `epic_v_vibecoding.feature` lançait `./gradlew` depuis `/tmp` inexistant en CI → `MaxRetriesExhausted`). Workflow `ingest-governance.yml` supprimé (task `:codebase-plugin:ingestGovernance` inexistante, CodebasePlugin non self-applied — fichier orphelin session 160). Fast Tests ✅ local + CI. Full Test Suite CI run 28196553990 en cours validation.
| ✅ | Session 168 — Fix 19 échecs JUnit5 CI `full-tests` (run 28197805505). 5 bugs corrigés : (1) `TokenTracker.kt` `COST_PER_MILLION` deepseek→gpt-oss/gemma4/qwen3, (2) `DilutionExecutor.kt`+`StimulusCascade.kt` `Locale.FRENCH` sur `DateTimeFormatter`+`String.format` (6 tests), (3) `MigrationTest` attendus 2→4 migrations (V1-V4), (4) `KoogPlanningOrchestratorTest` accepte `ContextBuildPartial`, (5) `TaskSchemaScannerTest` attendus alignés (13 tasks plugin, 4 collect/7 generate). 7 tests `@integration` légitimes exclus du task `test` via `excludeTags` : `ExecGradleToolTest` (subprocess Gradle conflit catalog), `VibecodingGraphTest.v6 retry` (idem), `PgVectorConfigTest.fromEnv` (reflection JDK24), `LlmConfigTest` (llm-config.yml gitignoré), `MultiChannelContextGraphTest`+`GovernanceContextLoaderTest` (paths hardcodés). `test` + `testFast` verts.
| ✅ | Session 169 — CI-green confirmé + synchronisation backlog. Run CI 28206228434 : fast-tests ✅ 2m31s + full-tests ✅ 33m50s (19 fixes session 168 validés). Backlog synchronisé : EPIC 1 stale purgé (8/8 TERMINÉ), EPIC OCR stale corrigé (✅ TERMINÉ), EPIC CR stale corrigé (✅ TERMINÉ 14/14). Backlog restant = EPIC V-FLYWAY uniquement (P2 conditionnel, seuil 5+ migrations, actuellement 4). Borough DORMANT CI-green.
| ✅ | Session 170 — SEC-1 purge deepseek features + CI anonymize.yml. Bump 0.0.4→0.0.5. workspace-bom 0.0.4 + codebase-plugin 0.0.5 publiés Maven Central.
| ✅ | Session 171 — Réduction bruit pgvector logs. 8 `log.warn`→`log.debug` dans 4 fichiers (CompositeContextBuilder, KoogAugmentedContextGraph, KoogPlanningOrchestrator, MultiChannelContextGraph). Mode dégradé sans pgvector = nominal en test, messages passés en debug. compileKotlin + cucumberTestEpicVLocal PASS.
| ✅ | Session 172 — EPIC C-BOM-FIX TERMINÉ (P0 critique, débloque bakery BKY-LANG-SWITCH). Versions explicites pour 6 artefacts `education.cccp` + 27 deps externes dans `libs.versions.toml` (le BOM en `implementation(platform(...))` ne propage pas ses contraintes aux consommateurs transitifs). Bump 0.0.5→0.0.6. `publishToMavenLocal` OK. POM 0.0.6 contient les `<version>` pour les 6 artefacts + 27 deps externes. 3 nouveaux UT `VersionCatalogTransitiveVersionsTest`. 22/22 UT bakery LangSwitch GREEN. Zéro régression codebase. Borough DORMANT Maven Local.
| ✅ | Session 173 — Publication Maven Central 0.0.6 (bundle `e4263c78`). Fix POM `<name>`+`<description>` manquants (rejeté par Central Portal 1er upload `f0e0e2fd`). Ajout bloc `publishing { pom { name.set / description.set } }` dans `codebase-plugin/build.gradle.kts`. `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL. Débloque bakery BKY-LANG-SWITCH sur CI (testRuntimeClasspath résolu). Borough DORMANT Maven Central.
| ✅ | Session 174 — Code review du vibecoding + audit working tree. 10 findings (S1-S3 sécurité : blacklists bypassables, pas d'allowlist Gradle, edit/write sans garde taille ; C1-C7 : heuristique `contains("failed")`, retry double-compté, VibecodingPlan dead code, flags Gradle cassés, LLM sans timeout, duplication, compteur négatif). SEC-1 propre. Suite tests non exécutée (borough dormant). Session lecture seule.
| ✅ | Session 175 — Clôture code review vibecoding + CR-GLOBALE-004. Gate tests `codebase.koog.*` + `contracts.vibecoding.*` verds (5m44s, Kotlin 2.4.10). Commit atomique `b12a092` (Kotlin 2.4.10 centralisé + POM publishing + LlmTranslator prompt). CR-GLOBALE-004 archivée dans GLOBAL_CODE_REVIEW.adoc racine. Working tree propre. Borough DORMANT maintenu. Backlog EPIC VIBE-HARDENING ajouté.
| ✅ | Session 184 — EPIC VIBE-HARDENING US-0 cadrage (gouvernance pure, zéro code). 3 constats P1 audités (S1 blacklists bypassables, C2 retry double-compté, C5 LLM sans timeout) + 7 rappels P2/P3 + 10 décisions architecturales. Document `CADRAGE_EPIC_VIBE_HARDENING.adoc`. 6 US ~12 pts découpees. Pattern S-057/S-063.
| ✅ | Session 185 — EPIC VIBE-HARDENING US-1 ExecShellTool allowlist. 10 regex `BASH_BLACKLIST` remplacées par `SHELL_ALLOWLIST` (10 patterns) + `SHELL_DENYLIST` (10 patterns second ride). `validateCommand` allowlist deny-by-default + deny-list second ride. `ExecShellToolTest` 11→17 tests. Gate `codebase.koog.tools.*` VERT 21 tests + `ToolRegistryTest` 8/8 non-régression. Zéro bump BOM.
| ✅ | Session 186 — EPIC VIBE-HARDENING US-2 ExecGradleTool allowlist. 4 regex `GRADLE_BLACKLIST` remplacées par `GRADLE_TASK_ALLOWLIST` (bare tasks + project:task) + `GRADLE_ARG_ALLOWLIST` (-Pprop + --flag) + `GRADLE_DENYLIST` (8 patterns second ride : clean build, --refresh-dependencies, rm, delete, sudo, curl, ;, |). `validateGradleTask` split tokens + allowlist par position. `ExecGradleToolTest` 6→17 tests (15/15 PASSED). Gate `codebase.koog.tools.*` VERT 32 tests + `ToolRegistryTest` 8/8 non-régression. Zéro bump BOM.
| ✅ | Session 187 — EPIC VIBE-HARDENING TERMINÉ 6/6 US (US-3+US-4+US-5 en 1 session, pattern S-113). US-3 C5 Timeout LLM : `withTimeout(llmTimeoutMs)` sur 3 sites `runBlocking { llmProvider.call }` (callLLMNode, error recovery replan, StepVerifier) + constructor param `llmTimeoutMs: Long = 30_000L` (VibecodingGraph + StepVerifier, propagation via default param) + catch `TimeoutCancellationException` → error/lastToolResult "LLMTimeout: ${ms}ms exceeded". US-4 C2 Retry single counter : supprimé `retryCount = state.retryCount + 1` dans executePlanTasks (2 sites) + `incrementRetry()` consolidé en début de bloc error recovery (1 incrément par essai au lieu de 2). US-5 Cucumber BDD : `codebase_vibe_hardening.feature` 4 scénarios `@vibe-hardening` + `VibeHardeningWorld` + `VibeHardeningSteps` (14 step defs préfixés "vibe hardening", bug S-088) + `VibeHardeningCucumberRunner` (pattern S-082) + `CucumberTaskSpec("cucumberTestVibeHardening")` 37e. Gate `codebase.koog.tools.*` + VibecodingGraph + StepVerifier VERT 64 tests 0 FAIL (17 ExecShellTool + 15 ExecGradleTool + 22 VibecodingGraph + 10 StepVerifier) + 4/4 cucumber PASSED. Zéro bump BOM, zéro secret, zéro régression.
| ✅ | Session 189 — EPIC VIBE-HARDENING-2 US-1 + US-2 (pattern S-113 multi-US 1 session). US-1 S2 Allowlist TaskDiscoveryRegistrar : `TASK_ALLOWLIST` (2 regex bare tasks + project:task) + `TASK_DENYLIST` (6 regex publishToMavenCentral/publishAggregation/clean/purge/wrapper/bootstrap) + `validateTaskName` companion (allowlist deny-by-default + deny-list second ride, pattern `ExecGradleTool.validateGradleTask`) + `registerAll` filtre via validateTaskName (skip denied). 18 unit allowlist + 6 tests existants réécrits sécuritaires (vibecode/qualityGate devenus denied — tasks métier plugin ne sont plus auto-enregistrées, sécuritaire). US-2 S3 Garde taille write_file/edit_file : `MAX_WRITE_CHARS = 1_000_000` const companion + 3 checks (write_file content, edit_file newString, edit_file resulting updated) → `IllegalArgumentException` avant writeText. Pattern `read_file MAX_OUTPUT_CHARS` mirror. +5 unit. Gate `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 105 tests (1 fail préexistant C4 US-3 GradleTaskArgsMapperTest). 2/8 US VIBE-HARDENING-2. Zéro bump BOM, zéro secret, zéro régression.
| ✅ | Session 190 — EPIC VIBE-HARDENING-2 US-3 C4 GradleTaskArgsMapper flags cassés (P2, 3 pts, baby-step TDD strict RED → GREEN → REFACTOR). `GRADLE_ARG_ALLOWLIST` étendu `--\w+` → `--\w+(?:=\S+)?` (flag seul ou `--flag=value`) + `isBareLongFlag(token)` helper (détecte flag sans `=` précédant une valeur séparée) + `validateGradleTask` refondu : un token d'arg qui ne matche pas l'allowlist est accepté si le token précédent est un `bareLongFlag`, à condition de ne pas commencer par `-` et de ne pas contenir `;&|` (defense in depth préservée). 5 nouveaux tests `ExecGradleToolTest` (3 positifs pair flag+value/flag=value/mixed + 2 négatifs bare value/standalone token). **Fail préexistant C4 résolu** : `GradleTaskArgsMapperTest > execute structured gradle task via handler()` PASSED (subprocess Gradle `test --tests FakeTestClassName` ne lance plus `SecurityException`). Description `ExecGradleTool` mise à jour. Gate combinée `codebase.koog.tools.*` + `codebase.koog.discovery.*` + `ToolRegistryTest` VERT 110 tests 0 FAIL (20 ExecGradleTool + 17 ExecShellTool + 60 discovery + 13 ToolRegistry). Zéro bump BOM, zéro secret, zéro régression. 3/8 US VIBE-HARDENING-2.
| ✅ | Session 195 — EPIC FT-PIPELINE US-3 `FineTuningGraph` (koog itératif). 5 fichiers prod créés (`FineTuningStage` enum 6 stages, `FineTuningState` data class 11 champs + 10 invariants croisés, `FineTuningPromptBuilder` port, `FineTuningLlm` port, `FineTuningGraph` graphe koog `AIAgentGraphStrategy` 3 nœuds propose/train/validate + 7 edges conditionnels + `execute` séquentiel pattern `KoogAugmentedContextGraph` + `asMermaidDiagram` + `adjustedCorpusRatio` parser) + 2 fichiers test (`FineTuningStateTest` 18 unit, `FineTuningGraphTest` 17 unit). Bug loop invariant découvert RED : re-entry `proposeNode` cassait `trainResult non-null requires stage >= TRAINED` — fix `state.copy(proposal="", trainResult=null, validationScore=0.0, stage=INITIALIZED)`. Gate `codebase.finetuning.*` 78/78 PASSED (+35 nets : 17 graph + 18 state), 0 FAIL. Domaine `codebase.finetuning` enrichi (7 prod + 5 test). Zéro bump BOM, zéro secret, zéro régression. EPIC FT-PIPELINE 3/7 US (US-0 ✅ S-188, US-1 ✅ S-193, US-2 ✅ S-194, US-3 ✅ S-195, US-4→6 À FAIRE).
| ✅ | Session 198 — EPIC FT-PIPELINE US-6 TERMINÉ (cross-borough training-gradle). TRN-8 reformulé : "Pipeline fine-tuning ~21 pts" → "Recettes CDA/FPA consommant FT-PIPELINE" (~10 pts). Séparation N1/N2 actée : machinerie générique N1 dans codebase (FT-PIPELINE TERMINÉ 7/7 US S-188→S-198), recettes métier N2 dans training (datasets corpus AFNOR/REAC + modèles de base + noms experts + curation Bloom/Qualiopi). Dépendance N2→N1 propre (training appelle `fineTuneExpert` de codebase via DSL, spécifie datasets/modèles). Littérature agentique préparatoire posée dans BACKLOG training (constat N1/N2 + plan baby-steps TRN-8.0→8.6), démarrage coding reporté post-travaux codex prioritaires. INDEX/PROMPT_REPRISE training mis à jour. EPIC FT-PIPELINE TERMINÉ 7/7 US. Zéro code modifié — gouvernance pure cross-borough pattern S-057.
| ✅ | Session 199 — EPIC CDX-RAG-SOCLE US-1 (cross-borough Brooklyn pilote). Domaine `codebase.store` créé (5 prod + 3 test, ~330 lignes) : migration verbatim de `codex.store.CodexVectorStore` → `codebase.store.RagVectorStore` + `StoreStatements` (ex-`IngestStatements`) + `IngestIndexing` + types autonomes `RetrieveResult`/`DocumentChunk`. Tables pgvector `codex_documents`/`codex_chunks` INCHANGÉES (Loi de l'Économie d'Encre, zéro re-vectorisation). Baby-step TDD strict RED → GREEN → REFACTOR : 25/25 characterization tests PASSED (6 `IngestIndexingTest` + 9 `StoreStatementsTest` + 10 `RagVectorStoreTest`), mirror des tests codex. Pré-requis bump versions : codex 0.0.5 → mavenLocal, codebase `libs.versions.toml` BOM 0.0.22→0.0.24 + codex 0.0.3→0.0.5, codebase 0.0.8 republié mavenLocal. `compileKotlin`/`compileTestKotlin` VERT, gate `codebase.rag.*` non-régression VERT. Zéro régression (call sites `CodexVectorStore` inchangés — bascule RAG-2), zéro bump BOM (pré-requis), zéro secret. EPIC CDX-RAG-SOCLE 2/6 US.
| ✅ | Session 203 — Maintenance : complétion WIP `BenchmarkRunner.kt` vers socle `codebase.store.RagVectorStore` (pattern codex `searchBlocking`), gate worktree isolé VERT. Interruption conservatoire — session codex (Brooklyn) éditait codebase en parallèle (migration legacy VectorStore en cours). Constat PUB : POM Central 0.0.8 contient encore `codex-plugin:0.0.2` → bump 0.0.9 prioritaire S-204 ; `codex-plugin:0.0.6` absent de Central malgré claim Brooklyn S-104.
| ✅ | Session 202 — EPIC CDX-OCR-CONTRACTS US-4 TERMINÉ (pure BDD, zéro prod modifié). `codebase_ocr_contracts.feature` 5 scénarios `@ocr-contracts` (fake AI engine AsciiDoc via port N0, miroir métadonnées requête→résultat, port stateless réutilisable 2 appels FIFO, rejet provider logiciel tesseract → erreur "codex", Gemini+Ollama failing → AI-only pointant vers codex collectOcr) + `OcrContractsCucumberRunner` (pattern S-082, 41e `CucumberTaskSpec("cucumberTestOcrContracts")`) + `OcrContractsWorld`/`StubVisionProvider` (fake AI engine FIFO + call count) + `OcrContractsSteps` (préfixés S-088). Baby-step BDD strict RED (5 UndefinedStepException / 23 steps) → GREEN (5/5 PASSED) → REFACTOR (néant). Gates : adapter unit 4/4 + OcrTaskAiOnlyBoundaryTest 2/2 + cucumberTestEpicOcr 29/29 VERT. Écart mission : « cache hit » non testable codebase-side (cache modèle exige clé+réseau ; cache hash côté codex) → scénario stateless équivalent. **EPIC CDX-OCR-CONTRACTS TERMINÉ 5/5 US** (US-1 MEMPHIS artefact N0, US-2 typealiases codex, US-3 bascule imports + retrait libs.codex.plugin, US-4 Cucumber BDD ; publications Central en attente accord pilote). Zéro bump BOM, zéro secret.

=== EPICs & Projects

[cols="1,4,1,1,1"]
|===
| EPIC | Sujet | Portee | Status | Sessions
| EPIC V | Vibecoding Task + Graph | N1 | 15/15 PTS | 041-045
| EPIC-V-SESSION-REPO | SessionRepository + TokenTracker + Dashboard | N1 | 3/3 pts | 059
| EPIC-V-FLYWAY | Migration Flyway (futur) | N2 | BACKLOG | —
| EPIC-V-9 | Vibecoding Gouvernance Agentique | N1 | ✅ TERMINE (20/20 items — Under-EPIC 1-5 tous complets V-9.1→V-9.20). Clôturé session 157. | 084-156
| EPIC K | Ontologie Workspace | NX | ✅ TERMINE | 015-020
| EPIC L | Migration langgraph4j → koog | NX | ✅ TERMINE | 034-049
| EPIC W | Auto-discovery Gradle Tasks | N1 | ✅ TERMINE | 098-100
| EPIC X | Decision Multi-Step | N1 | ✅ TERMINE | 101-105
| EPIC Y | Agentic Literature Compiler | N1 | ✅ TERMINE | 088-097
| EPIC Z | Autofocus | N1 | ✅ TERMINE | 106-108
| EPIC OCR | OCR Assisté IA (Gemini Vision + Ollama qwen3-vl) | N1 | ✅ TERMINÉ (OCR-0→4 + OCR-2b + OCR-3b, sessions 103, 115-120, 159) | 103, 115-120, 159
| EPIC PUB | Publication Maven Central | NX | ✅ TERMINÉ — codebase-plugin 0.0.3 Central (session 135), 0.0.4 Central + Plugin Portal (session 160) | 129, 134, 135, 160
| EPIC V-LOCAL | Vibecoding local auto-alimenté par gouvernance textuelle | N1 | ✅ TERMINÉ (V-LOCAL-0→6 tous livrés) | 129-132
| EPIC CR | Code Review Deep Analysis (14/14 items) | N1 | ✅ TERMINÉ | 119-125
| EPIC SP | opencode↔Gradle Thin Client (SessionProtocol) | N1 | ✅ TERMINE (fallback governance ajouté session 131) | 112-114, 131
| EPIC 8 | Expert Exposure via Ollama (manifest + reader cross-borough) | N1 | ✅ TERMINÉ (4/4 — ExpertManifestReader + round-trip + Cucumber cross-borough) | 127, 140
| EPIC TRAD | TranslationService cross-borough (contrat N0 + impl N1) | N1 | ✅ TERMINÉ (baby-step, débloque bakery US-4) | 133
| EPIC V-6-POOL | Adaptation Ollama Cloud Pool pour Vibecoding | N1 | ✅ TERMINÉ (4/4 items — AUTHORIZED_MODELS aligné sur AGENTS.adoc 2 modèles, LlmProviderResolver cycling, Cucumber epic_v6_pool) | 077, 158
| EPIC C-BOM-FIX | POM transitif versions manquantes | NX | ✅ TERMINÉ (3/3 US, 8/8 pts — versions explicites dans libs.versions.toml, bump 0.0.6, 22/22 UT bakery LangSwitch GREEN) | 172
| EPIC VIBE-HARDENING | Durcissement vibecoding (CR-GLOBALE-004) | N1 | ✅ TERMINÉ (6/6 US — US-0 cadrage S-184, US-1 shell allowlist S-185, US-2 gradle allowlist S-186, US-3 timeout LLM S-187, US-4 retry single counter S-187, US-5 cucumber BDD S-187) | 184-187
| EPIC VIBE-HARDENING-2 | Durcissement vibecoding P2/P3 (CR-GLOBALE-004 suite) | N1 | ✅ TERMINÉ (8/8 US — US-0 cadrage S-188, US-1 allowlist TaskDiscoveryRegistrar S-189, US-2 MAX_WRITE_CHARS S-189, US-3 GradleTaskArgsMapper flags cassés S-190, US-4 TaskResultVerifier refonte S-191, US-5 cleanup doc S-192, US-6 coerceAtLeast S-192, US-7 SessionProtocolEngine S-192, US-8 Cucumber BDD S-192) | 188-192
| EPIC FT-PIPELINE | Pipeline de fine-tuning N1 (P0 vision) | N1 | ✅ TERMINÉ (7/7 US — US-0 cadrage S-188, US-1 ✅ S-193 domaine `codebase.finetuning` + merger 4 sources, US-2 ✅ S-194 port `FineTuningPipeline` + `OllamaFineTunerAdapter` + `OllamaRegistryClient`/`HttpRegistryClient`, US-3 ✅ S-195 `FineTuningGraph` koog itératif 3 nœuds propose/train/validate + loop backtracking + `FineTuningState`/`FineTuningStage` + ports `FineTuningPromptBuilder`/`FineTuningLlm` + 35 unit, US-4 ✅ S-196 tâches Gradle `prepareFineTuningDataset`/`fineTuneExpert`/`publishExpertToOllama` + extension DSL 10 propriétés + câblage `CodebasePlugin` 47 unit + 3 functional, US-5 ✅ S-197 Cucumber BDD 4 scénarios `@finetuning` + `FineTuningCucumberRunner` 39e `CucumberTaskSpec`, US-6 ✅ S-198 cross-borough training-gradle TRN-8 reformulé en recettes CDA/FPA consommant FT-PIPELINE + littérature agentique préparatoire + démarrage coding post-travaux codex) | 188, 193, 194, 195, 196, 197, 198
| EPIC CDX-RAG-SOCLE | RAG appartient au socle N1 (cross-borough Brooklyn) | N1 | ✅ TERMINÉ (6/6 US — US-0 cadrage S-097 codex, US-1 ✅ S-199 domaine `codebase.store` migration verbatim, US-2 ✅ S-200 bascule 6 call sites, US-3 ✅ S-102 codex bascule+purge `codex.store.*`, US-4 ✅ S-201 retrait final `libs.codex.plugin` via N0 `ocr-contracts`, US-5 ✅ S-200 Cucumber BDD 6 scénarios. **Inversion N1→N2 morte** — flèche unique codex→codebase) | 097, 102, 199, 200, 201
| EPIC CDX-OCR-CONTRACTS | Extraction port OCR en artefact N0 `ocr-contracts` (cross-borough MEMPHIS+codex+codebase) | N0/N1/N2 | ✅ TERMINÉ (5/5 US — US-1 MEMPHIS artefact N0 S-103/S-104, US-2 codex typealiases S-104, US-3 codebase bascule + retrait `libs.codex.plugin` S-201, US-4 Cucumber BDD boundary 5 scénarios S-202). **Publications Central en attente accord pilote** (ocr-contracts 0.0.1, BOM 0.0.26, codex 0.0.6) | 102, 103, 104, 201, 202
| EPIC 1 | Init gouvernance agent | NX | ✅ TERMINÉ (items stale purgés session 169) | 001
|===