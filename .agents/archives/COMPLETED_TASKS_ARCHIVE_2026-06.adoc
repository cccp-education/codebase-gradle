= COMPLETED TASKS ARCHIVE — Juin 2026 — Codebase Gradle
:date: 2026-06-17

== Session 129 — EPIC PUB Preflight

* **Type** : Reprise + déblocage publication
* **Livrables** :
** Fix conflit `org.jetbrains:annotations:13.0` vs `26.0.2-1` (`codebase-plugin/build.gradle.kts`)
** Alignement `codex-plugin` `0.1.0-SNAPSHOT` → `0.0.1` (`gradle/libs.versions.toml`)
** Correction 8 erreurs `validatePlugins` (annotations Gradle sur 6 tâches)
** `publishToMavenLocal` signé et réussi
** Mise à jour gouvernance : archive 129, INDEX, PROMPT_REPRISE, SESSIONS_HISTORY

* **Commandes clés** :
** `./gradlew :codebase-plugin:build -x test -x koverVerify -x koverHtmlReport`
** `./gradlew :codebase-plugin:publishToMavenLocal`

* **Statut** : ✅ Terminé

== Session 130 — Koog 1.0.0 forwardTo

* **Type** : Maintenance EPIC L
* **Livrables** :
** Retrait import `forwardTo` dans 5 fichiers (koog 1.0.0)
** Build compile et tests compilent OK
* **Statut** : ✅ Terminé

== Session 131 — GovernanceContextLoader fallback + ports/models compliance

* **Type** : Feature + compliance
* **Livrables** :
** Câblage `GovernanceContextLoader` dans `SessionProtocolTask` et `SessionProtocolServer`
** Fallback auto quand aucun contexte n'est fourni
** Tests unitaires/fonctionnels ciblés PASS
** Correction ports Ollama 11434 → 11437-11465
** Correction modèles → `gpt-oss:120b-cloud`
** Mise à jour gouvernance : archive 131, INDEX, PROMPT_REPRISE, SESSIONS_HISTORY, TEST_COVERAGE_ANALYSIS
* **Commandes clés** :
** `./gradlew :codebase-plugin:compileKotlin :codebase-plugin:compileTestKotlin`
** `./gradlew :codebase-plugin:test --tests "codebase.koog.SessionProtocolTaskTest"`
** `./gradlew :codebase-plugin:test --tests "codebase.koog.SessionProtocolServerTest"`
* **Statut** : ✅ Terminé (Cucumber non finalisé — timeout lifecycle sans pgvector)

== Session 134 — Doublon + catalogue stale + Publication N0 Central

* **Type** : Maintenance + Publication Maven Central
* **Boroughs** : Queens (codebase-gradle N1) + MEMPHIS (workspace-bom N0)
* **Livrables** :
** `FakeAgenticChunkRepository.kt` (src/test, 73 lignes) supprimé — strict doublon de `InMemoryAgenticChunkRepository.kt` (src/main). Migration des 2 tests consommateurs (`AgenticIngestorTest`, `AgenticExternalImporterTest`), 14/14 PASS.
** `codebase-plugin/gradle/libs.versions.toml` supprimé — catalogue stale jamais résolu par Gradle (langchain4j 1.14.1 vs 1.16.3 utilisé).
** Diagnostic EPIC PUB option B (nmcp) : 2 deps N0 manquantes sur Central (`opencode-session-contracts:0.0.1` + `i18n-contracts:0.0.1`), supprimées de workspace-bom en session 084 mais toujours référencées par codebase + bakery.
** Restauration modules N0 depuis git history MEMPHIS :
--- `git checkout 5472115 -- opencode-session-contracts/` (6 classes main + tests)
--- `git checkout 633219d -- i18n-contracts/` (5 classes main + tests)
--- Ajout `TranslationService.kt` dans i18n-contracts (depuis code local session 133)
** Publication Maven Central via nmcp :
--- `opencode-session-contracts:0.0.1` — deployment 97aceef0, HTTP 200 ✅
--- `i18n-contracts:0.0.1` — deployment 01a0198a, propagation en cours
* **Commandes clés** :
** `git rm codebase-plugin/src/test/kotlin/codebase/koog/agentic/FakeAgenticChunkRepository.kt`
** `./gradlew :codebase-plugin:test --tests "codebase.koog.agentic.AgenticIngestorTest" --tests "codebase.koog.agentic.AgenticExternalImporterTest"`
** `./gradlew publishAggregationToCentralPortal --no-daemon` (depuis chaque module N0)
* **Statut** : ✅ Terminé (EPIC PUB codebase-plugin lui-même reporté à session 135)

== Session 135 — EPIC PUB clôturé + reprise EPIC V-6

* **Type** : Clôture publication + reprise backlog
* **Boroughs** : Queens (codebase-gradle N1)
* **Livrables** :
** `codebase-plugin:0.0.3` HTTP 200 confirmé sur Maven Central
** EPIC PUB passé à ✅ TERMINÉ dans `.agents/INDEX.adoc` et `codebase-plugin/BACKLOG.adoc`
** Configuration nmcp validée comme méthode éprouvée (aucun remplacement nécessaire)
** Build propre (`./gradlew build -x test -x cucumberTest -x koverVerify -x validateDependencies --no-daemon`)
** Analyse EPIC V-6 et définition du prochain baby-step (`OllamaInstanceScanner.kt` + tests JUnit5)
* **Commandes clés** :
** `curl -s -o /dev/null -I -L -w "%{http_code}" "https://repo1.maven.org/maven2/education/cccp/codebase-plugin/0.0.3/"` → 200
** `./gradlew build -x test -x cucumberTest -x koverVerify -x validateDependencies --no-daemon` → BUILD SUCCESSFUL
* **Statut** : ✅ Terminé (prochaine session : implémentation `OllamaInstanceScanner`)

== Session 138 — EPIC V-6 OllamaInstanceFactory + Cucumber Ollama cloud quota

* **Type** : Feature DDD/TDD/BDD
* **Boroughs** : Queens (codebase-gradle N1), MEMPHIS (workspace-bom N0)
* **Livrables** :
** Article 0120 lu et pattern canonique dual-abonnement Ollama Pro Docker extrait (port + volume SSH).
** Contrat N0 `contracts.llmpool.LlmInstance` : ajout champ optionnel `volumeTag: String?` (backward-compatible).
** `llm-pool-contracts:0.0.1` rebuild + `publishToMavenLocal`.
** `OllamaInstanceFactory` (singleton DDD) générant 29 instances Ollama Cloud sur ports 11437→11465, modèles cyclés, `volumeTag` métier.
** `OllamaInstanceFactoryTest` : 7 JUnit5 PASS.
** Feature Cucumber `epic_v6_ollama_cloud.feature` (2 scénarios) + `OllamaCloudWorld` + `OllamaCloudSteps` + `EpicV6OllamaCloudCucumberRunner` : zéro appel réseau, 2 scénarios PASS.
** Mise à jour gouvernance : archive 138, INDEX, PROMPT_REPRISE, SESSIONS_HISTORY, TEST_COVERAGE_ANALYSIS.
* **Commandes clés** :
** `./gradlew :llm-pool-contracts:build -x test --no-daemon && ./gradlew :llm-pool-contracts:publishToMavenLocal --no-daemon` (workspace-bom)
** `./gradlew :codebase-plugin:compileKotlin :codebase-plugin:compileTestKotlin --no-daemon`
** `./gradlew :codebase-plugin:test --tests "codebase.koog.llm.pool.OllamaInstanceFactoryTest" --no-daemon`
** `./gradlew :codebase-plugin:test --tests "codebase.scenarios.EpicV6OllamaCloudCucumberRunner" --no-daemon`
** `./gradlew :codebase-plugin:test --tests "codebase.koog.llm.pool.*" --no-daemon`
** `./gradlew build -x test -x cucumberTest -x koverVerify --no-daemon`
* **Statut** : ✅ Terminé

== Notes de période

* `.agents/` non versionné jusqu'à la session 129 ; historique des sessions 085-128 présent dans le code source et `BACKLOG.adoc` mais pas archivé dans `.agents/sessions/`.
* EPIC PUB passe de ✅ AUDITE à 🔄 PREFLIGHT OK, puis ⏸️ DIFFÉRÉ, puis 🔄 EN COURS (session 134 : N0 restaurés et publiés, codebase-plugin à publier en session 135), puis ✅ TERMINÉ (session 135 : HTTP 200 Central).
* Pivot stratégique : priorité donnée à la boucle vibecoding locale auto-alimentée (EPIC V-LOCAL) et au pont vers la gouvernance augmentée (EPIC Y).
* Session 134 : anti-pattern détecté — un artefact N0 supprimé de workspace-bom peut encore être référencé via MavenLocal par d'autres boroughs. Le build compile mais la publication Central casse.
* Session 138 : évolution backward-compatible d'un contrat N0 publié (`LlmInstance.volumeTag`) via champ nullable + `mavenLocal`, sans publication Central immédiate.

== Session 156 — EPIC V-9.20 Chunk diff incrémental entre sessions

* **Type** : Feature EPIC V-9 Under-EPIC 5
* **Livrables** :
** 5 value objects/services DDD : `ChunkSnapshot`/`ChunkSnapshotEntry`, `ChunkDiff`, `ChunkChangeDetector`, `JsonChunkStateStore`, `ChunkIncrementalReport`
** Câblage `IngestGovernanceTask` : `lastChunkIncrementalReport`, `mergeChunkSnapshots`, JSON `chunkIncremental`
** `GovernanceSummaryConfig.chunkIncremental` + `CodebaseGovernanceExtension.chunkIncremental` + CLI `-Pcodebase.governance.chunkIncremental=true`
** `GovernanceIngestor.IngestResult` enrichi `chunkSnapshot`
** `CodebasePlugin` refactor 3 propriétés CLI via `project.provider`
** Cucumber `@epic_v_9_20` (5 scénarios) + Runner + Steps + tâche `cucumberTestEpicV920`
** `IngestGovernanceWorld` étendu
* **Commandes clés** :
** `./gradlew :codebase-plugin:test --tests "codebase.koog.agentic.ChunkSnapshotTest"`
** `./gradlew :codebase-plugin:cucumberTestEpicV920`
** `./gradlew :codebase-plugin:jar`
* **Statut** : ✅ Terminé
